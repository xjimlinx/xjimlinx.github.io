<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Lua学习笔记</title>
    <link href="/2024/11/15/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/lua/"/>
    <url>/2024/11/15/%E7%BC%96%E7%A8%8B%E8%AF%AD%E8%A8%80/lua/</url>
    
    <content type="html"><![CDATA[<h1 id="Lua学习笔记"><a href="#Lua学习笔记" class="headerlink" title="Lua学习笔记"></a>Lua学习笔记</h1><p>TODO:</p>]]></content>
    
    
    <categories>
      
      <category>编程语言</category>
      
      <category>Lua</category>
      
    </categories>
    
    
    <tags>
      
      <tag>编程</tag>
      
      <tag>基础</tag>
      
      <tag>Lua</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Shell常用工具指南</title>
    <link href="/2024/11/15/Shell%E5%B8%B8%E7%94%A8%E5%B7%A5%E5%85%B7%E6%8C%87%E5%8D%97/"/>
    <url>/2024/11/15/Shell%E5%B8%B8%E7%94%A8%E5%B7%A5%E5%85%B7%E6%8C%87%E5%8D%97/</url>
    
    <content type="html"><![CDATA[<h1 id="Shell常用工具指南"><a href="#Shell常用工具指南" class="headerlink" title="Shell常用工具指南"></a>Shell常用工具指南</h1><h2 id="一、常用Shell介绍"><a href="#一、常用Shell介绍" class="headerlink" title="一、常用Shell介绍"></a>一、常用Shell介绍</h2><h3 id="1-0-sh"><a href="#1-0-sh" class="headerlink" title="1.0 sh"></a>1.0 sh</h3><h3 id="1-1-bash"><a href="#1-1-bash" class="headerlink" title="1.1 bash"></a>1.1 bash</h3><h3 id="1-2-fish"><a href="#1-2-fish" class="headerlink" title="1.2 fish"></a>1.2 fish</h3><h3 id="1-3-zsh"><a href="#1-3-zsh" class="headerlink" title="1.3 zsh"></a>1.3 zsh</h3><h3 id="1-4-posh"><a href="#1-4-posh" class="headerlink" title="1.4 posh"></a>1.4 posh</h3><h2 id="二、常用Shell-命令行工具"><a href="#二、常用Shell-命令行工具" class="headerlink" title="二、常用Shell 命令行工具"></a>二、常用Shell 命令行工具</h2><p>根据入门到熟练的所需使用频率做出了如下近似排序（可能会与其他人略有出入）</p><h3 id="2-1-文件管理相关"><a href="#2-1-文件管理相关" class="headerlink" title="2.1 文件管理相关"></a>2.1 文件管理相关</h3><h4 id="2-1-1-基础"><a href="#2-1-1-基础" class="headerlink" title="2.1.1 基础"></a>2.1.1 基础</h4><ol><li>cd</li><li>pwd</li><li>ls</li><li>mv</li><li>cp</li><li>touch</li><li>mkdir</li><li>cat</li><li>rm &amp; rmdir</li><li>ln</li><li>tree</li></ol><h4 id="2-1-2-进阶"><a href="#2-1-2-进阶" class="headerlink" title="2.1.2 进阶"></a>2.1.2 进阶</h4><ol><li>which</li><li>file</li><li>grep</li><li>head &amp; tail</li><li>stat：打印信息节点的内容</li></ol><h4 id="2-1-3-深入"><a href="#2-1-3-深入" class="headerlink" title="2.1.3 深入"></a>2.1.3 深入</h4><ol><li>sed</li><li>awk</li></ol><h3 id="2-2-进程管理相关"><a href="#2-2-进程管理相关" class="headerlink" title="2.2 进程管理相关"></a>2.2 进程管理相关</h3><ol><li>ps</li><li>top</li></ol><h3 id="2-3-其他"><a href="#2-3-其他" class="headerlink" title="2.3 其他"></a>2.3 其他</h3><ol><li>sudo</li></ol><h3 id="三、拓展（针对-zsh）"><a href="#三、拓展（针对-zsh）" class="headerlink" title="三、拓展（针对 zsh）"></a>三、拓展（针对 zsh）</h3><h4 id="3-1-oh-my-zsh"><a href="#3-1-oh-my-zsh" class="headerlink" title="3.1 oh-my-zsh"></a>3.1 oh-my-zsh</h4><h4 id="3-2-powerlevel10k"><a href="#3-2-powerlevel10k" class="headerlink" title="3.2 powerlevel10k"></a>3.2 powerlevel10k</h4><h4 id="3-3-zsh-users"><a href="#3-3-zsh-users" class="headerlink" title="3.3 zsh-users"></a>3.3 zsh-users</h4><h5 id="3-3-1-zsh-syntax-highlighting"><a href="#3-3-1-zsh-syntax-highlighting" class="headerlink" title="3.3.1 zsh-syntax-highlighting"></a>3.3.1 zsh-syntax-highlighting</h5><h5 id="3-3-2-zsh-autosuggetions"><a href="#3-3-2-zsh-autosuggetions" class="headerlink" title="3.3.2 zsh-autosuggetions"></a>3.3.2 zsh-autosuggetions</h5>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
      <category>Linux</category>
      
      <category>Shell</category>
      
    </categories>
    
    
    <tags>
      
      <tag>基础</tag>
      
      <tag>Shell</tag>
      
      <tag>Linux</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>FS 文件系统部分</title>
    <link href="/2024/11/14/FS%20%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E9%83%A8%E5%88%86/"/>
    <url>/2024/11/14/FS%20%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E9%83%A8%E5%88%86/</url>
    
    <content type="html"><![CDATA[<h1 id="FS-文件系统部分"><a href="#FS-文件系统部分" class="headerlink" title="FS 文件系统部分"></a>FS 文件系统部分</h1><blockquote><p>内容来源于阅读 NPUcore 操作系统内核构建实践书籍的笔记。</p><p>当一段内容不够熟或比较不理解或者还没办法完全记忆下来时，可能会<strong>原封不动摘录</strong>。</p></blockquote><h2 id="写在〇之前"><a href="#写在〇之前" class="headerlink" title="写在〇之前"></a>写在〇之前</h2><p>因为操作系统是一个工程性的问题，要实现同一个功能可能有（也应当有）许多种不同的方法，这些方法可能大同小异，也可能相距甚远。在笔者目前学习操作系统设计所面临的最大的问题，就是要记忆一大堆管理逻辑。这些管理逻辑或许有其他的平替，也可能这些管理逻辑可以组成一个耦合度不低的管理逻辑集合，同样可以被其他的集合平替，很多时候不知道为什么这样设计（当然有可能是学习操作系统这门课时没有好好学的问题），所以理解记忆起来会十分麻烦，当务之急是先将这些逻辑记忆起来，再不济记忆逻辑之间的关系，后面再来细究（毕竟再过一个礼拜就到自己写的截止期了）</p><h2 id="〇、概述"><a href="#〇、概述" class="headerlink" title="〇、概述"></a>〇、概述</h2><p>内核添加文件系统的大致开发过程</p><h4 id="第一步：能够写出与文件访问相关的应用"><a href="#第一步：能够写出与文件访问相关的应用" class="headerlink" title="第一步：能够写出与文件访问相关的应用"></a>第一步：能够写出与文件访问相关的应用</h4><h4 id="第二步：实现-easyfs-文件系统"><a href="#第二步：实现-easyfs-文件系统" class="headerlink" title="第二步：实现 easyfs 文件系统"></a>第二步：实现 easyfs 文件系统</h4><blockquote><p>可以先在用户态实现文件系统，并在用户态完成文件系统功能的基本测试并基本验证其实现正确性之后，将该模块嵌入到系统内核中。然后实现文件系统对接的接口，这样就可以让操作系统拥有一个简单可用的文件系统.</p></blockquote><p>文件系统的整体架构自下而上可分为五层：</p><ul><li>磁盘块设备接口层：读写磁盘块设备的trait接口</li><li>块缓存层：位于内存的磁盘块数据缓存</li><li>磁盘数据结构层：表示磁盘文件系统的数据结构</li><li>磁盘块管理器层：实现对磁盘文件系统的管理</li><li>索引节点层：实现文件创建&#x2F;打开&#x2F;读写等操作</li></ul><p><strong>最底层</strong>（<strong>磁盘块设备接口层</strong>）就是对块设备的访问操作接口。包括：</p><ul><li>read_block 函数：将数据从设备读取到内存缓冲区中</li><li>write_block 函数：将数据从内存缓冲区写入到设备中</li></ul><p>数据需要以块为单位进行读写。</p><p>然后，为了让程序可以<strong>自动化管理</strong>，以及增加编程正确性——当一个块内容被程序读取到缓冲区，并修改，还未写回到块设备的时候，另一个程序将该块的内容再读到另一个缓冲区，而不是使用第一个程序的缓冲区内的数据，将会造成<strong>数据不一致</strong>。所以添加<strong>第二层抽象层——块缓存层</strong>。</p><p>在<strong>块缓存</strong>基础之上，在内存中方便地处理文件系统在磁盘上的各种数据，就是<strong>第三层抽象层——磁盘数据结构层</strong>。</p><p>文件系统中所有需要持久保存的数据都会放到磁盘上，包括管理文件系统的<strong>超级块（Super Block）</strong>，<strong>索引节点位图区</strong>和<strong>数据块位图区</strong>（管理空闲磁盘块），以及<strong>索引节点区</strong>（管理文件）和<strong>数据块区</strong>（放置文件数据）。管理上述这些磁盘数据的控制逻辑主要集中在<strong>第四层抽象层——磁盘块管理器层</strong>。</p><p>对于单个文件的管理和读写的控制逻辑主要是索引节点（文件控制块）来完成，即<strong>第五层抽象层——索引节点层</strong>。</p><h4 id="第三步：把-easyfs-文件系统加入到内核中"><a href="#第三步：把-easyfs-文件系统加入到内核中" class="headerlink" title="第三步：把 easyfs 文件系统加入到内核中"></a>第三步：把 easyfs 文件系统加入到内核中</h4><p>先在 <strong>Qemu</strong> 模拟的 <strong>virtio</strong> 块设备上实现块设备驱动程序。因为要直接使用 virtio-drivers crate 中的块设备驱动，所以只要提供这个块设备驱动程序所需要的内存申请与释放以及虚实地址转换的4个函数就可以。在之前操作系统中的<strong>虚存管理</strong>实现中已经有这些函数。</p><p>然后是将文件访问相关的系统调用与 easyfs 为俄舰系统连接起来。在easyfs文件系统中没有进程概念，进程是程序运行过程中访问资源的管理实体，而之前的进程没有管理文件这种资源。琐细需要扩展进程的管理范围，将文件也纳入到进程的管理之中。因为需要多个进程访问文件，所以需要文件有共享的属性，随之伴生的是 <strong>open&#x2F;close&#x2F;read&#x2F;write</strong> 系统调用，便于进程通过互斥或共享方式访问文件。</p><p>内核中的进程看到的文件是一个便于访问的 <strong>Inode</strong>，在此需要对 <strong>Inode</strong> 结构进一步封装，形成 <strong>OSInode</strong> 结构，以表示进程中一个打开的常规文件。进程为了进一步管理多个文件，需要扩展文件描述符表（给进程新加一个属性）。这样进程通过系统调用打开一个文件后，会讲文件加入到自身的文件描述符表中，并进一步通过文件描述符（也就是某个特定文件）来读写该文件。对于应用程序而言，它理解的磁盘数据是常规的文件和目录，不是 <strong>OSInode</strong> 这样相对复杂的结构。常规文件对应的 <strong>OSInode</strong> 是操作系统内核中的文件控制块数据结构的实例。其实现了 <strong>File Trait</strong> 定义的函数接口。这些 <strong>OSInode</strong> 实例会放入到进程文件描述符表中，并通过 sys_read&#x2F;write 系统调用来完成读写文件的服务。这样就建立了文件与 <strong>OSInode</strong> 的对应关系，通过上面描述的三个开发步骤将形成包含文件系统的操作系统内核，可给应用提供基于文件的系统调用服务。</p><hr><h2 id="一、块设备接口层"><a href="#一、块设备接口层" class="headerlink" title="一、块设备接口层"></a>一、块设备接口层</h2><p><code>easy-fs/src/block_dev.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">BlockDevice</span>: <span class="hljs-built_in">Send</span> + <span class="hljs-built_in">Sync</span> + Any &#123;<br><span class="hljs-comment">// 将编号为 block_id 的块从磁盘读入内存的缓冲区buf</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">read_block</span>(&amp;<span class="hljs-keyword">self</span>, block_id: <span class="hljs-type">usize</span>, buf: &amp;<span class="hljs-keyword">mut</span> [<span class="hljs-type">u8</span>]);<br>    <span class="hljs-comment">// 将内存缓冲区buf中的数据写入磁盘编号为 block_id 的块</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">write_block</span>(&amp;<span class="hljs-keyword">self</span>, block_id: <span class="hljs-type">usize</span>, buf: &amp;[<span class="hljs-type">u8</span>]);<br>    <span class="hljs-comment">// 对磁盘编号为 block_id 的块进行擦除</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">clear_block</span>(&amp;<span class="hljs-keyword">self</span>, block_id: <span class="hljs-type">usize</span>, num: <span class="hljs-type">u8</span>)&#123;...&#125;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">clear_mult_block</span>(&amp;<span class="hljs-keyword">self</span>, block_id: <span class="hljs-type">usize</span>, cnt: <span class="hljs-type">usize</span>, num: <span class="hljs-type">u8</span>)&#123;...&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="二、块缓存层（buffer-cache）"><a href="#二、块缓存层（buffer-cache）" class="headerlink" title="二、块缓存层（buffer cache）"></a>二、块缓存层（buffer cache）</h2><p>块缓存层的几个关键作用（其实原理与 CPU cache 一样）：</p><ul><li>减少磁盘 IO 操作：通过在内存中缓存最近或频繁访问的磁盘块来减少对磁盘的直接访问（因为内存访问速度远快于磁盘）。</li><li>加速数据访问：应用请求数据时，操作系统先在缓存层查找，如果找到所需的数据块，就可以直接从内存中读取，而无需等待磁盘的慢速读取。</li><li>合并写操作性：写操作先被写入到块缓存中，然后在稍后的某个时刻一起写入磁盘。这样可以将多个小的写操作小合并为一个大的磁盘 IO 操作，提高写入效率</li><li>减少重复数据读取：如果多个应用或进程读取相同数据块，这些数据块只需要在磁盘内部读取依次，之后可以从块缓存中获取，减少重复的磁盘访问。</li><li>提供一致性和同步机制：块缓存可以保证文件系统中数据的一致性，比如在崩溃恢复期间，可以帮助恢复未完成的写操作，确保文件系统的完整性<strong>（那如果突然断电了呢？）</strong>。</li><li>支持异步写操作：数据可以先缓存在块缓存中，然后异步写入磁盘，这将允许程序继续执行而不必等待磁盘 IO 操作完成。（意思是内存这边自己执行，然后程序不用管，继续原有的操作）</li><li>减轻磁盘的负载：通过减少对磁盘的访问次数，块缓存有助于降低磁盘的工作负载，延长其使用寿命。</li></ul><p>简而言之，言而总之，就是为了减少对磁盘的访问——因为它慢！</p><p>NPUcore 中对于文件系统同样实现了buff cache层，主要定义于 <code>easy-fs/src/block_cache.rs</code> 中。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">Cache</span> &#123;<br>    <span class="hljs-comment">// 允许只读方式访问缓存，接受一个偏移量和一个闭包 f,闭包作用域缓存中的数据，并返回一个结果。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">read</span>&lt;T, V&gt;(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">usize</span>, f: <span class="hljs-keyword">impl</span> <span class="hljs-title class_">FnOnce</span>(&amp;T) <span class="hljs-punctuation">-&gt;</span> V) <span class="hljs-punctuation">-&gt;</span> V;<br>    <span class="hljs-comment">// 类似于read,但用于可变地修改缓存。也接受一个偏移量和一个闭包，但闭包可以修改数据。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">modify</span>&lt;T, V&gt;(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>, offset: <span class="hljs-type">usize</span>, f: <span class="hljs-keyword">impl</span> <span class="hljs-title class_">FnOnce</span>(&amp;<span class="hljs-keyword">mut</span> T) <span class="hljs-punctuation">-&gt;</span> V) <span class="hljs-punctuation">-&gt;</span> V;<br>    <span class="hljs-comment">// 将缓存中的数据写回磁盘，接受一个块ID列表和一个指向块设备的引用。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">sync</span>(&amp;<span class="hljs-keyword">self</span>, _block_ids: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">usize</span>&gt;, _block_device: &amp;Arc&lt;<span class="hljs-keyword">dyn</span> BlockDevice&gt;)&#123;&#125;<br>&#125;<br><span class="hljs-comment">// 此处的 FnOnce是指该闭包只能被调用一次。</span><br></code></pre></td></tr></table></figure><p>为了更好的管理缓存空间，需要实现一个 <code>CacheManager</code> 来对缓存进行管理。这样做可以统一管理不同类型的缓存，如块缓存和页表缓存。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">CacheManager</span> &#123;<br>    <span class="hljs-keyword">const</span> CACHE_SZ: <span class="hljs-type">usize</span>;<br>    <span class="hljs-keyword">type</span> <span class="hljs-title class_">CacheType</span>: Cache;<br>    <span class="hljs-comment">// 静态方法，用于创建一个新的缓存管理器实例</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span><br>    <span class="hljs-keyword">where</span><br>    <span class="hljs-keyword">Self</span>: <span class="hljs-built_in">Sized</span>;<br>    <span class="hljs-comment">// 用于系统内存不足时，释放缓存，接受一个邻居闭包来获取块ID,以及一个指向块设备的引用。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">oom</span>&lt;FUNC&gt;(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        _neighbor: FUNC,<br>        _block_device: &amp;Arc&lt;<span class="hljs-keyword">dyn</span> BlockDevice&gt;<br>    ) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span><br>    <span class="hljs-keyword">where</span><br>    FUNC: <span class="hljs-title function_ invoke__">Fn</span>(<span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">usize</span>&gt;<br>    &#123;<br>        <span class="hljs-built_in">unreachable!</span>()<br>    &#125;<br>    <span class="hljs-comment">// 当文件大小变化时，通知缓存管理器释放一些缓存块。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">notify_new_size</span>(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        _new_size: <span class="hljs-type">usize</span><br>    ) &#123;<br><span class="hljs-built_in">unreachable!</span>()<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>因为有块缓存和页缓存两种缓存块，所以如下对上述的<code>CacheManager</code> 进一步封装实现了 <code>BlockCacheManager</code> 以及 <code>PageCacheManager</code>。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">BlockCacheManager</span>: CacheManager &#123;<br>    <span class="hljs-comment">// 尝试获取传入参数 block_id (磁盘编号) 和 inner_cache_id (管理器内部编号)对应的Cache</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">try_get_block_cache</span>(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        block_id: <span class="hljs-type">usize</span>,<br>    ) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;Arc&lt;Mutex&lt;<span class="hljs-keyword">Self</span>::CacheType&gt;&gt;&gt;;<br>    <span class="hljs-comment">// 该函数获取传入参数 block_id (磁盘编号) 和 inner_cache_id (管理器内部编号)对应的 Cache , 如果内存中没有，则会通过 block_device 从块设备中读入对应内容。</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_block_cache</span>&lt;FUNC&gt;(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        block_id: <span class="hljs-type">usize</span>,<br>        block_device: &amp;Arc&lt;<span class="hljs-keyword">dyn</span> BlockDevice&gt;,<br>    ) <span class="hljs-punctuation">-&gt;</span> Arc&lt;Mutex&lt;<span class="hljs-keyword">Self</span>::CacheType&gt;&gt;<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">PageCacheManager</span>: CacheManager &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">try_get_page_cache</span>(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        inner_cache_id: <span class="hljs-type">usize</span>,<br>    ) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;Arc&lt;Mutex&lt;<span class="hljs-keyword">Self</span>::CacheType&gt;&gt;&gt;;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_page_cache</span>&lt;FUNC&gt;(<br>    &amp;<span class="hljs-keyword">self</span>,<br>        inner_id: <span class="hljs-type">usize</span>,<br>        neighbor: FUNC,<br>        block_device: &amp;Arc&lt;<span class="hljs-keyword">dyn</span> BlockDevice&gt;,<br>    ) <span class="hljs-punctuation">-&gt;</span> Arc&lt;Mutex&lt;<span class="hljs-keyword">Self</span>::CacheType&gt;&gt;<br>    <span class="hljs-keyword">where</span><br>    FUNC: <span class="hljs-title function_ invoke__">Fn</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">usize</span>&gt;;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="三、索引节点层"><a href="#三、索引节点层" class="headerlink" title="三、索引节点层"></a>三、索引节点层</h2><p>文件系统的构成</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241115132822809.png" alt="image-20241115132822809"></p><p>最开始是一个磁盘扇区组成的引导块。该部分的主要目的是用于对操作系统的引导。一般只在启动操作系统时使用。</p><p>然后就是<strong>块组0</strong>，其最开始是<strong>超级块</strong>。</p><p><strong>超级块（Super Block）</strong>：主要存放了<strong>该物理磁盘中文件系统结构的相关信息</strong>。并且对各个部分的大小进行说明。</p><p><strong>inode 块位图（inode Bitmap）</strong>：记录了 <strong>inode</strong> 块中哪些块已被使用</p><p><strong>data 块位图</strong> ：记录了<strong>data</strong> 块中那些块已被使用</p><p>为了对超级块、inode 块和数据块三部分进行高效的管理，Linux 创建了几种不同的数据结构，分别是文件系统类型、inode、dentry（data entry）等几种。</p><p>超级块反映了文件系统整体的控制信息。</p><p>dentry 反映出某个文件系统在全局文件系统树中的位置。</p><p>inode 反映了文件系统对象中的一般元数据信息。</p><p><strong>inode</strong> 存放了文件元数据信息，并指向文件的内容，所以在 Linux 中，“ls -l” 实际就是使用了inode 的文件元数据。NPUcore中虽然使用了 fat32 文件系统，但在代码层面上同样引入了 inode这个概念，而服务于文件相关系统调用的索引节点层的代码在 <code>vfs.rs</code> 中。</p><p>简单文件系统层实现了磁盘布局并能够将磁盘块有效的管理起来，但是对于文件系统的使用者而言，他们往往不关心磁盘布局是如何实现的（你想关心吗？），而是希望能够直接看到目录树结构中逻辑上的文件和目录（就是平时打开文件管理器时看到的那样），所以需要设计索引节点“<strong>Inode</strong>”，它屏蔽了简单文件系统层对磁盘的操作，让文件系统的使用者能够直接对文件和目录进行操作。代码如下所示：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Inode</span> &#123;<br>    <span class="hljs-comment">// 该inode的读写锁</span><br>    inode_lock: RwLock&lt;InodeLock&gt;,<br>    <span class="hljs-comment">// 指向文件的内容</span><br>    file_content: RwLock&lt;FileContent&gt;,<br>    <span class="hljs-comment">// 指向管理该 inode 的 PageCacheManager</span><br>    file_cache_mgr: PageCacheManager,<br>    <span class="hljs-comment">// 文件类型</span><br>    file_type: Mutex&lt;DiskInodeType&gt;,<br>    <span class="hljs-comment">// 指向父目录的 inode 以及该 inode 在父目录的位置</span><br>    parent_dir: Mutex&lt;<span class="hljs-type">Option</span>&lt;(Arc&lt;<span class="hljs-keyword">Self</span>&gt;, <span class="hljs-type">u32</span>)&gt;&gt;,<br>    <span class="hljs-comment">// 指向该 inode 所属的简单文件系统</span><br>    fs: Arc&lt;EasyFileSystem&gt;,<br>    <span class="hljs-comment">// 该文件的相关时间属性</span><br>    time: Mutex&lt;InodeTime&gt;,<br>    <span class="hljs-comment">// 控制在该 inode 执行析构函数时是否释放文件内容的区域的标识</span><br>    deleted: Mutex&lt;<span class="hljs-type">bool</span>&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><p>通过上述这个 <strong>Inode</strong> 结构体，文件系统的使用者就可以对文件系统进行一些常用操作，如：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 创建目录项</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">create_dir_ent</span>(<br>&amp;<span class="hljs-keyword">self</span>,<br>    inode_lock: &amp;RwLockWriteGuard&lt;InodeLock&gt;,<br>    short_ent: FATShortDirEnt,<br>    long_ents: <span class="hljs-type">Vec</span>&lt;FATLongDirEnt&gt;,<br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">u32</span>, ()&gt;<br><br><span class="hljs-comment">// 删除目录项</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">delete_dir_ent</span>(<br>&amp;<span class="hljs-keyword">self</span>,<br>    inode_lock: &amp;RwLockWriteGuard&lt;InodeLock&gt;,<br>    parent_dir: &amp;Arc&lt;<span class="hljs-keyword">Self</span>&gt;,<br>    parent_inode_lock: &amp;RwLockWriteGuard&lt;InodeLock&gt;,<br>    name: <span class="hljs-type">String</span>,<br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), ()&gt;<br><br><span class="hljs-comment">// 删除硬链接（unlink）</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">unlink_lock</span>(<br>&amp;<span class="hljs-keyword">self</span>,<br>    _inode_lock: &amp;RwLockWriteGuard&lt;InodeLock&gt;,<br>    delete: <span class="hljs-type">bool</span>,<br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;<br><br><span class="hljs-comment">// ls</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">ls_lock</span>(<br>&amp;<span class="hljs-keyword">self</span>,<br>    inode_lock: &amp;RwLockWriteGuard&lt;InodeLock&gt;,<br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">Vec</span>&lt;(<span class="hljs-type">String</span>, FATShortDirEnt)&gt;, ()&gt;<br><br><span class="hljs-comment">// 获取文件时间相关信息</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">time</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> MutexGuard&lt;InodeTime&gt;<br><br><span class="hljs-comment">// ...</span><br></code></pre></td></tr></table></figure><h2 id="四、目录和路径名"><a href="#四、目录和路径名" class="headerlink" title="四、目录和路径名"></a>四、目录和路径名</h2><h3 id="4-1-目录"><a href="#4-1-目录" class="headerlink" title="4.1 目录"></a>4.1 目录</h3><p>最早的文件系统没有现今的文件夹，只用文件名来标识文件，这种方法明显会带来管理上的麻烦。后来慢慢改变为使用按功能、按属性分类文件的方式，即将文件整理到不同层级的目录中，这样使得我们能够更轻松地定位所需文件（多级页表是不是也是这样的道理？）。</p><p>在Linux下可以使用 <strong>stat</strong> 工具来获取一个文件或者目录的相关信息，这些信息包括目录的权限、所有者、大小以及创建时间等。实际上，<strong>stat</strong> 工具就是直接获取一个目录文件（或者其他类型文件）的 <strong>inode</strong> 来获取如上提到的这些信息。</p><p>他有如下字段：</p><ul><li>File：文件的文件名</li><li>Size：文件的字节大小</li><li>Blocks：占据的块数</li><li>IO Block：该操作系统一个块所占据的字节大小</li><li>文件类型字段：根据获取到的文件类型信息打印</li><li><code>Access: (xxxx/xxxxxxxxxx)</code> ：访问的权限</li><li>Access：访问时间</li><li>Modify：修改时间</li><li>Uid &amp; Gid：用户id 和用户所在组id</li><li>Device：如果文件是一个特殊文件，那么打印该特殊文件的 major&#x2F;minor ID</li><li>Inode：该文件的底层编号，即用于索引的Inode节点</li><li>Links：硬链接数，即一个文件的不同文件名的数量，当两个文件硬链接到一个Inode时（实际上硬链接就是新创建一个文件，这个文件与原有文件共享一个Inode，且这个文件的数据与原文件一模一样，或者说就是一个原文件的引用），对其中一个的改变都会作用到另一个上面。</li></ul><p>目录也是一种文件，它也有自己的Inode，其中保存着若干目录项（Dirent，Directory Entry）。根据它下面的子文件名和子目录名可以查询到子文件和子目录在文件系统中的底层编号（Inode）。</p><h3 id="4-2-路径"><a href="#4-2-路径" class="headerlink" title="4.2 路径"></a>4.2 路径</h3><blockquote><p>以下内容不考虑软链接！</p></blockquote><p>在有了目录之后，就可以将文件和目录组织为一种被称为目录树（Directory Tree）的有根树结构（在有了基层党组织结构后，顶层党组织就可以不直接对底层党员进行管辖，而是分由基层党组织进行管理，那么顶层组织就只需要对基层组织进行管理即可），这个目录就是党组织，目录树就是党组织结构。</p><p>树中的每个节点都是一个文件或者目录，一个目录下面的所有的文件和子目录都是它的孩子。</p><p>所以，所有非目录文件都是目录树的叶子节点。</p><p>目录树的根节点就是一个目录，即我们日常所见的 “<strong>根目录</strong>”，也即“Root Directory”，在类Unix系统中，就是一个文件的绝对路径 <code>/xxx/xxx/.../xxfiile</code> 中的第一个 “<code>/</code>“。</p><p>路径的分隔符也是 “<code>/</code>”。</p><p><strong>相对路径</strong>和<strong>绝对路径</strong>太简单了，这里就不加描述了。</p><p>在此，需要提出一个新的概念——<strong>CWD</strong>，</p><p>即<strong>Current Working Directory</strong>，当前所在的工作目录，是一个绝对路径。</p><p>每个进程都会记录自己的 CWD，当进程在索引文件或目录的时候，如果传给它的路径并未以“&#x2F;”开头，就会被内核认为是一个相对于进程当前工作目录的<strong>相对路径</strong>，这个路径会被拼接在CWD后面，组成一个<strong>绝对路径</strong>，实际上索引用的是这个绝对路径对应的文件或者目录。</p><p>在文件系统的底层实现中，先将路径转化为一个文件或目录的Inode，再通过这个Inode具体索引文件或者目录。该过程是逐级进行的——对于绝对路径，先从根目录出发，每次根据当前目录Inode获取到信息，根据下一级子目录的目录名查到该子目录的底层编号，然后从该子目录继续向下遍历；对于相对路径，实际上就是绝对路径的过程中，跳过从根目录到当前目录的逐级遍历过程，直接从当前目录Inode获取信息，然后再如上述过程往下遍历。在这个过程中，目录的权限控制位会起到保护作用，阻止无权限的用户进行访问。</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241115153834638.png" alt="image-20241115153834638"></p><h3 id="4-3-NPUcore中目录树的数据结构及具体实现"><a href="#4-3-NPUcore中目录树的数据结构及具体实现" class="headerlink" title="4.3 NPUcore中目录树的数据结构及具体实现"></a>4.3 NPUcore中目录树的数据结构及具体实现</h3><p><strong>NPUcore</strong> 中的这部分代码位于</p><p><code>os/src/fs/directory_tree.rs</code></p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241115162022627.png" alt="image-20241115162022627"></p><p>DirectoryTreeNode 的数据结构</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">DirectoryTreeNode</span> &#123;<br>    spe_usage: Mutex&lt;<span class="hljs-type">usize</span>&gt;,<br>    name: <span class="hljs-type">String</span>,<br>    filesystem: Arc&lt;FileSystem&gt;,<br>    file: Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;,<br>    selfptr: Mutex&lt;Weak&lt;<span class="hljs-keyword">Self</span>&gt;&gt;,<br>    father: Mutex&lt;Weak&lt;<span class="hljs-keyword">Self</span>&gt;&gt;,<br>    children: RwLock&lt;<span class="hljs-type">Option</span>&lt;BTreeMap&lt;<span class="hljs-type">String</span>, Arc&lt;<span class="hljs-keyword">Self</span>&gt;&gt;&gt;&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><p>OSInode 的数据结构</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">OSInode</span> &#123;<br>    readble: <span class="hljs-type">bool</span>,<br>    writable: <span class="hljs-type">bool</span>,<br>    special_use: <span class="hljs-type">bool</span>,<br>    append: <span class="hljs-type">bool</span>,<br>    inner: Arc&lt;InodeImpl&gt;,<br>    offset: Mutex&lt;<span class="hljs-type">usize</span>&gt;,<br>    dirnode_ptr: Arc&lt;Mutex&lt;Weak&lt;DirectoryTreeNode&gt;&gt;&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到这个<strong>OSInode</strong>实际上是对内部的<strong>InodeImpl</strong>进行了封装，而这个<strong>InodeImpl</strong>实际是定义于 <code>fat32/vfs.rs</code> 下的<strong>Inode</strong>数据结构</p><p>在内核中全局定义了一个目录节点向量 <code>DIRECTORY_VEC</code>，记录当前系统所在的目录</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> DIRECTORY_VEC<br>: Mutex&lt;(<span class="hljs-type">Vec</span>&lt;Weak&lt;DirectoryTreeNode&gt;&gt;, <span class="hljs-type">usize</span>)&gt; <br>= Mutex::<span class="hljs-title function_ invoke__">new</span>((<span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">new</span>(), <span class="hljs-number">0</span>));<br></code></pre></td></tr></table></figure><p>同时还记录了根目录节点 <code>ROOT</code>，其目录是<strong>空字符串</strong>（毕竟类Unix系统下，根目录不是“<code>/</code>”吗，前面是不是啥都没有？）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> ROOT: Arc&lt;DirectoryTreeNode&gt; = &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">inode</span> = DirectoryTreeNode::<span class="hljs-title function_ invoke__">new</span>(<br>    <span class="hljs-string">&quot;&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(),<br>    Arc::<span class="hljs-title function_ invoke__">new</span>(FileSystem::<span class="hljs-title function_ invoke__">new</span>(FS::Fat32)),<br>    OSInode::<span class="hljs-title function_ invoke__">new</span>(InodeImpl::<span class="hljs-title function_ invoke__">root_inode</span>(&amp;FILE_SYSTEM)),<br>    Weak::<span class="hljs-title function_ invoke__">new</span>()<br>    );<br>    inode.<span class="hljs-title function_ invoke__">add_special_use</span>();<br>    inode,<br>&#125;;<br></code></pre></td></tr></table></figure><p>在通过文件树寻找指定文件的过程中，首先会判断文件的路径前缀是否在路径缓存中，这样使大量文件操作效率更高，同时NPUcore也对一些默认路径进行了路径的转化。</p><h2 id="五、文件描述符层"><a href="#五、文件描述符层" class="headerlink" title="五、文件描述符层"></a>五、文件描述符层</h2><h3 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h3><p><strong>文件描述符</strong>是进程访问文件的 <strong>接口</strong>，而 <strong>Inode</strong> 是文件在文件系统中的 <strong>描述符</strong>。具体来说：</p><ul><li>当一个文件被打开时，操作系统首先根据文件名查找文件的 Inode，然后为该进程分配一个文件描述符，并通过文件描述符来访问文件。</li><li>文件描述符在进程的文件描述符表中，而 Inode 存储在文件系统的 Inode 表中。</li></ul><hr><p>一个进程能访问多个文件，所以现在需要有一个结构，能够用于管理这个进程访问的多个文件。</p><p>所以，<strong>文件描述符表（File Descriptor Table，简称 FDT）</strong> 诞生了。</p><p>它包括了多个 <strong>文件描述符（File Descriptor，简称 FD）</strong>，每个<strong>FD</strong>代表了一个特定读写属性的IO资源。</p><p>为了简化操作系统设计实现，可以让每个进程都带有一个线性的文件描述符表，记录该进程请求内核打开并读写的那些文件集合。每个<strong>文件描述符</strong>是一个非负整数，表示文件描述符表中一个打开的文件描述符所处的位置（可以理解为数组下标）。进程通过<strong>FD</strong>，可以在自身的<strong>FDT</strong>中找到对应的文件记录信息，从而也就找到对应文件，并对文件进行读写。</p><p>当打开或创建一个文件的时候，一般情况下内核会返回给应用打开或创建的文件对应的<strong>FD</strong>；当应用想关闭一个文件的时候，也需要向内核提供<strong>FD</strong>，以完成对应文件相关资源的回收操作。</p><p>OSInode要放到进程文件描述符表中，并将通过 sys_read&#x2F;write 系统调用进行读写，因此需要为其实现 <strong>File Trait</strong>，如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">readable</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span> &#123;<br>    <span class="hljs-keyword">self</span>.readable<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">writable</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span> &#123;<br>    <span class="hljs-keyword">self</span>.writable<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">read</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">usize</span>&gt;, buffer: &amp;<span class="hljs-keyword">mut</span> [<span class="hljs-type">u8</span>]) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br><span class="hljs-keyword">match</span> offset &#123;<br>        <span class="hljs-title function_ invoke__">Some</span>(offset) =&gt; &#123;<br>            <span class="hljs-keyword">let</span> <span class="hljs-variable">len</span> = <br>            <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">read_at_block_cache</span>(*offset, buffer);<br>            *offset += len;<br>            len<br>        &#125;<br>        <span class="hljs-literal">None</span> =&gt; &#123;<br>            <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">offset</span> = <br>            <span class="hljs-keyword">self</span>.offset.<span class="hljs-title function_ invoke__">lock</span>();<br>            <span class="hljs-keyword">let</span> <span class="hljs-variable">len</span> = <br>            <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">read_at_block_cache</span>(*offset,<br>            buffer);<br>            *offset += len;<br>            len<br>        &#125;<br>    &#125;<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">write</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">usize</span>&gt;, buffer: &amp;[<span class="hljs-type">u8</span>]) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br>    <span class="hljs-keyword">match</span> offset &#123;<br>        <span class="hljs-title function_ invoke__">Some</span>(offset) =&gt; &#123;<br>            <span class="hljs-keyword">let</span> <span class="hljs-variable">len</span> = <br>            <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">write_at_block_cache</span>(*offset, buffer);<br>            *offset += len;<br>            len<br>        &#125;<br>        <span class="hljs-literal">None</span> =&gt; &#123;<br>            <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">offset</span> = <br>            <span class="hljs-keyword">self</span>.offset.<span class="hljs-title function_ invoke__">lock</span>();<br>            <span class="hljs-keyword">let</span> <span class="hljs-variable">inode_lock</span> = <br>            <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">write</span>();<br>            <span class="hljs-keyword">if</span> <span class="hljs-keyword">self</span>.append &#123;<br>                *offset = <br>                <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">get_file_size_wlock</span>(&amp;inode_lock)<br>                <span class="hljs-keyword">as</span> <span class="hljs-type">usize</span>;<br>            &#125;<br>            <span class="hljs-keyword">let</span> <span class="hljs-variable">len</span> = <span class="hljs-keyword">self</span><br>            .inner<br>            .<span class="hljs-title function_ invoke__">write_at_block_cache_lock</span>(&amp;inode_lock,<br>            *offset, buffer);<br>            *offset += len;<br>            len<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>read 将数据从文件读取到参数提供的缓冲区中，offset参数是一个可选的usize可变引用，即允许指定开始读取的偏移量。</p><p>write 将提供的缓冲区中的数据写入文件，与read类似。</p><p>在NPUcore中，使用文件描述符表 <strong>Fdtable</strong> 来进行对文件描述符的管理，该数据结构定义于</p><p><code>os/src/fs/poll.rs</code> 中</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FdTable</span> &#123;<br>    inner: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">Option</span>&lt;FileDesciptor&gt;&gt;,<br>    recycled: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">u8</span>&gt;,<br>    soft_limit: <span class="hljs-type">usize</span>,<br>    hard_limit: <span class="hljs-type">usize</span>,<br>&#125;<br></code></pre></td></tr></table></figure><p>可以理解为 <strong>FDT</strong> 内部存在一个向量组，通过 <strong>open</strong> 等操作得到的 <strong>FD（非负整数）</strong>，对应的就是该数组的下标。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FileDescriptor</span> &#123;<br>    cloexec: <span class="hljs-type">bool</span>,<br>    nonblock: <span class="hljs-type">bool</span>,<br>    <span class="hljs-keyword">pub</span> file: Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><p>因此，在一个进程中，活跃的文件被存储在 <strong>FDT</strong> 中。我们通过访问这个 <strong>FDT</strong> 获得所需的 <strong>FD</strong> ，进而定位并处理我们想要操作的特定文件。</p><h2 id="六、文件系统相关调用"><a href="#六、文件系统相关调用" class="headerlink" title="六、文件系统相关调用"></a>六、文件系统相关调用</h2><h3 id="写在前面-1"><a href="#写在前面-1" class="headerlink" title="写在前面"></a>写在前面</h3><p>NPUcore 实现了许多POSIX规定的系统调用，本章主要介绍几个与文件相关的系统调用，包括文件的打开、关闭、读取和写入。</p><h3 id="6-1-open-调用"><a href="#6-1-open-调用" class="headerlink" title="6.1 open 调用"></a>6.1 open 调用</h3><p>该系统调用用于打开文件并返回一个<strong>FD</strong>，应用可以通过该<strong>FD</strong>执行各种操作，如读取、写入、关闭文件等。</p><p>NPUcore提供给应用的接口为用户态下的sys_open系统调用（位于 <code>user/src/syscall.rs</code>），在读写一个常规文件之前，应用首先通过内核提供的 sys_open 系统调用让该文件在进程的<strong>FDT</strong>中占一项，并得到操作系统的返回值——<strong>FD</strong>，即文件关联的表项在<strong>FDT</strong>中的索引值。</p><p><code>user/src/syscall.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 接受两个参数：</span><br><span class="hljs-comment">// path 文件名（包括路径）</span><br><span class="hljs-comment">// flags 打开标志</span><br><span class="hljs-comment">// 打开成功则返回对应的FD</span><br><span class="hljs-comment">// 失败则返回对应的错误码</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">sys_open</span>(path: &amp;<span class="hljs-type">str</span>, flags: <span class="hljs-type">u32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span> &#123;<br>    <span class="hljs-title function_ invoke__">syscall</span>(SYSCALL_OPEN, [path.<span class="hljs-title function_ invoke__">as_str</span>() <span class="hljs-keyword">as</span> <span class="hljs-type">usize</span>, flags <span class="hljs-keyword">as</span> <span class="hljs-type">usize</span>, <span class="hljs-number">0</span>])<br>&#125;<br></code></pre></td></tr></table></figure><p>NPUcore中的flags可以为如下几种（多种标志可以共存）：</p><ul><li>0：RDONLY 只读模式</li><li>0x001（即第0位被设置）：WRONLY 只写模式</li><li>0x002（即第1位被设置）：RDWR 可读可写</li><li>0x200（即第9位被设置）：CREATE 允许创建<ul><li>找不到该文件时创建</li><li>找得到该文件时截断（将文件大小清零）</li></ul></li><li>0x400（即第10位被设置）：TRUNC 截断（清空并将文件大小置零）</li></ul><p><code>user/src/lib.rs</code></p><h6 id="教材错了！（其中一版）"><a href="#教材错了！（其中一版）" class="headerlink" title="教材错了！（其中一版）"></a>教材错了！（其中一版）</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">bitflags!</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">OpenFlags</span>: <span class="hljs-type">u32</span> &#123;<br>        <span class="hljs-keyword">const</span> RDONLY = <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">const</span> WRONLY = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">const</span> RDWR = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">const</span> CREATE = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">9</span>;<br>        <span class="hljs-keyword">const</span> TRUNC = <span class="hljs-number">1</span> &lt;&lt; <span class="hljs-number">10</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><code>user/src/user_call.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">open</span>(path: &amp;<span class="hljs-type">str</span>, flags: OpenFlags) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span> &#123;<br>    <span class="hljs-title function_ invoke__">sys_open</span>(path, flags.bits)<br>&#125;<br></code></pre></td></tr></table></figure><p>sys_open 传给内核的参数只有待打开文件的文件名字符串的起始地址（需要保证该字符串以 <code>\0</code> 结尾）和标志位。</p><p>由于每个通用寄存器为64位，需要先将u32的flags转换为usize来对齐。sys_open在NPUcore内核中调用了sys_openat函数（实验环节中自行编写）：</p><p><code>os/src/syscall/mod.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust">SYSCALL_OPEN =&gt; <span class="hljs-title function_ invoke__">sys_openat</span>(AT_FDCWD, args[<span class="hljs-number">0</span>] <span class="hljs-keyword">as</span> *<span class="hljs-keyword">const</span> <span class="hljs-type">u8</span>, args[<span class="hljs-number">1</span>] <span class="hljs-keyword">as</span> <span class="hljs-type">u32</span>, <span class="hljs-number">0o777u32</span>),<br></code></pre></td></tr></table></figure><h3 id="6-2-close-调用"><a href="#6-2-close-调用" class="headerlink" title="6.2 close 调用"></a>6.2 close 调用</h3><p>该系统调用用于关闭文件（打开文件做了相应操作之后，需要关闭该文件，让进程释放被该文件占用的内核资源）。</p><p>与open一样，NPUcore提供用户态的sys_close系统调用给应用。作用机制与sys_open相似。</p><p>其内核中的函数签名：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">sys_close</span>(fd: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span><br></code></pre></td></tr></table></figure><p>接收一个参数 <code>fd</code> ，即<strong>FD（文件描述符）</strong>类型（在此处为 usize）。<code>fd</code> 表示要关闭的<strong>FD</strong>，当文件被关闭后，该文件在内核中的资源会被释放，<strong>FD</strong> 会被回收，这样做之后，进程就不能继续使用该<strong>FD</strong>进行文件读写了。如果成功关闭返回 SUCCESS，否则返回对应错误码。</p><p><code>src/os/syscall.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">sys_close</span>(fd: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span> &#123;<br>    info!(<span class="hljs-string">&quot;[sys_close] fd: &#123;&#125;&quot;</span>, fd);<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">task</span> = <span class="hljs-title function_ invoke__">current_task</span>().<span class="hljs-title function_ invoke__">unwrap</span>();<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">fd_table</span> = task.files.<span class="hljs-title function_ invoke__">lock</span>();<br>    <span class="hljs-keyword">match</span> fd_table.<span class="hljs-title function_ invoke__">remove</span>(fd) &#123;<br>        <span class="hljs-title function_ invoke__">Ok</span>(_) =&gt; SUCCESS,<br>        <span class="hljs-title function_ invoke__">Err</span>(errno) =&gt; errno,<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>只需要将进程控制块中的<strong>FDT</strong>对应的一项改为<strong>None</strong>,代表其已经空闲，就可以实现<strong>sys_close</strong>。</p><p>具体实现</p><p><code>os/src/fs/mod.rs/impl FdTable</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[inline]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">remove</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>, fd: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;FileDescriptor, <span class="hljs-type">isize</span>&gt; &#123;<br>    <span class="hljs-keyword">if</span> fd &gt;= <span class="hljs-keyword">self</span>.inner.<span class="hljs-title function_ invoke__">len</span>() &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-title function_ invoke__">Err</span>(EBADF);<br>    &#125;<br>    <span class="hljs-comment">// 先尝试将fd从inner数组中取出</span><br>    <span class="hljs-keyword">match</span> <span class="hljs-keyword">self</span>.inner[fd].<span class="hljs-title function_ invoke__">take</span>() &#123;<br>        <span class="hljs-comment">// 成功则将fd push到recycled数组中</span><br>        <span class="hljs-title function_ invoke__">Some</span>(file_descriptor) =&gt; &#123;<br>            <span class="hljs-keyword">self</span>.recycled.<span class="hljs-title function_ invoke__">push</span>(fd <span class="hljs-keyword">as</span> <span class="hljs-type">u8</span>);<br>            <span class="hljs-title function_ invoke__">Ok</span>(file_descriptor)<br>        &#125;<br>        <span class="hljs-literal">None</span> =&gt; <span class="hljs-title function_ invoke__">Err</span>(EBADF),<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="6-3-read-调用"><a href="#6-3-read-调用" class="headerlink" title="6.3 read 调用"></a>6.3 read 调用</h3><h3 id="6-4-write-调用"><a href="#6-4-write-调用" class="headerlink" title="6.4 write 调用"></a>6.4 write 调用</h3><h3 id="6-5-fstat-调用"><a href="#6-5-fstat-调用" class="headerlink" title="6.5 fstat 调用"></a>6.5 fstat 调用</h3><h3 id="6-6-fstatat-调用"><a href="#6-6-fstatat-调用" class="headerlink" title="6.6 fstatat 调用"></a>6.6 fstatat 调用</h3><h2 id="七、虚拟文件系统及接口"><a href="#七、虚拟文件系统及接口" class="headerlink" title="七、虚拟文件系统及接口"></a>七、虚拟文件系统及接口</h2><h3 id="7-1-VFS简介"><a href="#7-1-VFS简介" class="headerlink" title="7.1 VFS简介"></a>7.1 VFS简介</h3><p><strong>Virtual File System</strong>（简称 VFS，即虚拟文件系统），也可称虚拟文件转换，是一个内核软件层，用于处理与Unix标准文件系统相关的所有系统调用。为用户程序提供文件和文件系统操作的统一接口，屏蔽不同文件系统的差异和操作细节。</p>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
      <category>NPUcore</category>
      
    </categories>
    
    
    <tags>
      
      <tag>OS</tag>
      
      <tag>NPUcore</tag>
      
      <tag>FileSystem</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>KDE使用技巧</title>
    <link href="/2024/11/13/KDE%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/"/>
    <url>/2024/11/13/KDE%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/</url>
    
    <content type="html"><![CDATA[<h1 id="KDE使用技巧"><a href="#KDE使用技巧" class="headerlink" title="KDE使用技巧"></a>KDE使用技巧</h1><h2 id="桌面"><a href="#桌面" class="headerlink" title="桌面"></a>桌面</h2><h3 id="虚拟桌面"><a href="#虚拟桌面" class="headerlink" title="虚拟桌面"></a>虚拟桌面</h3><p>在设置中可以添加虚拟桌面，然后有如下快捷键：</p><ul><li><code>Ctrl+Fx</code>：<code>Fx</code> 即键盘最上方的一排功能键， <code>x</code> 代表数字，即要切换到第几个虚拟桌面。</li><li><code>三指滑动</code>：同样的，使用笔记本触摸板时，<strong>三指滑动</strong>到上下左右四个方向可以切换到对应位置的<strong>虚拟桌面</strong>。</li></ul><h2 id="Terminal"><a href="#Terminal" class="headerlink" title="Terminal"></a>Terminal</h2><h3 id="Konsole-Yakuake"><a href="#Konsole-Yakuake" class="headerlink" title="Konsole&#x2F;Yakuake"></a>Konsole&#x2F;Yakuake</h3><p><strong>Konsole</strong>：KDE官方软件仓库的终端软件。</p><p><img src="/home/xein/CODE/WEBCODE/xeindeblog/source/img/KDE%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/Konsole1.png" alt="image-20241113155907564"></p><p><strong>Yakuake</strong>：KDE官方仓库的基于 <strong>Konsole</strong> 的下拉终端窗口软件。</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241113155734453.png" alt="image-20241113155734453"></p><blockquote><p>此处仅介绍Arch下的安装方式</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S yakuake konsole<br></code></pre></td></tr></table></figure><p>安装这两个软件</p><p>打开 <strong>yakuake</strong> 后会默认使用快捷键 <code>F12</code>呼出</p><p>然后可以取消勾选下拉窗口右下角三个点里面的 <strong>窗口失去焦点时保持展开</strong>。这样切换到别的窗口时就会自动收回下拉窗口。</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241113160004511.png" alt="image-20241113160004511"></p><p>这两个软件有许多快捷键是一样的：</p><ul><li><code>Ctrl+Shift+T</code>：在当前窗口中新建一个标签页</li><li><code>Alt+n</code>：<code>n</code> 为数字，表示要切换到哪个标签页</li></ul>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
      <category>Linux</category>
      
      <category>KDE</category>
      
    </categories>
    
    
    <tags>
      
      <tag>KDE</tag>
      
      <tag>技巧</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>KDE美化指南</title>
    <link href="/2024/11/13/KDE%E7%BE%8E%E5%8C%96%E6%8C%87%E5%8D%97/"/>
    <url>/2024/11/13/KDE%E7%BE%8E%E5%8C%96%E6%8C%87%E5%8D%97/</url>
    
    <content type="html"><![CDATA[<h1 id="KDE美化指南"><a href="#KDE美化指南" class="headerlink" title="KDE美化指南"></a>KDE美化指南</h1>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
      <category>Linux</category>
      
      <category>KDE</category>
      
    </categories>
    
    
    <tags>
      
      <tag>KDE</tag>
      
      <tag>美化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Hexo 折腾笔记</title>
    <link href="/2024/11/13/Hexo%E6%8A%98%E8%85%BE%E7%AC%94%E8%AE%B0/"/>
    <url>/2024/11/13/Hexo%E6%8A%98%E8%85%BE%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="Hexo折腾笔记"><a href="#Hexo折腾笔记" class="headerlink" title="Hexo折腾笔记"></a>Hexo折腾笔记</h1><h2 id="一、GitHub-建仓"><a href="#一、GitHub-建仓" class="headerlink" title="一、GitHub 建仓"></a>一、GitHub 建仓</h2><p>建立一个 <strong>GitHub</strong> 仓库，名字为 <code>[用户名].github.io</code></p><p>比方说我的用户名是 <code>xjimlinx</code>，那么就创建一个仓库名为 <code>xjimlinx.github.io</code></p><p>可以设置为 <code>private</code> 也可以设置为 <code>public</code></p><p>然后进入到该仓库主页面</p><p>复制好仓库地址做准备</p><p>注意如果是 <strong>git类型地址</strong> 要在本机上设置好 <strong>ssh key</strong> 同时在 <strong>GitHub</strong> 上设置好权限。</p><hr><h2 id="二、hexo-下载"><a href="#二、hexo-下载" class="headerlink" title="二、hexo 下载"></a>二、hexo 下载</h2><h3 id="2-1-NPM-下载"><a href="#2-1-NPM-下载" class="headerlink" title="2.1 NPM 下载"></a>2.1 NPM 下载</h3><blockquote><p>NPM 即 Node Package Manager，一般</p></blockquote><p>用你所能使用的比较适合的方法安装 <strong>npm</strong></p><p>从 <strong>2.1.1</strong> 和 <strong>2.1.2</strong> 中选择一个方法，不建议直接从官方下载（Windows系统当我没说）</p><h4 id="2-1-1-系统包管理器下载"><a href="#2-1-1-系统包管理器下载" class="headerlink" title="2.1.1 系统包管理器下载"></a>2.1.1 系统包管理器下载</h4><p>以 <strong>Arch</strong> 为例</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S npm<br></code></pre></td></tr></table></figure><p>同时还需要下载 <strong>nodejs</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S nodejs<br></code></pre></td></tr></table></figure><h4 id="2-1-2-NVM——Node-版本管理器"><a href="#2-1-2-NVM——Node-版本管理器" class="headerlink" title="2.1.2 NVM——Node 版本管理器"></a>2.1.2 NVM——Node 版本管理器</h4><blockquote><p>即 <strong>Node Version Manager</strong></p></blockquote><p>以 <strong>Arch</strong> 为例</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> pacman -S nvm<br></code></pre></td></tr></table></figure><p>注意同时可以使用 <strong><a href="https://github.com/nvm-sh/nvm">nvm</a></strong> 官方的安装方法（但是系统包管理器能用为什么不直接用呢？反正 <strong>Arch</strong> 的包一般都是<strong>最新的</strong>）</p><p>然后使用 <strong>nvm</strong> 下载最新的稳定版</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nvm install stable<br></code></pre></td></tr></table></figure><p>当然你要下载最新版也没人拦你</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nvm install node<br></code></pre></td></tr></table></figure><p>设置默认 <strong>node</strong> 版本</p><p>最新稳定版</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nvm <span class="hljs-built_in">alias</span> default stable<br></code></pre></td></tr></table></figure><p>最新版</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">nvm <span class="hljs-built_in">alias</span> default node<br></code></pre></td></tr></table></figure><h3 id="2-2-Hexo-下载"><a href="#2-2-Hexo-下载" class="headerlink" title="2.2 Hexo 下载"></a>2.2 Hexo 下载</h3><p>全局安装 <strong>hexo-cli</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm install hexo-cli -g<br></code></pre></td></tr></table></figure><p>这将把hexo安装到如下地址</p><p><code>/home/username/.nvm/versions/node/v23.2.0/bin/hexo</code></p><p>即安装到当前 <strong>node</strong> 版本的文件夹下。</p><hr><h2 id="三、使用-hexo-从-md-文件创建页面"><a href="#三、使用-hexo-从-md-文件创建页面" class="headerlink" title="三、使用 hexo 从 md 文件创建页面"></a>三、使用 hexo 从 md 文件创建页面</h2><h3 id="3-1-hexo-本地部署"><a href="#3-1-hexo-本地部署" class="headerlink" title="3.1 hexo 本地部署"></a>3.1 hexo 本地部署</h3><p>在想要放置博客的目录，使用 <strong>hexo</strong> 初始化一个文件夹：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo 文件夹的名字 <br></code></pre></td></tr></table></figure><p>比方说想放在 <strong>home</strong> 目录下的 <strong>test</strong> 里面</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span><br>hexo init <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>就会在 <strong>home</strong> 目录下初始化一个文件夹 <strong>test</strong> ，然后再执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> <span class="hljs-built_in">test</span> <span class="hljs-comment"># 虽然我也不知道为什么要写这一步，可是就是可能有的人不会啊？</span><br></code></pre></td></tr></table></figure><p>进入 <strong>test</strong> 目录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">hexo clean <span class="hljs-comment"># 清理文件</span><br>hexo generate <span class="hljs-comment"># 根据 source文件夹下的内容生成页面文件</span><br>hexo server <span class="hljs-comment"># 本地部署hexo服务，默认访问端口是4000 按 Ctrl+c 取消</span><br></code></pre></td></tr></table></figure><p><strong><code>hexo generate</code> 步骤会将页面文件生成到 <code>test/public</code> 文件夹下。</strong></p><p>然后就可以在浏览器里面通过 <code>localhost:4000</code> 访问 <strong>hexo</strong> 页面了。</p><p>然后之后只需要在 <code>test/source/_posts</code> 文件夹下放置自己的 <strong>md</strong> 笔记，然后再执行上面那<strong>三个步骤</strong>（**<code>hexo xxx</code>**）就可以看当前效果了。</p><h3 id="3-2-笔记内容格式要求"><a href="#3-2-笔记内容格式要求" class="headerlink" title="3.2 笔记内容格式要求"></a>3.2 笔记内容格式要求</h3><p><strong>md</strong> 笔记必须在开头有如下格式</p><figure class="highlight markdown"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs markdown">---<br>title: Hexo 折腾笔记<br>author: Xein<br>categories: <br><span class="hljs-bullet">-</span> Web<br><span class="hljs-bullet">-</span> 网站搭建<br>updated: YYYY-MM-DD HH:MM:SS<br>date: YYYY-MM-DD HH:MM:SS<br>tag:<br><span class="hljs-bullet">    -</span> tag1<br><span class="hljs-bullet">    -</span> tag2<br><span class="hljs-bullet">    -</span> ...<br><span class="hljs-section">    - tagn</span><br><span class="hljs-section">---</span><br></code></pre></td></tr></table></figure><p>注意<strong>冒号</strong>用英文输入法的<strong>冒号</strong> 即 “<strong>:</strong>“</p><p>其中：</p><ul><li>title：必选项，即显示在页面的文章标题</li><li>author：非必选，显示作者</li><li>categories：非必选，如果需要多层目录就像上面那样写，就会有一级目录 <strong>Web</strong> 和二级目录（一级目录子目录）<strong>网站搭建</strong>。同理可以设置多层，只需要在下面多写几个 “- xxx” 形式的行</li><li>updated：非必选，修改时间</li><li>date：非必选，创建时间</li><li>tag：非必选，像上面那样写会有从 <strong>tag1</strong> 到 <strong>tagn</strong> 的多个 <strong>tag</strong>，<strong>tag</strong> 没有层级关系存在</li><li>comments：非必选，是否开启评论，取值范围为 <strong>false</strong> 和 <strong>true</strong>，此外还需要在 <code>_config.yml</code> 里进行相关设置。</li><li>description：非必选，摘要，也可以使用如下方式，在开头格式下方添加一个</li><li><code>&lt;!--more--&gt;</code> ，然后这个和开头格式之间的内容就是摘要</li></ul><hr><h2 id="四、上传到-GitHub"><a href="#四、上传到-GitHub" class="headerlink" title="四、上传到 GitHub"></a>四、上传到 GitHub</h2><p><strong>以下方法我觉得方便，但是不一定对所有人来说都方便，所以可以参考，因为我网络环境不是很好，使用 hexo-deployer 因为没有详细的输出，会出现傻等的情况，并且 hexo-deployer 在 commit 步骤过后就当作是提交了，只能再更新内容才可以提交 push，所以我使用下面的方法</strong></p><p>首先到 <strong>test</strong> 的父文件夹</p><p>将第一步建仓得到的地址复制下面那个代码中</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git <span class="hljs-built_in">clone</span> 你的github.io仓库的地址<br></code></pre></td></tr></table></figure><p>然后在终端执行。</p><p>在第三步中创建的文件夹 <strong>test</strong> 中，编写如下文件：</p><p><code>test/Makefile</code></p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs makefile">COMMIT_DESCRIPTION ?= <span class="hljs-string">&quot;nothing&quot;</span><br>REPO_PATH ?= ../你的用户名.github.io<br><span class="hljs-section">update: clean</span><br>hexo generate<br><br><span class="hljs-section">clean:</span><br>hexo clean<br><br><span class="hljs-section">update-repo: update</span><br>cp -r ./public/* ../你的用户名.github.io/ <br><br><span class="hljs-section">push: commit</span><br>cd $&#123;REPO_PATH&#125; &amp;&amp; git push<br><br><span class="hljs-section">commit: add</span><br>cd $&#123;REPO_PATH&#125; &amp;&amp; git commit -m $&#123;COMMIT_DESCRIPTION&#125;<br><br><span class="hljs-section">add:</span><br>cd $&#123;REPO_PATH&#125; &amp;&amp; git add .<br><span class="hljs-section">run:</span><br>hexo s<br><br><span class="hljs-meta"><span class="hljs-keyword">.PHONY</span>: clean add commit push update run</span><br></code></pre></td></tr></table></figure><p>然后每次只要执行 <code>make update-repo</code> 就可以更新仓库内容，执行 <code>make push</code> 就可以直接将文章内容 <strong>push</strong> 到<strong>GitHub</strong> 仓库了</p><p>然后通过对 <code>你的用户名.github.io</code> 访问就可以访问到你的个人网站。</p>]]></content>
    
    
    <categories>
      
      <category>Web</category>
      
      <category>网站搭建</category>
      
    </categories>
    
    
    <tags>
      
      <tag>个人博客</tag>
      
      <tag>Web</tag>
      
      <tag>网站搭建</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>FS of NPUcore-LA</title>
    <link href="/2024/11/10/FS%20of%20NPUcore-LA/"/>
    <url>/2024/11/10/FS%20of%20NPUcore-LA/</url>
    
    <content type="html"><![CDATA[<h1 id="FS-of-NPUcore-LA"><a href="#FS-of-NPUcore-LA" class="headerlink" title="FS of NPUcore-LA"></a>FS of NPUcore-LA</h1><h2 id="〇、概览"><a href="#〇、概览" class="headerlink" title="〇、概览"></a>〇、概览</h2><p>先使用 <a href="https://github.com/xjimlinx/rtree">rtree</a> (我自己用gpt帮助写的用rust实现的tree简化版)看一下大概</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241110114028818.png" alt="image-20241110114028818"></p><p>可以看到有fs根目录下有三个文件夹，分别是：</p><ul><li><strong>dev</strong> ：还不知道放什么的代码（2024&#x2F;11&#x2F;10）</li><li><strong>fat32</strong> ：放有 fat32 相关的代码</li><li><strong>ext4</strong>（我自己加的）</li></ul><center class="half"><img src="/home/xein/.config/Typora/typora-user-images/image-20241110114314858.png"><img src="/home/xein/.config/Typora/typora-user-images/image-20241110114333313.png"></center>所以依葫芦画瓢，现在ext4下面创建如下结构，后面再慢慢改<figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust">src/fs/ext4<br>├── bitmap.rs       # 位图操作，用于跟踪已分配/未分配的块<br>├── dir_iter.rs     # 目录迭代器，用于遍历目录项<br>├── inode.rs        # 管理inode数据结构，这是EXT4文件系统中的核心<br>├── layout.rs       # 定义EXT4文件系统在磁盘上的布局<br>├── <span class="hljs-keyword">mod</span>.rs          # 模块入口，负责初始化EXT4文件系统支持<br>└── vfs.rs          # 虚拟文件系统接口，将EXT4特定的实现与VFS层对接<br></code></pre></td></tr></table></figure><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241111145530465.png" alt="image-20241111145530465"></p><hr><h2 id="一、FS-根文件"><a href="#一、FS-根文件" class="headerlink" title="一、FS 根文件"></a>一、FS 根文件</h2><p>然后我们现在先从 <code>fs</code> 文件夹下面看看纯文件</p><p><img src="/home/xein/.config/Typora/typora-user-images/image-20241111181040260.png" alt="image-20241111181040260"></p><hr><p>首先，先看看 <code>fs/mod.rs</code> 文件，毕竟是模块声明文件</p><h3 id="1-1-fs-mod-rs"><a href="#1-1-fs-mod-rs" class="headerlink" title="1.1 fs&#x2F;mod.rs"></a>1.1 fs&#x2F;mod.rs</h3><h4 id="1-1-1-lazy-static-段"><a href="#1-1-1-lazy-static-段" class="headerlink" title="1.1.1 lazy_static! 段"></a>1.1.1 lazy_static! 段</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust">lazy_static! &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> ROOT_FD: Arc&lt;FileDescriptor&gt; = Arc::<span class="hljs-title function_ invoke__">new</span>(FileDescriptor::<span class="hljs-title function_ invoke__">new</span>(<br>        <span class="hljs-literal">false</span>,<br>        <span class="hljs-literal">false</span>,<br>        self::directory_tree::ROOT<br>            .<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;.&quot;</span>, OpenFlags::O_RDONLY | OpenFlags::O_DIRECTORY, <span class="hljs-literal">true</span>)<br>            .<span class="hljs-title function_ invoke__">unwrap</span>()<br>    ));<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到创建了一个变量 <code>ROOT_FD</code>，类型是 <code>Arc&lt;FileDesciptor&gt;</code>，内部的值初始化为<code>false, false, self::directory::ROOT.open(...).unwrap()</code></p><h4 id="1-1-2-FileDescriptor-结构体"><a href="#1-1-2-FileDescriptor-结构体" class="headerlink" title="1.1.2 FileDescriptor 结构体"></a>1.1.2 FileDescriptor 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[derive(Clone)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FileDescriptor</span> &#123;<br>    cloexec: <span class="hljs-type">bool</span>,<span class="hljs-comment">// close on exec</span><br>    nonblock: <span class="hljs-type">bool</span>,<br>    <span class="hljs-keyword">pub</span> file: Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>cloexec</code>: 表示这个文件描述符是否应该在执行新的程序（比如通过<code>exec</code>系列函数）时关闭。</li><li><code>nonblock</code>: 表示文件描述符是否设置为非阻塞模式。</li><li><code>file</code>: 这是一个实现了<code>File</code> trait的对象的引用计数指针（<code>Arc&lt;dyn File&gt;</code>），这意味着它可以指向任何实现了<code>File</code> trait的具体文件类型。</li></ul><h4 id="1-1-3-FdTable-结构体"><a href="#1-1-3-FdTable-结构体" class="headerlink" title="1.1.3 FdTable 结构体"></a>1.1.3 FdTable 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[derive(Clone)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FdTable</span> &#123;<br>    inner: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">Option</span>&lt;FileDescriptor&gt;&gt;,<br>    recycled: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">u8</span>&gt;,<br>    soft_limit: <span class="hljs-type">usize</span>,<br>    hard_limit: <span class="hljs-type">usize</span>,<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>inner</code>: 一个可选的<code>FileDescriptor</code>类型的向量，每个元素代表一个文件描述符，使用<code>Option</code>来允许文件描述符被标记为未使用。</li><li><code>recycled</code>: 一个<code>u8</code>类型的向量，可能是用来追踪哪些文件描述符编号已经被回收并且可以重新分配。</li><li><code>soft_limit</code> 和 <code>hard_limit</code>: 这些可能是指文件描述符的数量限制，软限制是可以修改的限制，而硬限制则是最大上限。</li></ul><p>剩下的就是这两个东西的实现了，先暂时不在这里展开</p><h6 id="⚠️TODO"><a href="#⚠️TODO" class="headerlink" title="⚠️TODO!"></a>⚠️TODO!</h6><hr><h3 id="1-2-fs-file-trait-rs"><a href="#1-2-fs-file-trait-rs" class="headerlink" title="1.2 fs&#x2F;file_trait.rs"></a>1.2 fs&#x2F;file_trait.rs</h3><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">File</span>: DowncastSync &#123;<br>    <span class="hljs-comment">// 深度克隆文件对象</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">deep_clone</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;;<br>    <span class="hljs-comment">// 可读</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">readable</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span>;<br>    <span class="hljs-comment">// 可写</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">writable</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span>;<br>    <span class="hljs-comment">// 读取数据，返回读取的字节数</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">read</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">usize</span>&gt;, buf: &amp;<span class="hljs-keyword">mut</span> [<span class="hljs-type">u8</span>]) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 写入数据，返回写入的字节数</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">write</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">usize</span>&gt;, buf: &amp;[<span class="hljs-type">u8</span>]) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 是否准备好读</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">r_ready</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span>;<br>    <span class="hljs-comment">// 是否准备好写</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">w_ready</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span>;<br>    <span class="hljs-comment">// 从用户态读取数据，返回读取的字节数</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">read_user</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;<span class="hljs-type">usize</span>&gt;, buf: UserBuffer) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 从用户态写入数据，返回写入的字节数</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">write_user</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">Option</span>&lt;<span class="hljs-type">usize</span>&gt;, buf: UserBuffer) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 获取文件大小</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_size</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 获取文件Stat结构</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_stat</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> Stat;<br>    <span class="hljs-comment">// 获取文件类型</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_file_type</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> DiskInodeType;<br>    <span class="hljs-comment">// 检查文件的类型来判断是否是目录</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">is_dir</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span> &#123;<br>        <span class="hljs-keyword">self</span>.<span class="hljs-title function_ invoke__">get_file_type</span>() == DiskInodeType::Directory<br>    &#125;<br>    <span class="hljs-comment">// 检查文件的类型来判断是否是普通文件</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">is_file</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span> &#123;<br>        <span class="hljs-keyword">self</span>.<span class="hljs-title function_ invoke__">get_file_type</span>() == DiskInodeType::File<br>    &#125;<br>    <span class="hljs-comment">// 提供文件的目录树信息</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">info_dirtree_node</span>(&amp;<span class="hljs-keyword">self</span>, dirnode_ptr: Weak&lt;DirectoryTreeNode&gt;);<br>    <span class="hljs-comment">// 获取文件所在的目录树节点</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_dirtree_node</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;Arc&lt;DirectoryTreeNode&gt;&gt;;<br>    <span class="hljs-comment">// 打开文件</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">open</span>(&amp;<span class="hljs-keyword">self</span>, flags: OpenFlags, special_use: <span class="hljs-type">bool</span>) <span class="hljs-punctuation">-&gt;</span> Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;;<br>    <span class="hljs-comment">// 打开子文件（如果是个目录）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">open_subfile</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">Vec</span>&lt;(<span class="hljs-type">String</span>, Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;)&gt;, <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 创建文件</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">create</span>(&amp;<span class="hljs-keyword">self</span>, name: &amp;<span class="hljs-type">str</span>, file_type: DiskInodeType) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;, <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 链接一个子文件（用于目录）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">link_child</span>(&amp;<span class="hljs-keyword">self</span>, name: &amp;<span class="hljs-type">str</span>, child: &amp;<span class="hljs-keyword">Self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;<br>    <span class="hljs-keyword">where</span><br>        <span class="hljs-keyword">Self</span>: <span class="hljs-built_in">Sized</span>;<br>    <span class="hljs-comment">/// delete(unlink)</span><br>    <span class="hljs-comment">// 删除文件或解除链接</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">unlink</span>(&amp;<span class="hljs-keyword">self</span>, delete: <span class="hljs-type">bool</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 获取文件目录项，返回一个包含文件目录信息的Dirent结构（Vec类型）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_dirent</span>(&amp;<span class="hljs-keyword">self</span>, count: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Vec</span>&lt;Dirent&gt;;<br>    <span class="hljs-comment">// 获取偏移量</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_offset</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br>        <span class="hljs-keyword">self</span>.<span class="hljs-title function_ invoke__">lseek</span>(<span class="hljs-number">0</span>, SeekWhence::SEEK_CUR).<span class="hljs-title function_ invoke__">unwrap</span>()<br>    &#125;<br>    <span class="hljs-comment">// lseek 文件定位操作</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">lseek</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">isize</span>, whence: SeekWhence) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">usize</span>, <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 修改文件大小（改变量为diff）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">modify_size</span>(&amp;<span class="hljs-keyword">self</span>, diff: <span class="hljs-type">isize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 截断文件大小（设置量为new_size）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">truncate_size</span>(&amp;<span class="hljs-keyword">self</span>, new_size: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;;<br>    <span class="hljs-comment">// 设置文件时间戳，包含创建时间、访问时间和修改时间</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">set_timestamp</span>(&amp;<span class="hljs-keyword">self</span>, ctime: <span class="hljs-type">Option</span>&lt;<span class="hljs-type">usize</span>&gt;, atime: <span class="hljs-type">Option</span>&lt;<span class="hljs-type">usize</span>&gt;, mtime: <span class="hljs-type">Option</span>&lt;<span class="hljs-type">usize</span>&gt;);<br>    <span class="hljs-comment">// 获取单个缓存页</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_single_cache</span>(&amp;<span class="hljs-keyword">self</span>, offset: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;Mutex&lt;PageCache&gt;&gt;, ()&gt;;<br>    <span class="hljs-comment">// 获取所有缓存页</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_all_caches</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">Vec</span>&lt;Arc&lt;Mutex&lt;PageCache&gt;&gt;&gt;, ()&gt;;<br>    <span class="hljs-comment">// 内存溢出时处理</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">oom</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span>;<br>    <span class="hljs-comment">// 判断是否属于挂起状态</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">hang_up</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span>;<br>    <span class="hljs-comment">// 设备控制操作，允许直接向设备发送命令</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">ioctl</span>(&amp;<span class="hljs-keyword">self</span>, _cmd: <span class="hljs-type">u32</span>, _argp: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span> &#123;<br>        ENOTTY<br>    &#125;<br>    <span class="hljs-comment">// 文件控制操作，用于修改文件描述符的属性</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fcntl</span>(&amp;<span class="hljs-keyword">self</span>, cmd: <span class="hljs-type">u32</span>, arg: <span class="hljs-type">u32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span>;<br>&#125;<br>impl_downcast!(sync File);<br><span class="hljs-comment">// 这个宏表示 File trait 支持动态类型转换</span><br><span class="hljs-comment">// 允许在运行时将 dyn File 类型转换为具体类型</span><br></code></pre></td></tr></table></figure><hr><h3 id="1-3-fs-filesystem-rs"><a href="#1-3-fs-filesystem-rs" class="headerlink" title="1.3 fs&#x2F;filesystem.rs"></a>1.3 fs&#x2F;filesystem.rs</h3><h4 id="1-3-1-lazy-static-段"><a href="#1-3-1-lazy-static-段" class="headerlink" title="1.3.1 lazy_static! 段"></a>1.3.1 lazy_static! 段</h4><p>一段不知所云的 <code>lazy_static！</code> 代码</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust">lazy_static! &#123;<br><span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> FS_ID_COUNTER: Arc&lt;Mutex&lt;<span class="hljs-type">usize</span>&gt;&gt; = Arc::<span class="hljs-title function_ invoke__">new</span>(Mutex::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-number">0</span>));<br>&#125;<br></code></pre></td></tr></table></figure><p>这段代码创建了一个全局的、线程安全的 <code>FS_ID_COUNTER</code> 变量，初始化为0。这个计数器被 <code>Arc</code> 包裹，以便在多个线程间共享，并且通过 <code>Mutex</code> 确保在任何时刻，只有一个线程能够访问或修改计数器的值。</p><ul><li><p><code>static ref</code> 是 <code>lazy_static!</code> 宏的关键字（由lazy_static!提供），表示声明一个静态的、可变的引用。这个变量会在程序运行时被初始化并且保持全局唯一，直到程序退出。</p><ul><li><p><code>FS_ID_COUNTER</code> 是变量的名字，可以被其他代码访问。</p></li><li><p>由于是静态变量，Rust 会确保在整个程序生命周期内只有一个 <code>FS_ID_COUNTER</code> 实例存在，并且它在多线程环境下是安全的。</p></li></ul></li></ul><ul><li><code>Arc&lt;Mutex&lt;usize&gt;&gt;</code> 是 <code>FS_ID_COUNTER</code> 的类型，下面是它的具体含义：<ul><li><strong>Mutex</strong>：<code>Mutex</code> 是一种互斥锁，用于保证数据在多线程环境中的安全访问。只有一个线程可以访问被 <code>Mutex</code> 包裹的数据，其他线程必须等待直到该线程释放锁。</li></ul></li></ul><h4 id="1-3-2-FS-枚举"><a href="#1-3-2-FS-枚举" class="headerlink" title="1.3.2 FS 枚举"></a>1.3.2 FS 枚举</h4><p>枚举类型 FS，包含文件系统类型</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">enum</span> <span class="hljs-title class_">FS</span> &#123;<br>    NULL,<br>    Fat32,<br>    Ext4,<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到这里有</p><ul><li>NULL：应该是找不到类型的时候用的</li><li>Fat32：已经实现好的文件系统支持</li><li>Ext4: <strong>我添加的还没有实现的文件系统支持</strong></li></ul><h4 id="1-3-3-FileSystem-结构体"><a href="#1-3-3-FileSystem-结构体" class="headerlink" title="1.3.3 FileSystem 结构体"></a>1.3.3 FileSystem 结构体</h4><p><code>FileSystem</code> 数据结构</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FileSystem</span>&#123;<br>    <span class="hljs-keyword">pub</span> fs_id: <span class="hljs-type">usize</span>,<br>    <span class="hljs-keyword">pub</span> fs_type: FS,<span class="hljs-comment">// 即上面的枚举类型</span><br>&#125;<br></code></pre></td></tr></table></figure><p>剩下的就是FileSystem的方法，只有一个new方法（关联函数）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">FileSystem</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(fs_type: FS) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span> &#123;<br>        FS_ID_COUNTER.<span class="hljs-title function_ invoke__">lock</span>().<span class="hljs-title function_ invoke__">add_assign</span>(<span class="hljs-number">1</span>);<br>        <span class="hljs-keyword">let</span> <span class="hljs-variable">fs_id</span> = *FS_ID_COUNTER.<span class="hljs-title function_ invoke__">lock</span>();<br>        <span class="hljs-keyword">Self</span> &#123; fs_id, fs_type &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到，<code>FS_ID_COUNTER</code> 先上锁然后加 <code>1</code></p><p>然后将加 <code>1</code> 后的值给 <code>fs_id</code></p><hr><h3 id="1-4-fs-directory-tree-rs"><a href="#1-4-fs-directory-tree-rs" class="headerlink" title="1.4 fs&#x2F;directory_tree.rs"></a>1.4 fs&#x2F;directory_tree.rs</h3><h4 id="1-4-1-lazy-static-段"><a href="#1-4-1-lazy-static-段" class="headerlink" title="1.4.1 lazy_static! 段"></a>1.4.1 lazy_static! 段</h4><p>首先是一坨 <code>lazy_static</code>! 代码</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs rust">lazy_static! &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> FILE_SYSTEM: Arc&lt;EasyFileSystem&gt; = EasyFileSystem::<span class="hljs-title function_ invoke__">open</span>(<br>        BLOCK_DEVICE.<span class="hljs-title function_ invoke__">clone</span>(),<br>        Arc::<span class="hljs-title function_ invoke__">new</span>(Mutex::<span class="hljs-title function_ invoke__">new</span>(BlockCacheManager::<span class="hljs-title function_ invoke__">new</span>()))<br>    );<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> ROOT: Arc&lt;DirectoryTreeNode&gt; = &#123;<br>        <span class="hljs-keyword">let</span> <span class="hljs-variable">inode</span> = DirectoryTreeNode::<span class="hljs-title function_ invoke__">new</span>(<br>            <span class="hljs-string">&quot;&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(),<br>            Arc::<span class="hljs-title function_ invoke__">new</span>(FileSystem::<span class="hljs-title function_ invoke__">new</span>(FS::Fat32)),<br>            OSInode::<span class="hljs-title function_ invoke__">new</span>(InodeImpl::<span class="hljs-title function_ invoke__">root_inode</span>(&amp;FILE_SYSTEM)),<br>            Weak::<span class="hljs-title function_ invoke__">new</span>(),<br>        );<br>        inode.<span class="hljs-title function_ invoke__">add_special_use</span>();<br>        inode<br>    &#125;;<br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> DIRECTORY_VEC: Mutex&lt;(<span class="hljs-type">Vec</span>&lt;Weak&lt;DirectoryTreeNode&gt;&gt;, <span class="hljs-type">usize</span>)&gt; =<br>        Mutex::<span class="hljs-title function_ invoke__">new</span>((<span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">new</span>(), <span class="hljs-number">0</span>));<br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> PATH_CACHE: Mutex&lt;(<span class="hljs-type">String</span>, Weak&lt;DirectoryTreeNode&gt;)&gt; =<br>        Mutex::<span class="hljs-title function_ invoke__">new</span>((<span class="hljs-string">&quot;&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), Weak::<span class="hljs-title function_ invoke__">new</span>()));<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到包含四个变量，全部都是 <code>pub static ref</code> ，所以只能放在 <code>lazy_static!</code> 宏中</p><ul><li>FILE_SYSTEM：类型为 <code>Arc&lt;EasyFileSystem&gt;</code>，即用 <code>fat32</code> 模块内部的 <code>efs</code> 模块（很明显这里需要做一步解耦，将 <code>efs</code> 拆分出 <code>fat32</code> ，然后添加对 <code>ext4</code> 的支持）</li><li>ROOT：类型为 <code>Arc&lt;DirectoryTreeNode&gt;</code>， 即文件系统的根目录节点</li><li>DIRECTORY_VEC：类型为 <code>Mutex&lt;(Vec&lt;Weak&lt;DirectoryTreeNode&gt;&gt;, usize)&gt;</code>，存储一个元组：<ul><li><code>Vec&lt;Weak&lt;DirectoryTreeNode&gt;&gt;</code>：这是一个 <code>Weak</code> 引用的向量，<code>Weak</code> 引用用于避免循环引用，表示对目录树节点（<code>DirectoryTreeNode</code>）的弱引用。这意味着这些引用不会阻止目录节点的内存被回收。</li><li><code>usize</code>：初始化为 <code>0</code> ，用于计数，每次 <code>delete_directory_vec()</code> 操作后这个值都会自增 <code>1</code> 。</li></ul></li><li>PATH_CACHE：<code>PATH_CACHE</code> 用于优化路径查找。当系统访问路径时，它可以缓存路径和目录节点，避免每次都进行完整的路径解析，提高效率。</li></ul><h4 id="1-4-2-directory-vec-函数组"><a href="#1-4-2-directory-vec-函数组" class="headerlink" title="1.4.2 directory_vec 函数组"></a>1.4.2 directory_vec 函数组</h4><p>然后是三个关于 <code>directory_vec</code> 的函数</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">insert_directory_vec</span>(inode: Weak&lt;DirectoryTreeNode&gt;) &#123;...&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">delete_directory_vec</span>() &#123;...&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">update_directory_vec</span>(lock: &amp;<span class="hljs-keyword">mut</span> MutexGuard&lt;(<span class="hljs-type">Vec</span>&lt;Weak&lt;DirectoryTreeNode&gt;&gt;, <span class="hljs-type">usize</span>)&gt;) &#123;...&#125;<br></code></pre></td></tr></table></figure><p>即增、删、改</p><ul><li>insert：往 <code>DIRECTORY_VEC</code> 塞一个 <code>inode</code></li><li>delete：用于实现 <code>Drop</code> 特征，使 <code>DIRECTORY_VEC</code> 的内部的计数器加 <code>1</code>，当计数器值大于等于节点数量的一半时，调用 <code>update</code> 进行优化处理并重置计数器为 <code>0</code></li><li>update：优化处理无效的节点：创建一个新变量(与 <code>DIRECTORY_VEC</code> 第一个字段类型相同)，遍历 <code>DIRECTORY_VEC</code> 获取没被 <code>Drop</code> 的节点，<code>push</code> 到这个新变量里面，然后将新变量的值赋给 <code>DIRECTORY_VEC</code></li></ul><h4 id="1-4-3-DirectoryTreeNode-结构体"><a href="#1-4-3-DirectoryTreeNode-结构体" class="headerlink" title="1.4.3 DirectoryTreeNode 结构体"></a>1.4.3 DirectoryTreeNode 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">DirectoryTreeNode</span> &#123;<br>    <span class="hljs-comment">/// 如果这是个目录</span><br>    <span class="hljs-comment">/// 1. cwd （当前工作目录？）</span><br>    <span class="hljs-comment">/// 2. mount point 挂载点</span><br>    <span class="hljs-comment">/// 3. root node 根节点</span><br>    <span class="hljs-comment">/// 如果这是个文件</span><br>    <span class="hljs-comment">/// 1. executed by some processes 被某些进程执行</span><br>    <span class="hljs-comment">/// This parameter will add 1 when opening</span><br>    <span class="hljs-comment">/// spe_usage 在该文件被打开时增加1（使用计数）</span><br>    <span class="hljs-comment">/// 该字段即 special_usage 的简写</span><br>    spe_usage: Mutex&lt;<span class="hljs-type">usize</span>&gt;,<br>    <span class="hljs-comment">/// 文件名</span><br>    name: <span class="hljs-type">String</span>,<br>    <span class="hljs-comment">/// 该节点所属的文件系统类型</span><br>    filesystem: Arc&lt;FileSystem&gt;,<br>    <span class="hljs-comment">/// 存储该节点对应的文件对象</span><br>    file: Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;,<br>    <span class="hljs-comment">/// 自身的弱引用</span><br>    selfptr: Mutex&lt;Weak&lt;<span class="hljs-keyword">Self</span>&gt;&gt;,<br>    <span class="hljs-comment">/// 父节点的弱引用</span><br>    father: Mutex&lt;Weak&lt;<span class="hljs-keyword">Self</span>&gt;&gt;,<br>    <span class="hljs-comment">/// 可能存在的子节点</span><br>    children: RwLock&lt;<span class="hljs-type">Option</span>&lt;BTreeMap&lt;<span class="hljs-type">String</span>, Arc&lt;<span class="hljs-keyword">Self</span>&gt;&gt;&gt;&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>children</code> 是一个 <code>RwLock</code>，表示该字段可能会被多个线程同时读，但只有一个线程可以写。<code>RwLock</code> 确保多线程访问时的数据一致性。</li></ul><p>实现了 <code>Drop</code> 特征，该特征在被触发的时候（也即 <code>DirectoryTreeNode</code> 被销毁的时候）会调用上面的 <code>delete_directory_vec</code> 函数。</p><p>实现了如下方法：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">DirectoryTreeNode</span> &#123;<br>    <span class="hljs-comment">// 创建一个新目录树节点（Arc智能指针类型）</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(...) <span class="hljs-punctuation">-&gt;</span> Arc&lt;<span class="hljs-keyword">Self</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 文件被打开的相关的场景的时候会用到如下两个函数</span><br>    <span class="hljs-comment">// add_special_use spe_usage的值增加1</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_special_use</span>(&amp;<span class="hljs-keyword">self</span>)&#123;...&#125;<br>    <span class="hljs-comment">// sub_special_use spe_usage的值减少1</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">sub_special_use</span>(&amp;<span class="hljs-keyword">self</span>)&#123;...&#125;<br><br>    <span class="hljs-comment">// 获取当前工作目录，返回的是一个String类型（绝对路径）</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_cwd</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span>&#123;...&#125;<br>    <br>    <span class="hljs-comment">// 获取自身的强引用（从弱引用 selfptr upgrade 得来）（似乎就是一个inode）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_arc</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span>&#123;...&#125;<br>    <span class="hljs-comment">// 解析路径，返回一个 Vec&lt;&amp;str&gt;类型</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">parse_dir_path</span>(path: &amp;<span class="hljs-type">str</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Vec</span>&lt;&amp;<span class="hljs-type">str</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 缓存该文件夹下的所有子文件到lock中（参数中的lock）</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">cache_all_subfile</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 尝试获取子文件，从上面的cache_call_subfile中获得，</span><br>    <span class="hljs-comment">// 文件名由参数name提供</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">try_to_open_subfile</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">Self</span>&gt;, <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <br>    <span class="hljs-comment">// 通过一个动态数组参数进入某个目录</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">cd_comp</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">Self</span>&gt;, <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 调用 cd_comp 方法，通过一个字符串 path 来进入某个目录</span><br>    <span class="hljs-comment">// 其中 path 会调用 parse_dir_path 方法来解析</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">cd_path</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">Self</span>&gt;, <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <br>    <span class="hljs-comment">// 创建一个子文件，文件名和文件类型由参数提供</span><br>    <span class="hljs-comment">// 其中文件类型存储在fat32下，需要解耦</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">create</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;, <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 模拟文件系统的 open 调用</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">open</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;Arc&lt;<span class="hljs-keyword">dyn</span> File&gt;, <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 创建一个文件夹，同时会调用上面的多个方法</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">mkdir</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 删除一个文件夹或文件</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">delete</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>    <span class="hljs-comment">// 重命名一个文件夹或文件</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">rename</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;(), <span class="hljs-type">isize</span>&gt;&#123;...&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="1-4-4-init-fs-函数组"><a href="#1-4-4-init-fs-函数组" class="headerlink" title="1.4.4 init_fs 函数组"></a>1.4.4 init_fs 函数组</h4><p>接着是 <code>init_fs</code> 初始化文件系统函数</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">init_fs</span>() &#123;<br>    <span class="hljs-title function_ invoke__">init_device_directory</span>();<br>    <span class="hljs-title function_ invoke__">init_tmp_directory</span>();<br>    <span class="hljs-title function_ invoke__">init_proc_directory</span>();<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到包含了三个初始化函数：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 初始化设备目录</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">init_device_directory</span>() &#123;...&#125;<br><span class="hljs-comment">// 初始化 /tmp 目录</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">init_tmp_directory</span>() &#123;...&#125;<br><span class="hljs-comment">// 初始化 /proc 目录（进程目录）</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">init_proc_directory</span>() &#123;...&#125;<br></code></pre></td></tr></table></figure><h4 id="1-4-5-其他"><a href="#1-4-5-其他" class="headerlink" title="1.4.5 其他"></a>1.4.5 其他</h4><h6 id="oom"><a href="#oom" class="headerlink" title="oom"></a>oom</h6><p>即 <strong>Out Of Memory</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[cfg(feature = <span class="hljs-string">&quot;oom_handler&quot;</span>)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">oom</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;...&#125;<br></code></pre></td></tr></table></figure><p>用于处理OOM的情况，被 mm 模块调用</p><p>会调用 <code>tlb_invalidate</code> 函数，在 <code>arch/la64</code> 中实现</p><p>会调用 <code>update_directory_vec</code> 函数</p><p>返回被释放的内存页数（只找到如下部分会返回不为 <code>0</code> 的值）：</p><p><code>src/fs/fat32/vfs.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">/// When memory is low, it is called to free its cache</span><br><span class="hljs-comment">/// it just tries to lock it&#x27;s file contents to free memory</span><br><span class="hljs-comment">/// # Return Value</span><br><span class="hljs-comment">/// The number of freed pages</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">oom</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">neighbor</span> = |inner_cache_id| &#123;<br>        <span class="hljs-keyword">self</span>.<span class="hljs-title function_ invoke__">get_neighboring_sec</span>(&amp;<span class="hljs-keyword">self</span>.file_content.<span class="hljs-title function_ invoke__">read</span>().clus_list, inner_cache_id)<br>    &#125;;<br>    <span class="hljs-keyword">self</span>.file_cache_mgr.<span class="hljs-title function_ invoke__">oom</span>(neighbor, &amp;<span class="hljs-keyword">self</span>.fs.block_device)<br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="1-5-fs-poll-rs"><a href="#1-5-fs-poll-rs" class="headerlink" title="1.5 fs&#x2F;poll.rs"></a>1.5 fs&#x2F;poll.rs</h3><h4 id="1-5-1-bitflags-段"><a href="#1-5-1-bitflags-段" class="headerlink" title="1.5.1 bitflags! 段"></a>1.5.1 bitflags! 段</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">bitflags!</span> &#123;<br>    <span class="hljs-keyword">struct</span> <span class="hljs-title class_">PollEvent</span>: <span class="hljs-type">u16</span> &#123;<br>        <span class="hljs-comment">// 要监视的事件</span><br>        <span class="hljs-keyword">const</span> POLLIN = <span class="hljs-number">0x001</span>;        <span class="hljs-comment">// 有数据可读。</span><br>        <span class="hljs-keyword">const</span> POLLPRI = <span class="hljs-number">0x002</span>;       <span class="hljs-comment">// 有紧急数据可读。</span><br>        <span class="hljs-keyword">const</span> POLLOUT = <span class="hljs-number">0x004</span>;       <span class="hljs-comment">// 可以写数据而不会阻塞。</span><br><br>        <span class="hljs-comment">// 这些值是 XPG4.2（POSIX 标准）定义的</span><br>        <span class="hljs-keyword">const</span> POLLRDNORM = <span class="hljs-number">0x040</span>;    <span class="hljs-comment">// 有常规数据可读。</span><br>        <span class="hljs-keyword">const</span> POLLRDBAND = <span class="hljs-number">0x080</span>;    <span class="hljs-comment">// 有优先级数据可读。</span><br>        <span class="hljs-keyword">const</span> POLLWRNORM = <span class="hljs-number">0x100</span>;    <span class="hljs-comment">// 可以写常规数据。</span><br>        <span class="hljs-keyword">const</span> POLLWRBAND = <span class="hljs-number">0x200</span>;    <span class="hljs-comment">// 可以写优先级数据。</span><br><br>        <span class="hljs-comment">// Linux 扩展</span><br>        <span class="hljs-keyword">const</span> POLLMSG = <span class="hljs-number">0x400</span>;       <span class="hljs-comment">// 有消息可读。</span><br>        <span class="hljs-keyword">const</span> POLLREMOVE = <span class="hljs-number">0x1000</span>;   <span class="hljs-comment">// 从 poll 集合中移除文件描述符。</span><br>        <span class="hljs-keyword">const</span> POLLRDHUP = <span class="hljs-number">0x2000</span>;    <span class="hljs-comment">// 流套接字的对端关闭了连接。</span><br><br>        <span class="hljs-comment">// 始终隐式监视的事件（在 `revents` 中出现，但不需要在 `events` 中设置）</span><br>        <span class="hljs-keyword">const</span> POLLERR = <span class="hljs-number">0x008</span>;       <span class="hljs-comment">// 错误条件。</span><br>        <span class="hljs-keyword">const</span> POLLHUP = <span class="hljs-number">0x010</span>;       <span class="hljs-comment">// 挂起（例如连接断开）。</span><br>        <span class="hljs-keyword">const</span> POLLNVAL = <span class="hljs-number">0x020</span>;      <span class="hljs-comment">// 无效的文件描述符（无效的 polling 请求）。</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="1-5-2-PollFd-结构体"><a href="#1-5-2-PollFd-结构体" class="headerlink" title="1.5.2 PollFd 结构体"></a>1.5.2 PollFd 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[repr(C)]</span><br><span class="hljs-meta">#[derive(Debug, Clone, Copy)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">PollFd</span> &#123;<br>    <span class="hljs-comment">/// File descriptor</span><br>    <span class="hljs-comment">/// 文件描述符</span><br>    <span class="hljs-comment">/// 0 代表标准输入 stdin</span><br>    <span class="hljs-comment">/// 1 代表标准输出 stdout</span><br>    <span class="hljs-comment">/// 2 代表标准错误 stderr</span><br>    <span class="hljs-comment">/// 其他数字则表示由 open() 或 socket() 等系统调用打开的文件、网络套接字等。</span><br>    fd: <span class="hljs-type">u32</span>,<br>    <span class="hljs-comment">/// Requested events</span><br>    <span class="hljs-comment">/// 请求的事件</span><br>    events: PollEvent,<br>    <span class="hljs-comment">/// Returned events</span><br>    <span class="hljs-comment">/// 返回的事件</span><br>    revents: PollEvent,<br>&#125;<br></code></pre></td></tr></table></figure><p>含有三个字段。</p><h4 id="1-5-3-ppoll-函数"><a href="#1-5-3-ppoll-函数" class="headerlink" title="1.5.3 ppoll 函数"></a>1.5.3 ppoll 函数</h4><blockquote><p><code>poll()</code> 函数用于等待一组文件描述符（<code>poll_fd</code>）上发生某些事件，或者等待时间限制到期。如果设置了事件，<code>poll()</code> 会检查指定的文件描述符上的事件是否发生。与 <code>select()</code> 函数的工作方式不同，<code>poll()</code> 使用的是 <strong>OR</strong> 的方式（即文件描述符上的多个事件可以同时发生），而 <code>select()</code> 是 <strong>AND</strong> 的方式（即多个事件条件必须同时满足）。</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">ppoll</span>(<br>    fds: *<span class="hljs-keyword">mut</span> PollFd,               <span class="hljs-comment">// 文件描述符数组，包含要监视的文件描述符和对应的事件类型</span><br>    nfds: <span class="hljs-type">usize</span>,                    <span class="hljs-comment">// 文件描述符的数量,告诉 poll() 函数需要检查多少个文件描述符。</span><br>    tmo_p: *<span class="hljs-keyword">const</span> TimeSpec,         <span class="hljs-comment">// 超时值，如果为 `None` 则表示无限等待,在当前实现中，超时功能尚未支持，该参数会被忽略。</span><br>    sigmask: *<span class="hljs-keyword">const</span> Signals,        <span class="hljs-comment">// 信号屏蔽，指定当前线程在等待过程中不应处理哪些信号</span><br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span>                          <span class="hljs-comment">// 返回操作结果：成功时返回 0，失败时返回错误码</span><br></code></pre></td></tr></table></figure><h4 id="1-5-4-FdSet-结构体"><a href="#1-5-4-FdSet-结构体" class="headerlink" title="1.5.4 FdSet 结构体"></a>1.5.4 FdSet 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[derive(Clone, Copy, Debug)]</span><br><span class="hljs-meta">#[repr(C)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">FdSet</span> &#123;<br>    bits: [<span class="hljs-type">u64</span>; <span class="hljs-number">16</span>],<br>&#125;<br></code></pre></td></tr></table></figure><p><code>FdSet</code> 使用一个包含 16 个 <code>u64</code> 数组 (<code>bits: [u64; 16]</code>) 来存储文件描述符的位图，每个 <code>u64</code> 对应一个文件描述符的 64 个 bit 位。</p><p>方法实现：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">FdSet</span> &#123;<br>    <span class="hljs-comment">/// Return an empty bitmap for further manipulation</span><br>    <span class="hljs-comment">/// 返回一个空的 FdSet，即所有的位都是 0，表示没有文件描述符被设置。</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">empty</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span> &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// Divide `d` by 64 to decide the `u64` in `bits` to visit.</span><br>    <span class="hljs-comment">/// 因为是按照c语言的内存结构设置的，所以直接通过对d除以64的操作来获取其在bits的位置</span><br>    <span class="hljs-comment">/// elt是element的简写，表示元素</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fd_elt</span>(d: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// 计算文件描述符 d 在 u64 数组中的具体位置对应的位掩码。</span><br>    <span class="hljs-comment">/// 取低6位，即取模64，然后左移1位，即乘2</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fd_mask</span>(d: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">u64</span> &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// Clear the current struct.</span><br>    <span class="hljs-comment">/// 对所有位置零</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">clr_all</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>) &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// Collect all fds with their bits set.</span><br>    <span class="hljs-comment">/// 遍历所有的位，将被设置的位的索引放入一个Vec中</span><br>    <span class="hljs-comment">///（总位数为 16*64 = 1024位）</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_fd_vec</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">usize</span>&gt; &#123;...&#125;<br><br>    <span class="hljs-comment">/// The total number of set bits.</span><br>    <span class="hljs-comment">/// 计算所有被设置的位的个数</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">set_num</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">u32</span> &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// 将文件描述符 d 对应的位设置为 1</span><br>    <span class="hljs-comment">/// 表示该文件描述符已经被设置</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">set</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>, d: <span class="hljs-type">usize</span>) &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// Clear a certain bit `d` to stop waiting for the event of the correspond fd.</span><br>    <span class="hljs-comment">/// 将文件描述符 d 对应的位设置为 0</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">clr</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>, d: <span class="hljs-type">usize</span>) &#123;...&#125;<br>    <br>    <span class="hljs-comment">/// Predicate for whether the bit is set for the `d`</span><br>    <span class="hljs-comment">/// 判断文件描述符 d 对应的位是否被设置</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">is_set</span>(&amp;<span class="hljs-keyword">self</span>, d: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">bool</span> &#123;...&#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>然后为 <code>FdSet</code> 实现了 <code>Bytes</code> 特征，该特征定义在</p><p><code>src/lang_items.rs</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">Bytes</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">as_bytes</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> &amp;[<span class="hljs-type">u8</span>] &#123;...&#125;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">as_bytes_mut</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> &amp;<span class="hljs-keyword">mut</span> [<span class="hljs-type">u8</span>] &#123;...&#125;<br></code></pre></td></tr></table></figure><h4 id="1-5-5-pselect-函数"><a href="#1-5-5-pselect-函数" class="headerlink" title="1.5.5 pselect 函数"></a>1.5.5 pselect 函数</h4><p>类似于 Unix 系统中的 <code>pselect()</code> 系统调用，目的是通过轮询多个文件描述符，等待某些事件发生，如可读、可写或异常状态。它可以设置超时，并且支持信号屏蔽（<code>sigmask</code>）。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">pselect</span>(<br>    nfds: <span class="hljs-type">usize</span>,<br>    read_fds: &amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">Option</span>&lt;FdSet&gt;,<br>    write_fds: &amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">Option</span>&lt;FdSet&gt;,<br>    exception_fds: &amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">Option</span>&lt;FdSet&gt;,<br>    timeout: &amp;<span class="hljs-type">Option</span>&lt;TimeSpec&gt;,<br>    sigmask: *<span class="hljs-keyword">const</span> Signals,<br>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">isize</span> &#123;...&#125;<br></code></pre></td></tr></table></figure><ul><li><code>pselect</code> 是一个用于轮询多个文件描述符的函数，它可以等待某些事件的发生（如可读、可写或异常），并支持超时和信号屏蔽。</li><li>它的工作原理是通过不断轮询文件描述符集合，检查它们是否准备好，如果有准备好的文件描述符，函数会返回相应的数量。如果超时或没有事件发生，函数会退出。</li><li>它使用了 <code>FdSet</code> 来表示文件描述符集合，并通过与 <code>fd_table</code> 中的文件描述符进行比较来确定它们是否就绪。</li></ul><hr><h3 id="1-6-fs-layout-rs"><a href="#1-6-fs-layout-rs" class="headerlink" title="1.6 fs&#x2F;layout.rs"></a>1.6 fs&#x2F;layout.rs</h3><h4 id="1-6-1-bitflags-段"><a href="#1-6-1-bitflags-段" class="headerlink" title="1.6.1 bitflags! 段"></a>1.6.1 bitflags! 段</h4><p>首先是三大段 <code>bitflags!</code> 宏，全部都是常量，用于设置对应标志位</p><ul><li><p>OpenFlags——O_xxx：表示文件打开时使用的标志位</p><ul><li>O_RDONLY</li><li>O_WRONLY</li><li>O_RDWR</li><li>O_CREAT</li><li>O_EXCL</li><li>O_NOCTTY</li><li>O_TRUNC</li><li>O_APPEND</li><li>O_NONBLOCK</li><li>O_DSYNC</li><li>O_SYNC</li><li>O_RSYNC</li><li>O_DIRECTORY</li><li>O_NOFOLLOW</li><li>O_CLOEXEC</li><li>O_ASYNC</li><li>O_DIRECT</li><li>O_LARGEFILE</li><li>O_NOATIME</li><li>O_PATH</li><li>O_TMPFILE</li></ul></li><li><p>SeekWhence——SEEK_xxx：</p><ul><li>SEEK_SET</li><li>SEEK_CUR</li><li>SEEK_END</li></ul></li><li><p>StatMode——S_xxx：定义文件类型和文件权限</p><ul><li><p>S_IFMT：掩码，用于从 <code>st_mode</code> 提取文件类型</p></li><li><p>S_IFSOCK：socket 套接字</p></li><li><p>S_IFLINK：symbolic link 符号链接</p></li><li><p>S_IFREG：regular file 常规文件</p></li><li><p>S_IFBLK：block device 块设备</p></li><li><p>S_IFDIR：directory 目录（文件夹）</p></li><li><p>S_IFCHR：character device 字符设备</p></li><li><p>S_IFIFO：FIFO 命名管道</p><p>设置ID标志位组</p></li><li><p>S_ISUID：设置用户ID位</p></li><li><p>S_ISGID：设置组ID位</p></li><li><p>S_ISVTX：Sticky 位，通常用于 <strong>目录</strong>，是一种特殊的权限位。当某个目录设置了 Sticky 位时，目录中的文件只能由文件的拥有者、目录的所有者或 root 用户删除或重命名。这通常用于共享目录（例如 <code>/tmp</code>），以防止用户删除或修改其他用户的文件。</p><p>用户权限标志位组</p></li><li><p>S_IRWXU</p></li><li><p>S_IRUSR</p></li><li><p>S_IWUSR</p></li><li><p>S_IXUSR</p><p>组权限标志位组</p></li><li><p>S_IRWXG</p></li><li><p>S_IRGRP</p></li><li><p>S_IWGRP</p></li><li><p>S_IXGRP</p><p>其他用户权限标志位组</p></li><li><p>S_IRWXO</p></li><li><p>S_IROTH</p></li><li><p>S_IWOTH</p></li><li><p>S_IXOTH</p></li></ul></li></ul><h4 id="1-6-2-常量"><a href="#1-6-2-常量" class="headerlink" title="1.6.2 常量"></a>1.6.2 常量</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">const</span> NAME_LIMIT: <span class="hljs-type">usize</span> = <span class="hljs-number">128</span>;<br></code></pre></td></tr></table></figure><p>即下面的 <code>Dirent</code>（<code>Directory Entry</code>）的 <code>d_name</code> 字段的限定大小值</p><h4 id="1-6-3-Stat-结构体"><a href="#1-6-3-Stat-结构体" class="headerlink" title="1.6.3 Stat 结构体"></a>1.6.3 Stat 结构体</h4><p>然后是 <code>Stat</code> 结构体（派生了<code>Clone, Copy, Debug</code> 的 <code>Trait</code>）</p><p>该结构体灰常重要（实际上是因为非常好理解），保存了文件的状态（从支持的文件，不支持的文件我暂时不知道会咋样）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Stat</span> &#123;<br>    <span class="hljs-comment">/// ID of device containing file</span><br>    st_dev: <span class="hljs-type">u64</span>,<br>    <span class="hljs-comment">/// Inode number</span><br>    st_ino: <span class="hljs-type">u64</span>,<br>    <span class="hljs-comment">/// File type and mode   </span><br>    st_mode: <span class="hljs-type">u32</span>,<br>    <span class="hljs-comment">/// Number of hard links</span><br>    st_nlink: <span class="hljs-type">u32</span>,<br>    <span class="hljs-comment">/// User ID of the file&#x27;s owner.</span><br>    st_uid: <span class="hljs-type">u32</span>,<br>    <span class="hljs-comment">/// Group ID of the file&#x27;s group.</span><br>    st_gid: <span class="hljs-type">u32</span>,<br>    <span class="hljs-comment">/// Device ID (if special file)</span><br>    st_rdev: <span class="hljs-type">u64</span>,<br>    __pad: <span class="hljs-type">u64</span>,<br>    <span class="hljs-comment">/// Size of file, in bytes.</span><br>    st_size: <span class="hljs-type">i64</span>,<br>    <span class="hljs-comment">/// Optimal block size for I/O.</span><br>    st_blksize: <span class="hljs-type">u32</span>,<br>    __pad2: <span class="hljs-type">i32</span>,<br>    <span class="hljs-comment">/// Number 512(2048?)-byte blocks allocated.</span><br>    st_blocks: <span class="hljs-type">u64</span>,<br>    <span class="hljs-comment">/// Backward compatibility. Used for time of last access.</span><br>    st_atime: TimeSpec,<br>    <span class="hljs-comment">/// Time of last modification.</span><br>    st_mtime: TimeSpec,<br>    <span class="hljs-comment">/// Time of last status change.</span><br>    st_ctime: TimeSpec,<br>    __unused: <span class="hljs-type">u64</span>,<br>&#125;<br></code></pre></td></tr></table></figure><p>接着是 <code>Stat</code> 的方法实现</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Stat</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_ino</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;...&#125;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_size</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;...&#125;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span>&#123;...&#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><p>get_ino：获取 <code>st_ino</code> ，即 <code>inode number</code></p></li><li><p>get_size：获取 <code>st_size</code>，即文件大小</p></li><li><p>new：关联函数，创建一个新的 <code>Stat</code> ，</p><ul><li><p><code>st_uid</code>：初始化为 <code>0</code></p></li><li><p><code>st_gid</code>：初始化为 <code>0</code></p></li><li><p><code>st_blksize</code>：BLK_SIZE，与BLOCK_SZ 相同，后者在 <code>os/src/arch/la64/board/2k500.rs</code> 内定义，大小为 <code>2048</code></p></li><li><p><code>st_blocks</code>：<code>(st_size 文件大小 + BLK_SIZE 块大小 - 1)/BLK_SIZE</code>  这个表达式确保至少 <code>st_blocks</code> 大小为 <code>1</code> 。</p><p><code>st_size</code> 的大小总为 <code>n * BLK_SIZE + remainder</code> ，那么经过这个式子转化后，当 <code>remainder</code> 为 <code>0</code> ， <code>st_blocks == n</code> </p><p>当 <code>remainder</code> 为 <code>1</code> ，<code>st_blocks == n + 1</code></p><p>这保证了最小块对齐。</p></li><li><p><code>__xxx</code>：全部初始化为 <code>0</code></p></li></ul></li></ul><h4 id="1-6-4-Dirent-结构体-为啥不命名为DirEnt？"><a href="#1-6-4-Dirent-结构体-为啥不命名为DirEnt？" class="headerlink" title="1.6.4 Dirent 结构体 (为啥不命名为DirEnt？)"></a>1.6.4 Dirent 结构体 (为啥不命名为DirEnt？)</h4><p>即 <code>Directory Entry</code> ，看注释意思是原生Linux 目录项结构，（好像就是用于文件夹）</p><p>理论上 <code>d_name</code> 字段并不是定长的，可以任意长，但是代码中将该字段设置为定长大小</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Dirent</span> &#123;<br>    <span class="hljs-comment">/// Inode number</span><br>    <span class="hljs-comment">/// Inode 索引节点号</span><br>    <span class="hljs-keyword">pub</span> d_ino: <span class="hljs-type">usize</span>,<br><br>    <span class="hljs-comment">/// Offset to next &#x27;linux_directory_entry&#x27;</span><br>    <span class="hljs-comment">/// 下一个目录项相对于该项的偏移量</span><br>    <span class="hljs-keyword">pub</span> d_off: <span class="hljs-type">isize</span>,<br>    <br>    <span class="hljs-comment">/// Length of current &#x27;linux_directory_entry&#x27;</span><br>    <span class="hljs-comment">/// 该目录项的长度</span><br>    <span class="hljs-keyword">pub</span> d_reclen: <span class="hljs-type">u16</span>,<br>    <br>    <span class="hljs-comment">/// Type of this file</span><br>    <span class="hljs-comment">/// 该文件的类型</span><br>    <span class="hljs-keyword">pub</span> d_type: <span class="hljs-type">u8</span>,<br>    <br>    <span class="hljs-comment">/// Fix-sized d_name array</span><br>    <span class="hljs-comment">/// 文件名 定长字符数组（u8）</span><br>    <span class="hljs-keyword">pub</span> d_name: [<span class="hljs-type">u8</span>; NAME_LIMIT],<br>&#125;<br></code></pre></td></tr></table></figure><p>然后是 <code>Dirent</code> 的实现</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Dirent</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(...) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-title function_ invoke__">Self</span>(...)<br>&#125;<br></code></pre></td></tr></table></figure><p>其中 <code>d_name</code> 大小初始化为 <code>0</code> ，然后使用 <code>copy_from_slice</code> 从关联函数 <code>new</code> 中给的参数复制。</p><p>好了没了</p><hr><h3 id="1-7-fs-swap-rs"><a href="#1-7-fs-swap-rs" class="headerlink" title="1.7 fs&#x2F;swap.rs"></a>1.7 fs&#x2F;swap.rs</h3><p><strong>交换空间相关</strong></p><h4 id="1-7-1-lazy-static"><a href="#1-7-1-lazy-static" class="headerlink" title="1.7.1 lazy_static!"></a>1.7.1 lazy_static!</h4><p>首先是一坨 <code>lazy_static!</code> </p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust">lazy_static! &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">ref</span> SWAP_DEVICE: Mutex&lt;Swap&gt; = Mutex::<span class="hljs-title function_ invoke__">new</span>(Swap::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-number">16</span>));<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到定义了一个 <code>pub static ref</code> ，类型为 <code>Mutex&lt;Swap&gt;</code>，初始值为16</p><p>该引用用于为 <code>SwapTracker</code> 类型实现 <code>Drop</code> 特征</p><h4 id="1-7-2-常量"><a href="#1-7-2-常量" class="headerlink" title="1.7.2 常量"></a>1.7.2 常量</h4><blockquote><p>都为 <code>usize</code> 类型</p></blockquote><ul><li>BLK_PER_PG：<code>PAGE_SIZE / BLOCK_SZ;</code> 每个交换空间页面对应的块数。一个页面的大小是 <code>PAGE_SIZE</code>，而每个块的大小是 <code>BLOCK_SZ</code> 。</li><li>SWAP_SIZE：<code>1024 * 1024;</code> 交换空间的总大小，这里设置为 1MB（1024 * 1024 字节）。该常量用于计算交换空间总量。</li></ul><h4 id="1-7-3-SwapTracker-结构体"><a href="#1-7-3-SwapTracker-结构体" class="headerlink" title="1.7.3 SwapTracker 结构体"></a>1.7.3 SwapTracker 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">SwapTracker</span>(<span class="hljs-keyword">pub</span> <span class="hljs-type">usize</span>);<br></code></pre></td></tr></table></figure><p>是一个元组类型的结构体，包含一个 <code>usize</code>类型变量</p><p>首先实现了 <code>Drop</code> 特征</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Drop</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">SwapTracker</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">drop</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-comment">// 丢弃 SwapTracker 实例时，将该页标记为已释放</span><br>        SWAP_DEVICE.<span class="hljs-title function_ invoke__">lock</span>().<span class="hljs-title function_ invoke__">discard</span>(<span class="hljs-keyword">self</span>.<span class="hljs-number">0</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="1-7-4-Swap-结构体"><a href="#1-7-4-Swap-结构体" class="headerlink" title="1.7.4 Swap 结构体"></a>1.7.4 Swap 结构体</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Swap</span> &#123;<br>    bitmap: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">u64</span>&gt;,<br>    block_ids: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">usize</span>&gt;,<br>&#125;<br></code></pre></td></tr></table></figure><p>Swap内部有两个字段</p><ul><li><code>bitmap</code>：位图，用来表示交换空间中的页面是否已经被分配。每一位表示一个页面，位为 1 表示该页面已被分配，位为 0 表示该页面未分配。</li><li><code>block_ids</code>：存储交换空间块编号的数组，表示每个交换空间页面（通常对应一个内存页）所对应的物理存储位置。</li></ul><p>Swap 有 <code>3</code> 个关联函数：</p><ul><li><code>new</code>：创建一个新的 <code>Swap</code> 实例</li><li><code>read_page</code>：从交换空间读取数据</li><li><code>write_page</code>：向交换空间写入数据</li></ul><p>有 <code>7</code> 个方法：</p><ul><li><code>set_bit</code>：位图中某一位置 1 ，表示该页已被使用</li><li><code>clear_bit</code>：位图中某一位置 0 ，标志该页被释放</li><li><code>alloc_page</code>：尝试为交换空间分配一个页面。如果找到了一个未分配的页面（位图中的位为 0），它会返回该页面的 <code>swap_id</code>，否则返回 <code>None</code>。</li><li><code>get_block_ids</code>：根据 <code>swap_id</code> 获取该交换页面对应的块 ID 列表。</li><li><code>read</code>：从交换空间读取数据，会调用 <code>read_page</code> 和 <code>get_block_id</code></li><li><code>write</code>：向交换空间写入数据，会调用上面多个函数，返回一个 <code>Arc</code> 包装的 <code>SwapTracker</code></li><li><code>discard</code>：调用 <code>clear_bit</code> ，将位图中的某一位清零</li></ul><hr><h3 id="1-8-fs-cache-rs"><a href="#1-8-fs-cache-rs" class="headerlink" title="1.8 fs&#x2F;cache.rs"></a>1.8 fs&#x2F;cache.rs</h3><hr><h2 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h2><blockquote><p>是什么</p><p>为什么</p><p>怎么做</p></blockquote><h3 id="附录一、操作系统概念"><a href="#附录一、操作系统概念" class="headerlink" title="附录一、操作系统概念"></a>附录一、操作系统概念</h3><h4 id="1-inode"><a href="#1-inode" class="headerlink" title="1. inode"></a>1. inode</h4><p><strong><code>inode</code></strong> 是文件系统中的数据结构，用于存储文件的元数据（权限、大小、时间戳等）和指向文件数据块的指针，但不包含文件的内容和文件名。</p><p>通过 <code>inode</code>，操作系统可以管理文件的存储和访问，允许文件通过硬链接共享同一个 <code>inode</code>。</p><h4 id="2-OOM"><a href="#2-OOM" class="headerlink" title="2. OOM"></a>2. OOM</h4><p>即 <strong>内存不足（Out of Memory）</strong></p><hr><h3 id="附录二、Rust-语言"><a href="#附录二、Rust-语言" class="headerlink" title="附录二、Rust 语言"></a>附录二、Rust 语言</h3><h4 id="1-Rust-基础"><a href="#1-Rust-基础" class="headerlink" title="1. Rust 基础"></a>1. Rust 基础</h4><h5 id="ref-与-的区别"><a href="#ref-与-的区别" class="headerlink" title="ref 与 &amp; 的区别"></a>ref 与 &amp; 的区别</h5><p><strong><code>&amp;</code></strong> 是用来创建引用的操作符，它在表达式中用于借用（不可变或可变）某个值。</p><p><strong><code>ref</code></strong> 用于在模式匹配中解构时创建引用。它不会像 <code>&amp;</code> 那样直接在表达式中借用一个值，而是在解构的过程中通过 <code>ref</code> 获取对值的引用。</p><h4 id="2-Rust-特征"><a href="#2-Rust-特征" class="headerlink" title="2. Rust 特征"></a>2. Rust 特征</h4><h5 id="Drop"><a href="#Drop" class="headerlink" title="Drop"></a>Drop</h5><h6 id="是什么"><a href="#是什么" class="headerlink" title="是什么"></a>是什么</h6><p>在 Rust 中，<code>Drop</code> 是一个特殊的 trait，它定义了对象销毁时需要执行的清理操作。</p><h6 id="为什么"><a href="#为什么" class="headerlink" title="为什么"></a>为什么</h6><p>当一个值的生命周期结束时，Rust 会自动调用该值的 <code>drop</code> 方法。这个方法通常用于释放资源，进行清理操作。Rust 的内存管理是基于所有权系统的，当一个变量超出作用域或被销毁时，Rust 会自动释放其占用的资源。</p><h6 id="怎么做"><a href="#怎么做" class="headerlink" title="怎么做"></a>怎么做</h6><p>可以通过为一个类型实现 <code>Drop</code> trait 来自定义清理逻辑。当一个对象不再需要时（比如当它超出作用域或被手动销毁时），Rust 会调用它的 <code>drop</code> 方法。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">MyStruct</span> &#123;<br>    name: <span class="hljs-type">String</span>,<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Drop</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">MyStruct</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">drop</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Dropping MyStruct with name: &#123;&#125;&quot;</span>, <span class="hljs-keyword">self</span>.name);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = MyStruct &#123; name: <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;example&quot;</span>) &#125;;<br>    <span class="hljs-comment">// 当 `s` 超出作用域时，Rust 会自动调用 `drop` 方法</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="3-Rust-宏"><a href="#3-Rust-宏" class="headerlink" title="3. Rust 宏"></a>3. Rust 宏</h4><h5 id="1-lazy-static-宏"><a href="#1-lazy-static-宏" class="headerlink" title="1. lazy_static! 宏"></a>1. lazy_static! 宏</h5><p><code>lazy_static!</code> 是 Rust 的一个宏，它用于声明一个静态变量，并且支持延迟初始化。普通的静态变量必须在编译时就已知其值，而 <code>lazy_static!</code> 允许我们在运行时初始化静态变量，只会在第一次访问时进行初始化。</p><p>形如 <code>static ref</code> 的用法只能在 <code>lazy_static!</code> 宏中使用。</p><h5 id="2-bitflags-宏"><a href="#2-bitflags-宏" class="headerlink" title="2. bitflags! 宏"></a>2. bitflags! 宏</h5><h6 id="是什么-1"><a href="#是什么-1" class="headerlink" title="是什么"></a>是什么</h6><p><code>bitflags!</code> 宏是 Rust 中用于简化位标志（bit flags）操作的一个宏，属于 <code>bitflags</code> crate 中的功能。位标志通常用于表示多个布尔状态，它通过位运算（如 AND、OR、NOT 等）将多个布尔值合并到一个整数类型的值中。这样做能够节省内存，并提高性能，特别是在需要存储多个状态时。</p><h6 id="为什么-1"><a href="#为什么-1" class="headerlink" title="为什么"></a>为什么</h6><p>在 Rust 中，位标志常常用于表示一组互斥或可组合的状态，例如：</p><ul><li>权限控制（读、写、执行等）</li><li>游戏中的多个开关（如：飞行、无敌、速度提升等）</li><li>系统标志（如：文件的打开模式、网络协议标志等）</li></ul><p>如果手动实现这些位标志的管理，代码会变得比较繁琐，因为需要手动定义每个位的值，并处理位运算。<code>bitflags!</code> 宏简化了这个过程，它通过提供一种更直观的方式来定义和操作位标志。</p><h6 id="怎么做-1"><a href="#怎么做-1" class="headerlink" title="怎么做"></a>怎么做</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> bitflags::bitflags;<br><br><span class="hljs-built_in">bitflags!</span> &#123;<br>    <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Permissions</span>: <span class="hljs-type">u32</span> &#123;<br>        <span class="hljs-keyword">const</span> READ = <span class="hljs-number">0b00000001</span>;  <span class="hljs-comment">// 第1位，表示可读</span><br>        <span class="hljs-keyword">const</span> WRITE = <span class="hljs-number">0b00000010</span>; <span class="hljs-comment">// 第2位，表示可写</span><br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">perms</span> = Permissions::<span class="hljs-title function_ invoke__">empty</span>(); <span class="hljs-comment">// 初始时没有任何权限</span><br><br>    <span class="hljs-comment">// 设置可读权限</span><br>    perms.<span class="hljs-title function_ invoke__">insert</span>(Permissions::READ);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Has READ permission: &#123;&#125;&quot;</span>, perms.<span class="hljs-title function_ invoke__">contains</span>(Permissions::READ)); <span class="hljs-comment">// true</span><br>    <br>    <span class="hljs-comment">// 设置可写权限</span><br>    perms.<span class="hljs-title function_ invoke__">insert</span>(Permissions::WRITE);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Has WRITE permission: &#123;&#125;&quot;</span>, perms.<span class="hljs-title function_ invoke__">contains</span>(Permissions::WRITE)); <span class="hljs-comment">// true</span><br>    <br>    <span class="hljs-comment">// 更改权限，移除可读权限，保持可写权限</span><br>    perms.<span class="hljs-title function_ invoke__">remove</span>(Permissions::READ);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Has READ permission: &#123;&#125;&quot;</span>, perms.<span class="hljs-title function_ invoke__">contains</span>(Permissions::READ)); <span class="hljs-comment">// false</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Has WRITE permission: &#123;&#125;&quot;</span>, perms.<span class="hljs-title function_ invoke__">contains</span>(Permissions::WRITE)); <span class="hljs-comment">// true</span><br>&#125;<br></code></pre></td></tr></table></figure><p><code>remove</code> 相当于对对应位置零。</p><h4 id="4-Rust-属性"><a href="#4-Rust-属性" class="headerlink" title="4. Rust 属性"></a>4. Rust 属性</h4><h5 id="1-repr-xxx-representation"><a href="#1-repr-xxx-representation" class="headerlink" title="1. #[repr(xxx)] (representation)"></a>1. #[repr(xxx)] (representation)</h5><p><code>#[repr]</code> 是 Rust 的一个属性，指定一个类型的内存布局（representation）。通过 <code>repr</code> 属性，Rust 允许你控制结构体、枚举、联合体或其他类型在内存中的布局，确保它们与其他语言（例如 C 语言）或者硬件接口的兼容性。</p><p><code>#[repr(C)]</code> 是最常用的 <code>repr</code> 属性之一。它告诉 Rust 使用 C 语言的内存布局规则来布局结构体或枚举</p><h4 id="5-Rust-高阶"><a href="#5-Rust-高阶" class="headerlink" title="5. Rust 高阶"></a>5. Rust 高阶</h4><h5 id="1-Arc-Atomic-Reference-Counting"><a href="#1-Arc-Atomic-Reference-Counting" class="headerlink" title="1. Arc (Atomic Reference Counting)"></a>1. Arc (Atomic Reference Counting)</h5><p><code>Arc</code> 是一个线程安全的引用计数智能指针，允许多个线程共享对同一对象的所有权。当没有任何线程持有 <code>Arc</code> 时，资源会被自动释放。<code>Arc</code> 是为了在多线程环境中共享数据而设计的。</p><h5 id="2-Weak-Weak-Reference"><a href="#2-Weak-Weak-Reference" class="headerlink" title="2. Weak (Weak Reference)"></a>2. Weak (Weak Reference)</h5><p>弱引用，不会增加引用计数</p><p>包含的 <code>lock().upgrade()</code> 方法可以使其升级为一个强引用</p><h5 id="3-Rwlock"><a href="#3-Rwlock" class="headerlink" title="3. Rwlock"></a>3. Rwlock</h5>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
      <category>NPUcore</category>
      
    </categories>
    
    
    <tags>
      
      <tag>OS</tag>
      
      <tag>NPUcore</tag>
      
      <tag>FileSystem</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Makefile小记</title>
    <link href="/2024/11/05/Makefile%20%E5%B0%8F%E8%AE%B0/"/>
    <url>/2024/11/05/Makefile%20%E5%B0%8F%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="Makefile小记"><a href="#Makefile小记" class="headerlink" title="Makefile小记"></a>Makefile小记</h1><h2 id="赋值语句"><a href="#赋值语句" class="headerlink" title="赋值语句"></a>赋值语句</h2><h3 id="1"><a href="#1" class="headerlink" title="1. ="></a>1. <code>=</code></h3><table><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td>&#x3D;</td><td>变量的值是整个makefile中最后被指定的值</td></tr></tbody></table><p><strong>例子</strong></p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs makefile">VIR_A = A<br>VIR_B = <span class="hljs-variable">$(VIR_A)</span> B<br>VIR_A = AA<br></code></pre></td></tr></table></figure><p>最终 <code>VIR_B</code> 的值是”AA B” 而不是 “A B”</p><p>在make时，会把整个makefile展开，然后决定最后变量的值</p><h3 id="2"><a href="#2" class="headerlink" title="2. :="></a>2. <code>:=</code></h3><table><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td>:&#x3D;</td><td>直接赋值</td></tr></tbody></table><p><strong>例子</strong></p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs makefile">VIR_A = A<br>VIR_B = <span class="hljs-variable">$(VIR_A)</span> B<br>VIR_A = AA<br></code></pre></td></tr></table></figure><p>最终 <code>VIR_B</code> 的值是”A B”</p><h3 id="3"><a href="#3" class="headerlink" title="3. ?="></a>3. <code>?=</code></h3><table><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td>?&#x3D;</td><td>如果变量没有被赋值，则赋予等号后面的值，相当于默认值</td></tr></tbody></table><p><strong>例子</strong></p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs makefile"><span class="hljs-comment"># Makefile1</span><br>VIR ?= new_value<br><span class="hljs-comment"># 上面就会等于 new_value</span><br></code></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs makefile"><span class="hljs-comment"># Makefile2</span><br>VIR := old_value<br>VIR ?= new_value<br><span class="hljs-comment"># 上面就会等于 old_value</span><br></code></pre></td></tr></table></figure><h3 id="4"><a href="#4" class="headerlink" title="4. +="></a>4. <code>+=</code></h3><table><thead><tr><th>符号</th><th>含义</th></tr></thead><tbody><tr><td>+&#x3D;</td><td>将等号后面的值添加到前面的变量上，中间会插入空格</td></tr></tbody></table><p><strong>例子</strong></p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs makefile">varA := a<br>varA += 1<br><span class="hljs-section">debug:</span><br>@echo <span class="hljs-variable">$(varA)</span><br><span class="hljs-comment"># 输出</span><br><span class="hljs-comment"># a a</span><br></code></pre></td></tr></table></figure>]]></content>
    
    
    <categories>
      
      <category>编程语言</category>
      
      <category>Makefile</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Makefile</tag>
      
      <tag>编程</tag>
      
      <tag>基础</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>操作系统笔记</title>
    <link href="/2024/11/01/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%AC%94%E8%AE%B0/"/>
    <url>/2024/11/01/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E7%AC%94%E8%AE%B0/</url>
    
    <content type="html"><![CDATA[<h1 id="操作系统笔记"><a href="#操作系统笔记" class="headerlink" title="操作系统笔记"></a>操作系统笔记</h1><h2 id="〇、概述"><a href="#〇、概述" class="headerlink" title="〇、概述"></a>〇、概述</h2><p>2024-11-15</p><p>将现代操作系统简化视为：</p><p><strong>内核</strong> + <strong>内核提供的系统调用集合</strong> + <strong>用户空间</strong></p><h2 id="一、内存管理"><a href="#一、内存管理" class="headerlink" title="一、内存管理"></a>一、内存管理</h2><h4 id="1-1-计算机体系结构-内存层次"><a href="#1-1-计算机体系结构-内存层次" class="headerlink" title="1.1 计算机体系结构&#x2F;内存层次"></a>1.1 计算机体系结构&#x2F;内存层次</h4><p>内存管理方式</p><p>重定位（relocation）</p><p>分段（segmentation）</p><p>分页（paging）</p><p>虚拟存储（virtual memory）</p><h4 id="1-2-地址空间-地址生成"><a href="#1-2-地址空间-地址生成" class="headerlink" title="1.2 地址空间&amp;地址生成"></a>1.2 地址空间&amp;地址生成</h4><h5 id="1-2-1-地址空间的定义"><a href="#1-2-1-地址空间的定义" class="headerlink" title="1.2.1 地址空间的定义"></a>1.2.1 地址空间的定义</h5><p>物理地址空间：起始地址0,直到MAX of sys</p><p>逻辑地址空间：在CPU运行的进程看到的地址，起始地址0,直到MAX of progress（process）</p><h5 id="1-2-2-地址生成"><a href="#1-2-2-地址生成" class="headerlink" title="1.2.2 地址生成"></a>1.2.2 地址生成</h5><p>逻辑地址生成——编译</p><p>经过编译、汇编、链接之后，得到可执行文件，被CPU加载程序到内存上时（重定位），就会得到逻辑地址</p><h5 id="1-2-3-地址检查"><a href="#1-2-3-地址检查" class="headerlink" title="1.2.3 地址检查"></a>1.2.3 地址检查</h5><h4 id="1-3-连续内存分配"><a href="#1-3-连续内存分配" class="headerlink" title="1.3 连续内存分配"></a>1.3 连续内存分配</h4><h4 id="1-4-伙伴系统"><a href="#1-4-伙伴系统" class="headerlink" title="1.4 伙伴系统"></a>1.4 伙伴系统</h4><h2 id="二、进程管理"><a href="#二、进程管理" class="headerlink" title="二、进程管理"></a>二、进程管理</h2><h2 id="三、文件系统"><a href="#三、文件系统" class="headerlink" title="三、文件系统"></a>三、文件系统</h2><p><strong>Inode</strong>：Index Node，索引节点，存储文件的元数据，关键在于文件的描述和索引以及其他元数据，但不存储文件的内容数据。</p><h2 id="四、IO"><a href="#四、IO" class="headerlink" title="四、IO"></a>四、IO</h2><h2 id="五、多处理器"><a href="#五、多处理器" class="headerlink" title="五、多处理器"></a>五、多处理器</h2>]]></content>
    
    
    <categories>
      
      <category>操作系统</category>
      
    </categories>
    
    
    <tags>
      
      <tag>OS</tag>
      
      <tag>基础</tag>
      
      <tag>笔记</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Rust学习笔记</title>
    <link href="/2024/10/09/Rust/"/>
    <url>/2024/10/09/Rust/</url>
    
    <content type="html"><![CDATA[<h1 id="Rust学习笔记"><a href="#Rust学习笔记" class="headerlink" title="Rust学习笔记"></a>Rust学习笔记</h1><h2 id="〇、基础入门"><a href="#〇、基础入门" class="headerlink" title="〇、基础入门"></a>〇、基础入门</h2><h3 id="0-基础语法"><a href="#0-基础语法" class="headerlink" title="0.基础语法"></a>0.基础语法</h3><h4 id="0-1-变量"><a href="#0-1-变量" class="headerlink" title="0.1 变量"></a>0.1 变量</h4><blockquote><p>Rust 语言具有可变变量和不可变变量的概念</p><p>可变变量：Mutable 即在后续过程中可以改变该变量数值字面量</p><p>不可变变量：Immutable 与上述相反</p><p>且 Rust 是强类型语言，具有自动判断变量类型的能力</p><p>为什么要这样做？</p><p>Rust 声称为了高并发安全而做的设计 <del>我现在也不懂，等以后再回来看看</del></p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = <span class="hljs-number">123</span>; <span class="hljs-comment">// 不可变变量</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">b</span> = <span class="hljs-number">123</span>; <span class="hljs-comment">// 可变变量</span><br>a = <span class="hljs-number">12</span>; <span class="hljs-comment">// 此句会报错</span><br>b = <span class="hljs-number">1</span>; <span class="hljs-comment">// 此句不会报错</span><br>b = <span class="hljs-string">&quot;changed&quot;</span>; <span class="hljs-comment">// 此句会报错 因为上面的声明中 b 明显为 int 类型</span><br>b = <span class="hljs-number">12.3</span>; <span class="hljs-comment">// 此句会报错 因为12.3 由 float 转换为 int 类型精度会有损失，而Rust不允许该情况发生（精度由损失的自动数据类型转换的情况）</span><br></code></pre></td></tr></table></figure><p>可以看到，只要在声明关键字 <strong>let</strong> 后面添加 关键字 <strong>mut</strong> 就可以使 <strong>变量</strong> 成为 <strong>可变变量</strong></p><h4 id="0-1-1-变量解构"><a href="#0-1-1-变量解构" class="headerlink" title="0.1.1 变量解构"></a>0.1.1 变量解构</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> (a, <span class="hljs-keyword">mut</span> b): (<span class="hljs-type">bool</span>, <span class="hljs-type">bool</span>) = (<span class="hljs-literal">true</span>, <span class="hljs-literal">true</span>);<br></code></pre></td></tr></table></figure><h4 id="0-2-常量"><a href="#0-2-常量" class="headerlink" title="0.2 常量"></a>0.2 常量</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">const</span> a: <span class="hljs-type">i32</span> = <span class="hljs-number">123</span>;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = <span class="hljs-number">456</span>;<span class="hljs-comment">// 此处不合法，因为a此前为常量</span><br></code></pre></td></tr></table></figure><h4 id="0-3-显示声明"><a href="#0-3-显示声明" class="headerlink" title="0.3 显示声明"></a>0.3 显示声明</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span>: <span class="hljs-type">u64</span> = <span class="hljs-number">123</span>;<span class="hljs-comment">// 此处a将会被判断为 u64 类型</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = <span class="hljs-number">123</span>;<span class="hljs-comment">// 此处a将会被判断为 i32 类型</span><br></code></pre></td></tr></table></figure><h4 id="0-4-注释"><a href="#0-4-注释" class="headerlink" title="0.4 注释"></a>0.4 注释</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 单行注释 MK1</span><br><br><span class="hljs-comment">/* 单行注释 MK2 */</span><br><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">多行注释</span><br><span class="hljs-comment">*/</span><br><br><span class="hljs-comment">/// 说明文档的注释</span><br><span class="hljs-comment">/// 用的是 markdown 语法</span><br></code></pre></td></tr></table></figure><hr><h3 id="1-数据类型"><a href="#1-数据类型" class="headerlink" title="1.数据类型"></a>1.数据类型</h3><h4 id="1-1-基本类型"><a href="#1-1-基本类型" class="headerlink" title="1.1 基本类型"></a>1.1 基本类型</h4><table><thead><tr><th>类型关键字</th><th>类型含义</th><th>类型范围</th><th>类型例子</th></tr></thead><tbody><tr><td>i8</td><td>8位integer</td><td></td><td></td></tr><tr><td>i16</td><td>16位integer</td><td></td><td></td></tr><tr><td>i32</td><td>32位integer</td><td></td><td>12</td></tr><tr><td>i64</td><td>64位integer</td><td></td><td>-23</td></tr><tr><td>i128</td><td>128位integer</td><td></td><td>-1231231</td></tr><tr><td>isize</td><td>与CPU位数相同</td><td></td><td></td></tr><tr><td>u8</td><td>8位unsigned</td><td></td><td>12</td></tr><tr><td>u16</td><td>16位unsigned</td><td></td><td>32</td></tr><tr><td>u32</td><td>32位unsigned</td><td></td><td>3213</td></tr><tr><td>u64</td><td>64位unsigned</td><td></td><td>3213131</td></tr><tr><td>u128</td><td>128位unsigned</td><td></td><td></td></tr><tr><td>usize</td><td>与CPU位数相同</td><td></td><td></td></tr><tr><td>f32</td><td>32位float</td><td></td><td></td></tr><tr><td>f64</td><td>64位float（默认使用这种）</td><td></td><td></td></tr><tr><td>bool</td><td>布尔类型</td><td></td><td>true</td></tr><tr><td>char</td><td>字符</td><td></td><td>‘R’</td></tr></tbody></table><p>注意char 使用单引号</p><p>String类型使用双引号</p><h4 id="1-2-复合类型"><a href="#1-2-复合类型" class="headerlink" title="1.2 复合类型"></a>1.2 复合类型</h4><blockquote><p>可以使用编译器属性标记 “#[allow(unused_variables)]” 来让编译器忽略未使用的变量</p></blockquote><h5 id="1-2-1-元组"><a href="#1-2-1-元组" class="headerlink" title="1.2.1 元组"></a>1.2.1 元组</h5><blockquote><p>元组可以包含不同类型数据</p></blockquote><p>例子：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">tup</span>: (<span class="hljs-type">i32</span>, <span class="hljs-type">u8</span>, <span class="hljs-type">f32</span>) = (-<span class="hljs-number">100</span>, <span class="hljs-number">100</span>, <span class="hljs-number">1.0</span>);<br><span class="hljs-comment">// tup.0 = -100</span><br><span class="hljs-comment">// tup.1 = 100</span><br><span class="hljs-comment">// tup.2 = 1.0</span><br><span class="hljs-keyword">let</span> (x, y, z) = tup;<br><span class="hljs-comment">// x = -100</span><br><span class="hljs-comment">// y = 100</span><br><span class="hljs-comment">// z = 1.0</span><br></code></pre></td></tr></table></figure><h5 id="1-2-2-数组"><a href="#1-2-2-数组" class="headerlink" title="1.2.2 数组"></a>1.2.2 数组</h5><blockquote><p>数组只能包含同类型数据</p></blockquote><p>例子：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">b</span> = [<span class="hljs-string">&quot;apple&quot;</span>, <span class="hljs-string">&quot;banana&quot;</span>, <span class="hljs-string">&quot;orange&quot;</span>];<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">c</span>: [<span class="hljs-type">i32</span>; <span class="hljs-number">5</span>] = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><span class="hljs-comment">// 5 表示长度，i32 表示类型</span><br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">d</span> = [<span class="hljs-number">3</span>; <span class="hljs-number">5</span>];<br><span class="hljs-comment">// 即 let d = [3, 3, 3, 3, 3];</span><br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">first</span> = a[<span class="hljs-number">0</span>];<br><span class="hljs-keyword">let</span> <span class="hljs-variable">second</span> = a[<span class="hljs-number">1</span>];<br><span class="hljs-comment">// 数组的访问</span><br><br>a[<span class="hljs-number">0</span>] = <span class="hljs-number">123</span>; <span class="hljs-comment">// 数组不可变，所以报错</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">a</span> = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br>a[<span class="hljs-number">0</span>] = <span class="hljs-number">4</span>; <span class="hljs-comment">// 此处不会报错</span><br></code></pre></td></tr></table></figure><h5 id="1-2-3-字符串"><a href="#1-2-3-字符串" class="headerlink" title="1.2.3 字符串"></a>1.2.3 字符串</h5><p>分为静态字符串 &amp;str 和 动态字符串 String</p><p>其中 &amp;str 不可变， String 可变</p><p><strong>&amp;str 的大小固定为（因为相当于是一个指针或者说引用）2个字长（即2个CPU位数），在64位CPU上即为16B，包含一个指针字段（包含地址）和字符串长度字段（包含该字符串长度）的数据</strong></p><p><strong>切片（Slice）</strong></p><p>对于字符串而言，切片就是对String类型的某一部分的引用</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Hello world&quot;</span>);<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">hello</span> = &amp;s[<span class="hljs-number">0</span>..<span class="hljs-number">5</span>];<br><span class="hljs-keyword">let</span> <span class="hljs-variable">world</span> = &amp;s[<span class="hljs-number">6</span>..<span class="hljs-number">11</span>];<br></code></pre></td></tr></table></figure><p>hello 没有引用整个 String s，而是引用了 s 的一部分内容，通过[0..5] 来指定</p><p>创建切片的语法，使用方括号包括的一个序列：[开始索引..终止索引]，其中开始索引是切片中第一个元素的索引位置，而终止索引是最后一个元素后一个的索引位置。长度为 终止索引 - 开始索引。</p><p>在rust中</p><p>[0..2] 与 [..2] 等效</p><p>同样的，假设长度为len，</p><p>[3..len] 与 [3..] 等效</p><p>所以完整切片</p><p>[0..len] 与 [..] 等效</p><blockquote><p>字符串切片的类型标识是&amp;str</p></blockquote><blockquote><p>除了字符串之外，其他集合类型也有，如数组</p></blockquote><blockquote><p>&amp;str 是一个不可变引用！！！</p></blockquote><p><strong>String 与 &amp;str切换</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;xxx&quot;</span>);<br><span class="hljs-string">&quot;xxx&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>();<br><br><span class="hljs-comment">// String取引用即是&amp;str类型</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;xxx&quot;</span>);<br><span class="hljs-comment">// &amp;s;</span><br><span class="hljs-comment">// &amp;s[..];</span><br><span class="hljs-comment">// s.as_str();</span><br></code></pre></td></tr></table></figure><p><strong>字符串操作</strong></p><ul><li>追加（ Push）</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Hello &quot;</span>);<br>  <br>    s.<span class="hljs-title function_ invoke__">push_str</span>(<span class="hljs-string">&quot;rust&quot;</span>);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;追加字符串 push_str() -&gt; &#123;&#125;&quot;</span>, s);<br>  <br>    s.<span class="hljs-title function_ invoke__">push</span>(<span class="hljs-string">&quot;!&quot;</span>);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;追加字符 push() -&gt; &#123;&#125;&quot;</span>, s);<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><p>插入（Insert）</p></li><li><p>替换（Replace） 返回一个新的字符串（三种操作方式）</p></li><li><p>删除（Delete）pop() 删除并返回最后一个字符，remove() 删除并返回字符串中指定位置的字符</p><p>  truncate() 删除字符串中从指定位置开始到结尾的全部字符，无返回值</p><p>  clear() 清空字符串</p></li><li><p>连接（Concatenate）返回一个新的字符串</p></li><li><p>可使用 format! 来连接字符串，功能与c语言下的fprintf类似</p></li></ul><p><strong>字符串转义</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// \ 字符功能与其他语言类似</span><br></code></pre></td></tr></table></figure><p>使用如下方法可以直接保持字符串原样：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">raw_str</span> = <span class="hljs-string">r&quot;hahah\dw\a\d\213\fd\as \gb\fd\u&#123;211D&#125;&quot;</span>;<br><br><span class="hljs-comment">// 若字符串包含双引号，则可以在开头和结尾添加 #</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">quotes_raw</span> = <span class="hljs-string">r#&quot;djiasoidjaosi&quot;dqdwjihui&quot;&quot;#</span>;<br><br><span class="hljs-comment">// 若字符串包含#号，则可以在开头和结尾多加个#号，最多255个，只需要保证与字符串中连续#号个数不超过开头和结尾的#号个数</span><br></code></pre></td></tr></table></figure><p><strong>操作UTF-8字符串</strong></p><ul><li>使用字符方式遍历字符串</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">for</span> <span class="hljs-variable">c</span> <span class="hljs-keyword">in</span> <span class="hljs-string">&quot;中国人&quot;</span>.<span class="hljs-title function_ invoke__">chars</span>() &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, c);<br>&#125;<br><br><span class="hljs-comment">// output</span><br>中<br>国<br>人<br></code></pre></td></tr></table></figure><ul><li>使用字节方式</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">for</span> <span class="hljs-variable">b</span> <span class="hljs-keyword">in</span> <span class="hljs-string">&quot;中国人&quot;</span>.<span class="hljs-title function_ invoke__">bytes</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, b);<br>&#125;<br><br><span class="hljs-comment">// output 将会返回字符串的底层字节数组表现形式</span><br><span class="hljs-number">228</span><br><span class="hljs-number">184</span><br><span class="hljs-number">173</span><br><span class="hljs-number">229</span><br><span class="hljs-number">155</span><br><span class="hljs-number">189</span><br><span class="hljs-number">228</span><br><span class="hljs-number">186</span><br><span class="hljs-number">186</span><br></code></pre></td></tr></table></figure><ul><li>获取子串</li></ul><p>std下的方法做不到，可以使用 utf8_slice crate</p><h4 id="1-3-运算"><a href="#1-3-运算" class="headerlink" title="1.3 运算"></a>1.3 运算</h4><h5 id="1-3-1-基本运算"><a href="#1-3-1-基本运算" class="headerlink" title="1.3.1 基本运算"></a>1.3.1 基本运算</h5><p>无非加减乘除模</p><h5 id="1-3-2-位运算"><a href="#1-3-2-位运算" class="headerlink" title="1.3.2 位运算"></a>1.3.2 位运算</h5><table><thead><tr><th>运算符</th><th>含义</th></tr></thead><tbody><tr><td>&amp;</td><td>位与</td></tr><tr><td>|</td><td>位或</td></tr><tr><td>^</td><td>位异或</td></tr><tr><td>!</td><td>非</td></tr><tr><td>&lt;&lt;</td><td>左移指定位数，右位补0</td></tr><tr><td>&gt;&gt;</td><td>右移指定位数，带符号移动（正数补0，负数补1）</td></tr></tbody></table><h4 id="1-4-序列（Range）"><a href="#1-4-序列（Range）" class="headerlink" title="1.4 序列（Range）"></a>1.4 序列（Range）</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">for</span> <span class="hljs-variable">i</span> <span class="hljs-keyword">in</span> <span class="hljs-number">1</span>..=<span class="hljs-number">5</span> &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, i);<br>&#125;<br>Output:<br><span class="hljs-number">1</span><br><span class="hljs-number">2</span><br><span class="hljs-number">3</span><br><span class="hljs-number">4</span><br><span class="hljs-number">5</span><br><br><span class="hljs-keyword">for</span> <span class="hljs-variable">i</span> <span class="hljs-keyword">in</span> <span class="hljs-number">1</span>..<span class="hljs-number">5</span> &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, i);<br>&#125;<br>Output:<br><span class="hljs-number">1</span><br><span class="hljs-number">2</span><br><span class="hljs-number">3</span><br><span class="hljs-number">4</span><br></code></pre></td></tr></table></figure><blockquote><p>序列只允许数字或者字符类型</p></blockquote><h4 id="1-5-类型转换"><a href="#1-5-类型转换" class="headerlink" title="1.5 类型转换"></a>1.5 类型转换</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust">value1 <span class="hljs-keyword">as</span> type2<br></code></pre></td></tr></table></figure><h4 id="1-6-复数"><a href="#1-6-复数" class="headerlink" title="1.6 复数"></a>1.6 复数</h4><p>使用num库</p><p>例子：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs toml"><span class="hljs-comment"># Cargo.toml</span><br><span class="hljs-comment"># 添加以下内容</span><br><span class="hljs-section">[dependencies]</span><br><span class="hljs-attr">num</span> = <span class="hljs-string">&quot;0.4.0&quot;</span><br><br><br><span class="hljs-comment"># main.rs</span><br>use num::complex::Complex<br></code></pre></td></tr></table></figure><h4 id="1-7-单元类型"><a href="#1-7-单元类型" class="headerlink" title="1.7 单元类型"></a>1.7 单元类型</h4><p>() 即是一个单元类型，大小为0</p><p>非发散函数(diverge function 为发散函数)再没有显示返回一个表达式的情况下，会返回一个单元类型</p><p>控制块也是！如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">5</span>;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">z</span> = &#123;<br>    <span class="hljs-number">2</span>*x;<br>&#125;<br><span class="hljs-comment">// 则此时z = ()，是一个单元类型</span><br></code></pre></td></tr></table></figure><h4 id="1-8-发散函数"><a href="#1-8-发散函数" class="headerlink" title="1.8 发散函数"></a>1.8 发散函数</h4><p>用 ! 作函数返回类型的时候，表示该函数永不返回（ diverge function ），特别的，这种语法往往用做会导致程序崩溃的函数：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">dead_end</span>() <span class="hljs-punctuation">-&gt;</span> ! &#123;<br>    <span class="hljs-built_in">panic!</span>(<span class="hljs-string">&quot;Panic!!!&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>下面函数创建了一个无限循环，该循环永不跳出，因此函数也永不返回：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">forever</span>() <span class="hljs-punctuation">-&gt;</span> !&#123;<br>    <span class="hljs-keyword">loop</span> &#123;<br>        <span class="hljs-comment">//...</span><br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以使用如下四种方式实现发散函数（即永远不返回）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">panic!</span>();<br>todo!(); <span class="hljs-comment">// not yet implemented 将导致panic</span><br><span class="hljs-built_in">unimplemented!</span>(); <span class="hljs-comment">// not implemented 将导致panic</span><br><span class="hljs-keyword">loop</span>&#123;&#125;;<br></code></pre></td></tr></table></figure><hr><h3 id="2-函数"><a href="#2-函数" class="headerlink" title="2.函数"></a>2.函数</h3><p>基本形式：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> &lt;函数名&gt; ( [参数列表] ) [<span class="hljs-punctuation">-&gt;</span> &lt;返回值&gt;] &#123;&lt;函数体&gt;&#125;<br></code></pre></td></tr></table></figure><p>更为具体的例子：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">function_name</span> (arg_1: type1, arg_2: type2, ... arg_n: typen) <span class="hljs-punctuation">-&gt;</span> ret_type &#123;<br>    function body<br>&#125;<br></code></pre></td></tr></table></figure><p>其中 “**-&gt; ret_type<strong>“ 在没有返回值的情况下可以省略，此时默认返回类型为 <strong>()</strong> ，即</strong>空元组**</p><blockquote><p>语句：执行某些操作且没有返回值的步骤。如：</p><p>let a &#x3D; 1;</p><p>表达式：有计算步骤且有返回值。如：</p><p>a &#x3D; 1</p><p>b + 1</p><p>a + b * c</p></blockquote><h4 id="函数体表达式"><a href="#函数体表达式" class="headerlink" title="函数体表达式"></a>函数体表达式</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust">&#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = <span class="hljs-number">1</span>;<br>    a + <span class="hljs-number">1</span><br>&#125;;<br><span class="hljs-comment">// 该表达式块最后一个步骤是表达式，该表达式结果值即为整个表达式块所代表的值，这种表达式块叫做函数体表达式</span><br></code></pre></td></tr></table></figure><h4 id="函数返回值"><a href="#函数返回值" class="headerlink" title="函数返回值"></a>函数返回值</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>(a: <span class="hljs-type">i32</span>, b: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>    <span class="hljs-keyword">return</span> a + b;<br>&#125;<span class="hljs-comment">// 如果没有明确声明返回值类型，不可以在 return 后面添加表达式！</span><br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">add1</span>(a: <span class="hljs-type">i32</span>, b: <span class="hljs-type">i32</span>)<span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>    a + b;<br>&#125;<span class="hljs-comment">// 与上面一样</span><br></code></pre></td></tr></table></figure><hr><h3 id="3-流程控制"><a href="#3-流程控制" class="headerlink" title="3.流程控制"></a>3.流程控制</h3><h4 id="3-1-if-else"><a href="#3-1-if-else" class="headerlink" title="3.1 if-else"></a>3.1 if-else</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">n</span> = <span class="hljs-number">3</span>;<br>    <span class="hljs-keyword">if</span> n &lt; <span class="hljs-number">5</span> &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;True&quot;</span>);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;False&quot;</span>);<br>    &#125;<br>  <br>    <span class="hljs-keyword">if</span> n == <span class="hljs-number">3</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;n == 3&quot;</span>);<br>    &#125;<br>  <br>    <span class="hljs-keyword">if</span> n == <span class="hljs-number">2</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;n == 2&quot;</span>);<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> n == <span class="hljs-number">1</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;n == 1&quot;</span>);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;n != 2 &amp;&amp; n != 1&quot;</span>);<br>    &#125;<br>&#125;<br><span class="hljs-comment">// 其中 if 后面的以及 else if 后面的表达式必须是bool类型，不像部分其他的编程语言有非0即真的特性</span><br></code></pre></td></tr></table></figure><h5 id="3-1-1-三目运算符的效果"><a href="#3-1-1-三目运算符的效果" class="headerlink" title="3.1.1 三目运算符的效果"></a>3.1.1 三目运算符的效果</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">number</span> = <span class="hljs-keyword">if</span> a &gt; <span class="hljs-number">0</span> &#123;<span class="hljs-number">1</span>&#125; <span class="hljs-keyword">else</span> &#123; -<span class="hljs-number">1</span> &#125;;<br></code></pre></td></tr></table></figure><h4 id="3-2-loop"><a href="#3-2-loop" class="headerlink" title="3.2 loop"></a>3.2 loop</h4><blockquote><p>当做没有条件判断的 <strong>while</strong> 来使用</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 如果没有break,那么就是个死循环</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">count</span> = <span class="hljs-number">0</span>;<br><span class="hljs-keyword">loop</span> &#123;<br>        count += <span class="hljs-number">1</span>;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello World! &#123;&#125; times!&quot;</span>, count);<br>        <span class="hljs-keyword">if</span> count &gt;= <span class="hljs-number">100</span> &#123;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="3-3-while"><a href="#3-3-while" class="headerlink" title="3.3 while"></a>3.3 while</h4><blockquote><p>可以将 <strong>while</strong> 视为带有条件判断的 <strong>loop</strong></p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>()&#123;<br>     <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">number</span> = <span class="hljs-number">1</span>;<br>     <span class="hljs-keyword">while</span> number &lt; <span class="hljs-number">4</span> &#123;<br>         <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, number);<br>         number += <span class="hljs-number">1</span>;<br>     &#125;<br>     <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;EXIT&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>rust 将do 设置为保留关键字，也就是将来还会用到</p></blockquote><h4 id="3-4-for"><a href="#3-4-for" class="headerlink" title="3.4 for"></a>3.4 for</h4><blockquote><p>for 会创建一个迭代器</p><p>其中已经实现copy特征的数据类型可以省略掉iter()方法，因为不需要引用</p><p>而未实现的数据类型需要使用iter()方法，这样会创建一个引用，不会转移所有权</p><p>iter()的enumerate()方法会返回一个元组，构成为(i, v)，i为索引，v为数值</p></blockquote><hr><h3 id="4-其他复合类型"><a href="#4-其他复合类型" class="headerlink" title="4.其他复合类型"></a>4.其他复合类型</h3><h4 id="4-1-结构体"><a href="#4-1-结构体" class="headerlink" title="4.1 结构体"></a>4.1 结构体</h4><h5 id="⚠TODO！"><a href="#⚠TODO！" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h5><h4 id="4-2-切片"><a href="#4-2-切片" class="headerlink" title="4.2 切片"></a>4.2 切片</h4><h5 id="⚠TODO！-1"><a href="#⚠TODO！-1" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h5><h4 id="4-3-枚举"><a href="#4-3-枚举" class="headerlink" title="4.3 枚举"></a>4.3 枚举</h4><h5 id="⚠TODO！-2"><a href="#⚠TODO！-2" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h5><hr><h3 id="5-所有权"><a href="#5-所有权" class="headerlink" title="5.所有权"></a>5.所有权</h3><blockquote><p>计算机语言不断演变过程中，出现了三种流派：</p><ul><li>垃圾回收机制 GC： 在程序运行时不断寻找不再使用的内存，典型：Java、Go</li><li>手动管理内存的分配和释放：在程序中通过函数调用方式来申请和释放内存，典型：C++</li><li>通过所有权来管理内存：编译器在编译时会根据一系列规则进行检查</li></ul><p>Rust选择了第三种，这种检查只会在编译期出现，对于程序运行期不会有任何性能上的损失</p></blockquote><p><strong>所有权原则</strong>：</p><ul><li>Rust中每一个值都被一个变量所拥有，该变量被称为值的所有者</li><li>一个值同时只能被一个变量所拥有，或者说一个值只能拥有一个所有者</li><li>当所有者（变量）离开作用域范围时，这个值将被丢弃（drop）</li></ul><blockquote><p>当拷贝过于简单时（固定大小的简单值），不会发生所有权转移（发生的是<strong>自动拷贝</strong>），比如：</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">a</span> = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">b</span> = a;<br><span class="hljs-comment">// b 和 a 在不同内存上，没有发生所有权转移</span><br></code></pre></td></tr></table></figure><h4 id="5-1-Move-移动（转移所有权）"><a href="#5-1-Move-移动（转移所有权）" class="headerlink" title="5.1 Move 移动（转移所有权）"></a>5.1 Move 移动（转移所有权）</h4><p>对于基本类型（存储在栈上的），Rust 会自动拷贝，但是String不是基本类型，存储在堆上，所以不能自动拷贝</p><blockquote><p>String 由存储在栈上的<strong>堆指针</strong>、<strong>字符串长度</strong>、<strong>字符串容量</strong>共同组成</p><p>String 类型指向了一个堆上的空间</p></blockquote><p><strong>二次释放</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">s1</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<br><span class="hljs-keyword">let</span> <span class="hljs-variable">s2</span> = s1;<br></code></pre></td></tr></table></figure><p>此处若是深拷贝，由于是在堆上的数据，全部拷贝会对性能产生非常大的影响（当数据量大时）</p><p>若是浅拷贝，即只拷贝String本身，则这个值出现了两个所有者，而 Rust 的机制，当变量离开作用域时，会自动调用drop函数并清理变量的堆内存，不过由于两个String变量指向了同一位置。那么当s1 和s2 离开作用域时，都会尝试释放相同的内存。即<strong>二次释放（double free）</strong>错误。属于内存安全性bug之一，两次释放相同内存会导致内存污染，可能会导致潜在的安全漏洞。</p><p>因此，Rust 会在s1赋予给s2之后，认为s1不再有效，无需再在s1离开作用域时drop任何东西，即把所有权从s1转移给了s2。</p><p>其他语言中有术语<strong>浅拷贝（shallow copy）</strong>和<strong>深拷贝（deep copy）</strong>，拷贝指针、长度和容量而不拷贝数据就像是浅拷贝，但是又因为Rust同时使第一个变量s1无效了，因此这个操作称之为<strong>移动（move）</strong>，而不是浅拷贝，可以理解为s1被移动到s2.</p><h4 id="5-2-Clone-克隆（深拷贝）"><a href="#5-2-Clone-克隆（深拷贝）" class="headerlink" title="5.2 Clone 克隆（深拷贝）"></a>5.2 Clone 克隆（深拷贝）</h4><p>Rust 永远也不会自动创建数据的”深拷贝“。因此任何自动的复制都不是深拷贝，可以被认为对运行时性能影响较小。</p><p>如果确实需要深拷贝，可以使用clone方法</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">s1</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<br><span class="hljs-keyword">let</span> <span class="hljs-variable">s2</span> = s1.<span class="hljs-title function_ invoke__">clone</span>();<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;s1 = &#123;&#125;, s2 = &#123;&#125;&quot;</span>, s1, s2);<br></code></pre></td></tr></table></figure><h4 id="5-3-Copy-复制（浅拷贝）"><a href="#5-3-Copy-复制（浅拷贝）" class="headerlink" title="5.3 Copy 复制（浅拷贝）"></a>5.3 Copy 复制（浅拷贝）</h4><p>浅拷贝只会发生在栈上，因此性能很高</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">5</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = x;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;x = &#123;&#125;, y = &#123;&#125;&quot;</span>, x, y);<br></code></pre></td></tr></table></figure><p>像整型这样的基本类型在编译时是已知大小的，会被存储在栈上，所以拷贝其实际的值是快速的。（可以理解成此处发生了在栈上做了深拷贝）</p><p><strong>任何基本类型的组合可以copy，不需要分配内存或者某种形式资源的类型是可以copy的。</strong>如：</p><ul><li>所有整数类型</li><li>布尔类型</li><li>所有浮点数类型</li><li>字符类型</li><li>元组</li><li>不可变引用 &amp;T （注意 可变引用 &amp;mut T 是不可以copy的）</li></ul><h4 id="5-4-函数传值与返回"><a href="#5-4-函数传值与返回" class="headerlink" title="5.4 函数传值与返回"></a>5.4 函数传值与返回</h4><p>将值传递给函数，一样会发生移动或者复制，就跟let语句一样，如下展示所有权、作用域的规则：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<span class="hljs-comment">// s 进入作用域</span><br>  <br>    <span class="hljs-title function_ invoke__">takes_ownership</span>(s);<span class="hljs-comment">// s 的值移动到函数内</span><br>    <span class="hljs-comment">// 故 s在此之后不再有效</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">5</span>;<span class="hljs-comment">// x 进入作用域</span><br>  <br>    <span class="hljs-title function_ invoke__">makes_copy</span>(x);<span class="hljs-comment">// x应该移动到函数里，但是i32是可copy的，所以在后面可以继续用x</span><br>  <br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, s);<span class="hljs-comment">// 该句会报错</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, x);<span class="hljs-comment">// 该句不会报错</span><br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">takes_ownership</span>(some_string: <span class="hljs-type">String</span>) &#123;<span class="hljs-comment">// some_string 进入作用域</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, some_string);<br>&#125;<span class="hljs-comment">// some_string 移出作用域并调用`drop`方法，占用的内存释放</span><br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">makes_copy</span>(some_integer: <span class="hljs-type">i32</span>) &#123;<span class="hljs-comment">// some_integer 进入作用域</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, some_integer);<br>&#125;<span class="hljs-comment">// some_integer移出作用域，不会有特殊操作</span><br></code></pre></td></tr></table></figure><p>同样，函数返回值也有所有权</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">s1</span> = <span class="hljs-title function_ invoke__">gives_ownership</span>();<span class="hljs-comment">// gives_ownership 将返回值</span><br>    <span class="hljs-comment">// 移给s1</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s2</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<span class="hljs-comment">// s2 进入作用域</span><br>  <br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s3</span> = <span class="hljs-title function_ invoke__">takes_and_gives_back</span>(s2);<span class="hljs-comment">// s2 被移动到takes_and_gives_back 中</span><br>    <span class="hljs-comment">// 它也将返回值移给s3</span><br>&#125;<span class="hljs-comment">// 此处，s3移出作用域被丢弃，s1移出作用域被丢弃，s2已被移动不会发生丢弃。</span><br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">gives_ownership</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<span class="hljs-comment">// gives_ownership 将返回值移动给</span><br>    <span class="hljs-comment">// 调用它的函数</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">some_string</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<span class="hljs-comment">// some_string 进入作用域</span><br>    some_string<span class="hljs-comment">// 返回some_string 并移出给调用的函数</span><br>&#125;<br><br><span class="hljs-comment">// takes_and_gives_back 将传入字符串并返回该值</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">takes_and_gives_back</span>(a_string:<span class="hljs-type">String</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span>&#123;<span class="hljs-comment">// a_string 进入作用域</span><br>    a_string<span class="hljs-comment">// 返回 a_string 并移出给调用的函数</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="5-5-引用（借用）-Borrowing"><a href="#5-5-引用（借用）-Borrowing" class="headerlink" title="5.5 引用（借用） Borrowing"></a>5.5 引用（借用） Borrowing</h4><p>Rust 只允许同时存在一个可变引用或多个不可变引用</p><p>如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Foo</span> &#123;<br>    x: <span class="hljs-type">i32</span><br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">do_something</span>(a: &amp;Foo) <span class="hljs-punctuation">-&gt;</span> &amp;<span class="hljs-type">i32</span> &#123;<br>    <span class="hljs-keyword">return</span> &amp;a.x;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">foo</span> = Foo &#123; x: <span class="hljs-number">42</span> &#125;;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">x</span>= &amp;<span class="hljs-keyword">mut</span> foo.x;<br>    <span class="hljs-comment">// foo borrowed as mutable</span><br>    *x = <span class="hljs-number">13</span>;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, x);<br>    <span class="hljs-comment">// 上面这一行加上去不会导致报错，因为在这里过后</span><br>    <span class="hljs-comment">// x就被释放了</span><br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = <span class="hljs-title function_ invoke__">do_something</span>(&amp;foo);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, y);<br>    <span class="hljs-comment">// println!(&quot;&#123;&#125;&quot;, x); // 此处被注释</span><br>    <span class="hljs-comment">// 如果没有被注释，那么x 未被释放，foo仍存在一个可变引用</span><br>    <span class="hljs-comment">// 因此不能再出现上面的不可变引用，所以会导致do something函数报错</span><br>&#125;<br></code></pre></td></tr></table></figure><h5 id="5-5-1-引用与解引用"><a href="#5-5-1-引用与解引用" class="headerlink" title="5.5.1 引用与解引用"></a>5.5.1 引用与解引用</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">5</span>;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = &amp;x;<span class="hljs-comment">// 引用</span><br>  <br>    <span class="hljs-built_in">assert_eq!</span>(<span class="hljs-number">5</span>, x);<br>    <span class="hljs-built_in">assert_eq!</span>(<span class="hljs-number">5</span>, *y);<span class="hljs-comment">// 解引用</span><br>&#125;<br></code></pre></td></tr></table></figure><p>可以通过引用的方式来解决释放问题，如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">do_something</span>(f: &amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">i32</span>)&#123;<br>    <span class="hljs-comment">// 此处需要先解引用</span><br>    *f += <span class="hljs-number">1</span>;<br>    <span class="hljs-comment">// 此步过后f被释放</span><br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>()&#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">foo</span> = <span class="hljs-number">2i32</span>;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, foo);<br>    <span class="hljs-title function_ invoke__">do_something</span>(&amp;<span class="hljs-keyword">mut</span> foo);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, foo);<br>&#125;<br></code></pre></td></tr></table></figure><p>输出为</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">2<br>3<br></code></pre></td></tr></table></figure><h5 id="5-5-2-不可变引用"><a href="#5-5-2-不可变引用" class="headerlink" title="5.5.2 不可变引用"></a>5.5.2 不可变引用</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s1</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<br>  <br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">len</span> = <span class="hljs-title function_ invoke__">calculate_length</span>(&amp;s1);<br>  <br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The length of &#x27;&#123;&#125;&#x27; is &#123;&#125;.&quot;</span>, s1, len);<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">calculate_length</span>(s: &amp;<span class="hljs-type">String</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br>    s.<span class="hljs-title function_ invoke__">len</span>()<br>&#125;<br></code></pre></td></tr></table></figure><p>此处 &amp;符号即是引用，允许使用值而不是所有权。</p><p><strong>但是无法通过这个不可变引用来修改引用指向的值！</strong></p><h5 id="5-5-3-可变引用"><a href="#5-5-3-可变引用" class="headerlink" title="5.5.3 可变引用"></a>5.5.3 可变引用</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<br>  <br>    <span class="hljs-title function_ invoke__">change</span>(&amp;<span class="hljs-keyword">mut</span> s);<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">change</span>(some_string: &amp;<span class="hljs-keyword">mut</span> <span class="hljs-type">String</span>) &#123;<br>some_string.<span class="hljs-title function_ invoke__">push_str</span>(<span class="hljs-string">&quot;, world&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>可变引用同时只能存在一个！！！</p></blockquote><blockquote><p>可变引用与不可变引用不能同时存在！！!</p></blockquote><p><strong>NLL</strong></p><p>Non-Lexical-Lifetimes(NLL)</p><p>Ruts的编译器优化行为，专门用于找到某个引用在作用域（}）结束前就不再被使用的代码位置</p><h5 id="5-5-4-悬垂引用-Dangling-References"><a href="#5-5-4-悬垂引用-Dangling-References" class="headerlink" title="5.5.4 悬垂引用 Dangling References"></a>5.5.4 悬垂引用 Dangling References</h5><p>也叫做悬垂指针</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">reference_to_nothing</span> = <span class="hljs-title function_ invoke__">dangle</span>();<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">dangle</span>() <span class="hljs-punctuation">-&gt;</span> &amp;<span class="hljs-type">String</span> &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>);<br>  <br>    &amp;s<br>&#125; <span class="hljs-comment">// s在这里被丢弃，&amp;s将不指向任何值</span><br></code></pre></td></tr></table></figure><p>上述代码会报错（返回了一个悬垂指针）</p><p>将返回值类型改为String, &amp;s改为s可以解决</p><hr><h3 id="6-模式匹配"><a href="#6-模式匹配" class="headerlink" title="6.模式匹配"></a>6.模式匹配</h3><h4 id="6-1-match"><a href="#6-1-match" class="headerlink" title="6.1 match"></a>6.1 match</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">5</span>;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = <span class="hljs-keyword">match</span> x&#123;<br>        <span class="hljs-number">0</span>..<span class="hljs-number">2</span> =&gt; <span class="hljs-number">5</span>;<br>        <span class="hljs-number">3</span> =&gt; <span class="hljs-number">7</span>;<br>        <span class="hljs-number">4</span>|<span class="hljs-number">8</span>|<span class="hljs-number">9</span> =&gt; <span class="hljs-number">9</span>;<br>        _ =&gt; <span class="hljs-number">10</span>;<br>    &#125;<br>&#125; <br></code></pre></td></tr></table></figure><h4 id="6-2-matches-宏"><a href="#6-2-matches-宏" class="headerlink" title="6.2 matches! 宏"></a>6.2 matches! 宏</h4><h5 id="⚠TODO！-3"><a href="#⚠TODO！-3" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h5><h4 id="6-3-if-let"><a href="#6-3-if-let" class="headerlink" title="6.3 if let"></a>6.3 if let</h4><blockquote><p>当模式匹配仅要匹配一个模式其他模式不管的时候使用 if let</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-title function_ invoke__">Some</span>(<span class="hljs-number">5</span>);<br>    <span class="hljs-comment">// if let 后面可以理解为一个判断真值的表达式</span><br>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">let</span> <span class="hljs-variable">Some</span>(<span class="hljs-number">5</span>) = x&#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;x == Some(5)&quot;</span>);<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="6-4-匹配守卫"><a href="#6-4-匹配守卫" class="headerlink" title="6.4 匹配守卫"></a>6.4 匹配守卫</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 形式如下</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">8</span>;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">match</span> x &#123;<br>        <span class="hljs-number">1</span>..<span class="hljs-number">9</span> <span class="hljs-keyword">if</span> y =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;yes&quot;</span>),<br>        _ =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;no&quot;</span>),<br>    &#125;<br>&#125;<br><span class="hljs-comment">// [if ... ]部分就是匹配守卫</span><br></code></pre></td></tr></table></figure><h4 id="6-2-绑定"><a href="#6-2-绑定" class="headerlink" title="6.2 @绑定"></a>6.2 @绑定</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Message</span> &#123;<br>    Hello &#123; id: <span class="hljs-type">i32</span> &#125;,<br>&#125;<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">msg</span> = Message::Hello &#123; id: <span class="hljs-number">5</span> &#125;;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">match</span> msg &#123;<br>    Message::Hello &#123; id: id_variable @ <span class="hljs-number">3</span>..=<span class="hljs-number">7</span> &#125; =&gt; &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Found an id in range: &#123;&#125;&quot;</span>, id_variable)<br>    &#125;, <span class="hljs-comment">// 此处id_variable 可以绑定 id 中的值</span><br>    Message::Hello &#123; id: <span class="hljs-number">10</span>..=<span class="hljs-number">12</span> &#125; =&gt; &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Found an id in another range&quot;</span>)<br>    &#125;, <span class="hljs-comment">// 此处无法使用id中的值</span><br>    Message::Hello &#123; id &#125; =&gt; &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Found some other id: &#123;&#125;&quot;</span>, id)<br>    &#125;,<br>&#125;<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="6-2-1-其他注意事项"><a href="#6-2-1-其他注意事项" class="headerlink" title="6.2.1 @其他注意事项"></a>6.2.1 @其他注意事项</h5><blockquote><p>在使用@绑定包含 <strong>“|”</strong> 的情况时，需要将后面的匹配的模式加上括号，否则会当作只匹配第一个</p><p>如下第12行</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Message</span> &#123;<br>    Hello &#123; id: <span class="hljs-type">i32</span> &#125;,<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">msg</span> = Message::Hello &#123; id: <span class="hljs-number">5</span> &#125;;<br><br>    <span class="hljs-keyword">match</span> msg &#123;<br>        Message::Hello &#123;<br>            id:  id@ <span class="hljs-number">3</span>..=<span class="hljs-number">7</span>,<br>        &#125; =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;id 值的范围在 [3, 7] 之间: &#123;&#125;&quot;</span>, id),<br>        Message::Hello &#123; id: newid@ (<span class="hljs-number">10</span> | <span class="hljs-number">11</span> | <span class="hljs-number">12</span>) &#125; =&gt; &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;id 值的范围在 [10, 12] 之间: &#123;&#125;&quot;</span>, newid)<br>        &#125;<br>        Message::Hello &#123; id &#125; =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Found some other id: &#123;&#125;&quot;</span>, id),<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="7-方法Method"><a href="#7-方法Method" class="headerlink" title="7. 方法Method"></a>7. 方法Method</h3><blockquote><p>与面向对象语言中的方法差不多</p></blockquote><h4 id="7-1-定义方法"><a href="#7-1-定义方法" class="headerlink" title="7.1 定义方法"></a>7.1 定义方法</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Square</span> &#123;<br>    x: <span class="hljs-type">f64</span><br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Square</span> &#123;<br>    <span class="hljs-comment">// new 是 Square 的关联函数，因为它的第一个参数不是self,且new并不是关键字</span><br>    <span class="hljs-comment">// 这种方法往往用于初始化当前结构体的实例</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(x: <span class="hljs-type">f64</span>) <span class="hljs-punctuation">-&gt;</span> Square &#123;<br>        Square &#123;<br>            x: x,<br>        &#125;<br>    &#125;<br>  <br>    <span class="hljs-comment">// Square 的方法，&amp;self 表示借用当前的Square 结构体</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">area</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">f64</span> &#123;<br>        <span class="hljs-keyword">self</span>.x * <span class="hljs-keyword">self</span>.x<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>Rust语言与其他语言的对比</p><p><img src="https://pica.zhimg.com/80/v2-0d848e960f3279999eab4b1317f6538e_1440w.png" alt="img"></p><blockquote><p>这样将对象和方法定义分离是为了给予使用者更高的灵活度（数据和使用分离的方式）</p></blockquote><p>如上代码中</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Square</span> &#123;&#125;<br><span class="hljs-comment">// 表示impl语句块内的一切都和Square相关联</span><br></code></pre></td></tr></table></figure><p>注意，当rust在调用方法时，会自动引用与解引用，即自动添加&amp;、&amp;mut、或者* 来使代码与方法签名匹配</p><blockquote><p>方法和函数一样可以使用多个参数</p></blockquote><h4 id="7-2-关联函数"><a href="#7-2-关联函数" class="headerlink" title="7.2 关联函数"></a>7.2 关联函数</h4><blockquote><p>方法的参数中不包含self的称之为关联函数</p></blockquote><p>如构造函数就可以使用关联函数</p><blockquote><p>同一个结构体可以构造多个 impl 块，以提供更多灵活性和代码组织性</p></blockquote><p><strong>枚举也可以使用方法！！！</strong></p><blockquote><p>可以使用Self用于在构造函数内表示当前结构体类型</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Square</span>&#123;<br>    x: <span class="hljs-type">u32</span><br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Square</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span>&#123;<br>        <span class="hljs-keyword">Self</span>&#123;<br>            x:<span class="hljs-number">1</span>,<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="8-泛型和特征-Generics-Traits"><a href="#8-泛型和特征-Generics-Traits" class="headerlink" title="8.泛型和特征 Generics &amp; Traits"></a>8.泛型和特征 Generics &amp; Traits</h3><h4 id="8-1-泛型-Generics"><a href="#8-1-泛型-Generics" class="headerlink" title="8.1 泛型 Generics"></a>8.1 泛型 Generics</h4><blockquote><p>有点像 <strong>C++</strong> 里的函数模板</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>&lt;T: std::ops::Add&lt;Output = T&gt;&gt;(a:T, b:T) <span class="hljs-punctuation">-&gt;</span> T &#123;<br>    a + b<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;add i8: &#123;&#125;&quot;</span>, <span class="hljs-title function_ invoke__">add</span>(<span class="hljs-number">2i8</span>, <span class="hljs-number">3i8</span>));<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;add i32: &#123;&#125;&quot;</span>, <span class="hljs-title function_ invoke__">add</span>(<span class="hljs-number">20</span>, <span class="hljs-number">30</span>));<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;add f64: &#123;&#125;&quot;</span>, <span class="hljs-title function_ invoke__">add</span>(<span class="hljs-number">1.23</span>, <span class="hljs-number">1.23</span>));<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>出于惯例，常使用T（type的首字母）作为泛型参数的首选名称</p></blockquote><p>泛型在使用之前，需要先进行声明，如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">largest</span>&lt;T&gt;(list: &amp;[T]) <span class="hljs-punctuation">-&gt;</span> T &#123;...&#125;<br><span class="hljs-comment">// 如上largest&lt;T&gt;对泛型参数进行了声明，然后才在函数参数中进行使用该泛型参数 list: &amp;[T]</span><br></code></pre></td></tr></table></figure><h5 id="8-1-1-泛型参数约束"><a href="#8-1-1-泛型参数约束" class="headerlink" title="8.1.1 泛型参数约束"></a>8.1.1 泛型参数约束</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fmt::Display;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">create_and_print</span>&lt;T&gt;() <span class="hljs-keyword">where</span> T: <span class="hljs-built_in">From</span>&lt;<span class="hljs-type">i32</span>&gt; + Display &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">a</span>: T = <span class="hljs-number">100</span>.<span class="hljs-title function_ invoke__">into</span>(); <span class="hljs-comment">// 创建了类型为 T 的变量 a，它的初始值由 100 转换而来</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;a is: &#123;&#125;&quot;</span>, a);<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-title function_ invoke__">create_and_print</span>();<br>&#125;<br><br><span class="hljs-comment">// 此处的where T: From&lt;i32&gt; + Display 指定了 T 类型必须实现From&lt;i32&gt; 以及 Display 的特征</span><br></code></pre></td></tr></table></figure><p><strong>结构体和枚举以及方法也可以使用泛型</strong>（泛型参数可以有多个）</p><blockquote><p>可以使用如下方法进行多个泛型参数的使用</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Point</span>&lt;T, U&gt; &#123;<br>    x:T,<br>    y:U,<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">p</span> = Point&#123;x: <span class="hljs-number">1</span>, y: <span class="hljs-number">1.1</span>&#125;;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>此外注意尽量不要让泛型的参数个数和代码复杂度过高！！</p></blockquote><p><strong>可以为具体类型定义方法</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Point</span>&lt;<span class="hljs-type">f32</span>&gt; &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">distance_from_origin</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">f32</span> &#123;<br>        (<span class="hljs-keyword">self</span>.x.<span class="hljs-title function_ invoke__">powi</span>(<span class="hljs-number">2</span>) + <span class="hljs-keyword">self</span>.y.<span class="hljs-title function_ invoke__">powi</span>(<span class="hljs-number">2</span>)).<span class="hljs-title function_ invoke__">sqrt</span>()<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="8-1-2-const-泛型"><a href="#8-1-2-const-泛型" class="headerlink" title="8.1.2 const 泛型"></a>8.1.2 const 泛型</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">display_array</span>&lt;T: std::fmt::<span class="hljs-built_in">Debug</span>, <span class="hljs-keyword">const</span> N: <span class="hljs-type">usize</span>&gt;(arr: [T; N]) &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, arr);<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">arr</span>: [<span class="hljs-type">i32</span>; <span class="hljs-number">3</span>] = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br>    <span class="hljs-title function_ invoke__">display_array</span>(arr);<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">arr</span>: [<span class="hljs-type">i32</span>; <span class="hljs-number">2</span>] = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>];<br>    <span class="hljs-title function_ invoke__">display_array</span>(arr);<br>&#125;<br><span class="hljs-comment">// 上面的display_array函数为泛型 N 指定了const,且基于的值的类型是usize</span><br></code></pre></td></tr></table></figure><blockquote><p>有了const泛型之后，Rust将变得适合复杂矩阵的运算</p></blockquote><h6 id="8-1-2-1-const-泛型表达式"><a href="#8-1-2-1-const-泛型表达式" class="headerlink" title="8.1.2.1 const 泛型表达式"></a>8.1.2.1 const 泛型表达式</h6><p>目前只能在nightly版本内使用，先跳过</p><h6 id="8-1-2-2-const-fn"><a href="#8-1-2-2-const-fn" class="headerlink" title="8.1.2.2 const fn"></a>8.1.2.2 const fn</h6><blockquote><p>const fn 用于编译期间就需要知道计算结果的场景</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">const</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>(a: <span class="hljs-type">usize</span>, b: <span class="hljs-type">usize</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">usize</span> &#123;<br>    a + b<br>&#125;<br><br><span class="hljs-keyword">const</span> RESULT: <span class="hljs-type">usize</span> = <span class="hljs-title function_ invoke__">add</span>(<span class="hljs-number">5</span>, <span class="hljs-number">10</span>);<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The result is: &#123;&#125;&quot;</span>, RESULT);<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>Rust 中的泛型是零成本抽象，即使用泛型时，完全不用担心性能上的问题，但是编译的时候，Rust 会对代码进行单态化（monomorphization）来提升运行效率，就好像手写了每个具体定义的重复代码，所以没有运行时开销，但是编译速度会变慢，最终生成文件大小会变大</strong></p><h4 id="8-2-特征-Trait"><a href="#8-2-特征-Trait" class="headerlink" title="8.2 特征 Trait"></a>8.2 特征 Trait</h4><blockquote><p>如果不同的类型具有相同行为，那么就可以定义一个特征，接着为这些类型实现该特征</p><p>定义特征是把一些方法组合在一起，目的是定义一个实现某些目标所必需的行为的集合</p></blockquote><p>特征只定义行为看起来是怎么样而不去具体实现。</p><p>因此只定义特征方法的签名（即方法名、参数、以及返回值等），而不进行实现，此时方法签名结尾是;</p><p>而不是{}</p><p>(即类似c中的只声明)</p><p>如下例子Summary为Weibo和Post实现了特征</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 此处声明特征 Summary</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">Summary</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">summarize</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span>;<br>&#125;<br><br><span class="hljs-comment">// 此处声明结构体struct</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Post</span> &#123;<br>    <span class="hljs-keyword">pub</span> title: <span class="hljs-type">String</span>, <span class="hljs-comment">// 标题</span><br>    <span class="hljs-keyword">pub</span> author: <span class="hljs-type">String</span>, <span class="hljs-comment">// 作者</span><br>    <span class="hljs-keyword">pub</span> content: <span class="hljs-type">String</span>, <span class="hljs-comment">// 内容</span><br>&#125;<br><br><span class="hljs-comment">// 此处为 Post 实现特征 Summary</span><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Summary</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Post</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">summarize</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>        <span class="hljs-built_in">format!</span>(<span class="hljs-string">&quot;文章&#123;&#125;, 作者是&#123;&#125;&quot;</span>, <span class="hljs-keyword">self</span>.title, <span class="hljs-keyword">self</span>.author)<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 此处声明结构体 weibo</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Weibo</span> &#123;<br>    <span class="hljs-keyword">pub</span> username: <span class="hljs-type">String</span>,<br>    <span class="hljs-keyword">pub</span> content: <span class="hljs-type">String</span><br>&#125;<br><br><span class="hljs-comment">// 此处为 weibo 实现特征 Summary</span><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Summary</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Weibo</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">summarize</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>        <span class="hljs-built_in">format!</span>(<span class="hljs-string">&quot;&#123;&#125;发表了微博&#123;&#125;&quot;</span>, <span class="hljs-keyword">self</span>.username, <span class="hljs-keyword">self</span>.content)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>实现特征的语法与为结构体、枚举实现方法很像：<code>impl Summary for Post</code>，即为 Post 实现 Summary 特征</p></blockquote><p>接下来就可以在类型上调用特征的方法：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>()&#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">post</span> = Post&#123;title: <span class="hljs-string">&quot;Rust语言简介&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), author:<span class="hljs-string">&quot;Sunface&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), content:<span class="hljs-string">&quot;Rust屌爆了！&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>()&#125;;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">weibo</span> = Weibo&#123;username: <span class="hljs-string">&quot;sunface&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), content: <span class="hljs-string">&quot;疑似有点太极端了&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>()&#125;;<br>  <br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, post.<span class="hljs-title function_ invoke__">summarize</span>());<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, weibo.<span class="hljs-title function_ invoke__">summarize</span>());<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="8-3-1-特征定义与实现的位置（孤儿规则）"><a href="#8-3-1-特征定义与实现的位置（孤儿规则）" class="headerlink" title="8.3.1 特征定义与实现的位置（孤儿规则）"></a>8.3.1 特征定义与实现的位置（孤儿规则）</h5><p>如果想要为类型A实现特征T,则A或者T至少有一个要在当前作用域被定义</p><h5 id="8-3-2-默认实现"><a href="#8-3-2-默认实现" class="headerlink" title="8.3.2 默认实现"></a>8.3.2 默认实现</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">Echo</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">echo</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello from default implementation!&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Echo</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Weibo</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">echo</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello from Weibo!&quot;</span>)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>特征也可以作为函数参数</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">notify</span>(item: &amp;<span class="hljs-keyword">impl</span> <span class="hljs-title class_">Summary</span>) &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Breaking news! &#123;&#125;&quot;</span>, item.<span class="hljs-title function_ invoke__">summarize</span>());<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="8-3-3-特征约束（trait-bound）"><a href="#8-3-3-特征约束（trait-bound）" class="headerlink" title="8.3.3 特征约束（trait bound）"></a>8.3.3 特征约束（trait bound）</h5><p>形如下方为参数添加了Echo的特征约束（即参数必须要实现Echo特征）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">notify</span>&lt;T: Echo&gt; (item1: &amp;T, item2: &amp;T) &#123;&#125;<br></code></pre></td></tr></table></figure><p><code>T: Echo</code> 说明T必须要实现Echo特征，而内部的参数说明 <code>item1</code>和 <code>item2</code>必须有相同类型</p><h6 id="8-3-3-1多重约束"><a href="#8-3-3-1多重约束" class="headerlink" title="8.3.3.1多重约束"></a>8.3.3.1多重约束</h6><p>形如下方为参数添加了多个特征约束，称为多重约束</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">notify</span>&lt;T: Summary + Display&gt;(item: &amp;T) &#123;&#125;<br></code></pre></td></tr></table></figure><p>也可以用如下形式</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">notiry</span>(item: &amp;(<span class="hljs-keyword">impl</span> <span class="hljs-title class_">Summary</span> + Display)) &#123;&#125;<br></code></pre></td></tr></table></figure><h6 id="8-3-3-2-Where-约束"><a href="#8-3-3-2-Where-约束" class="headerlink" title="8.3.3.2 Where 约束"></a>8.3.3.2 Where 约束</h6><p>当参数和特征约束变得很多时，可以使用where来使代码更易读，也可以保留原有格式：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">some_func</span>&lt;T: Display + <span class="hljs-built_in">Clone</span>, U: <span class="hljs-built_in">Clone</span> + <span class="hljs-built_in">Debug</span>&gt;(t: &amp;T, u: &amp;U) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span>&#123;&#125;<br></code></pre></td></tr></table></figure><p>使用where作改进之后：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">some_func</span>&lt;T, U&gt;(t: &amp;T, u: &amp;U) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span><br><span class="hljs-keyword">where</span> T: Display + <span class="hljs-built_in">Clone</span>,<br>  U: <span class="hljs-built_in">Clone</span> + <span class="hljs-built_in">Debug</span><br>&#123;&#125;<br></code></pre></td></tr></table></figure><p><strong>可以通过函数返回 impl Trait</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">returns_summarizable</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">impl</span> <span class="hljs-title class_">Summary</span> &#123;<br>    Weibo &#123;<br>        username: <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;sunface&quot;</span>),<br>        content: <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<br>         <span class="hljs-string">&quot;这是内容&quot;</span>,<br>        )<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如上因为Weibo实现了Summary特征，所以可以用它来作为返回值</p><p>但是这种情况下只能返回一种类型！（除非使用特征对象）</p><p><strong>通过derive派生特征</strong></p><p>在结构体前加上形如 <code>#[derive(Debug)]</code>的标记，就可以为其自动实现对应的Debug特征代码。</p><p>derive提供的是rust默认提供的特征，如有特殊需求，可以手动重载实现</p><h4 id="8-3-特征对象-Todo"><a href="#8-3-特征对象-Todo" class="headerlink" title="8.3 特征对象 Todo!"></a>8.3 特征对象 <code>Todo!</code></h4><blockquote><p>在拥有继承的语言中，可以定义一个名为 <code>Component</code> 的类，该类上有一个 <code>draw</code> 方法。其他的类比如 <code>Button</code>、<code>Image</code> 和 <code>SelectBox</code> 会从 <code>Component</code> 派生并因此继承 <code>draw</code> 方法。它们各自都可以覆盖 <code>draw</code> 方法来定义自己的行为，但是框架会把所有这些类型当作是 <code>Component</code> 的实例，并在其上调用 <code>draw</code>。不过 Rust 并没有继承，我们得另寻出路。</p></blockquote><p>为了解决上面的所有问题，Rust引入了一个概念——特征对象</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 定义特征</span><br><span class="hljs-keyword">trait</span> <span class="hljs-title class_">Draw</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span>;<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Draw</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">u8</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>        <span class="hljs-built_in">format!</span>(<span class="hljs-string">&quot;u8: &#123;&#125;&quot;</span>, *<span class="hljs-keyword">self</span>)<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Draw</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">f64</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>        <span class="hljs-built_in">format!</span>(<span class="hljs-string">&quot;f64: &#123;&#125;&quot;</span>, *<span class="hljs-keyword">self</span>)<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">// 若 T 实现了 Draw 特征， 则调用该函数时传入的 Box&lt;T&gt; 可以被隐式转换成函数参数签名中的 Box&lt;dyn Draw&gt;</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw1</span>(x: <span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> Draw&gt;) &#123;<br>    <span class="hljs-comment">// 由于实现了 Deref 特征，Box 智能指针会自动解引用为它所包裹的值，然后调用该值对应的类型上定义的 `draw` 方法</span><br>    x.<span class="hljs-title function_ invoke__">draw</span>();<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw2</span>(x: &amp;<span class="hljs-keyword">dyn</span> Draw) &#123;<br>    x.<span class="hljs-title function_ invoke__">draw</span>();<br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Screen</span> &#123;<br>    <span class="hljs-keyword">pub</span> components: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> Draw&gt;&gt;,<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Screen</span> &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">run</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-keyword">for</span> <span class="hljs-variable">component</span> <span class="hljs-keyword">in</span> <span class="hljs-keyword">self</span>.components.<span class="hljs-title function_ invoke__">iter</span>() &#123;<br>            component.<span class="hljs-title function_ invoke__">draw</span>();<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-number">1.1f64</span>;<br>    <span class="hljs-comment">// do_something(&amp;x);</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">y</span> = <span class="hljs-number">8u8</span>;<br><br>    <span class="hljs-comment">// x 和 y 的类型 T 都实现了 `Draw` 特征，因为 Box&lt;T&gt; 可以在函数调用时隐式地被转换为特征对象 Box&lt;dyn Draw&gt; </span><br>    <span class="hljs-comment">// 基于 x 的值创建一个 Box&lt;f64&gt; 类型的智能指针，指针指向的数据被放置在了堆上</span><br>    <span class="hljs-title function_ invoke__">draw1</span>(<span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(x));<br>    <span class="hljs-comment">// 基于 y 的值创建一个 Box&lt;u8&gt; 类型的智能指针</span><br>    <span class="hljs-title function_ invoke__">draw1</span>(<span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(y));<br>    <span class="hljs-title function_ invoke__">draw2</span>(&amp;x);<br>    <span class="hljs-title function_ invoke__">draw2</span>(&amp;y);<br>&#125;<br></code></pre></td></tr></table></figure><p>如上代码中可以发现，<code>draw1</code>和 <code>draw2</code>使用了不同的参数，</p><ul><li><code>draw1</code>的参数是 <code>Box&lt;dyn Draw&gt;</code>形式的特征对象，该特征对象通过 <code>Box::new(x)</code>创建</li><li><code>draw2</code>的参数是 <code>&amp;dyn Draw</code>形式的特征对象，该特征对象通过 <code>&amp;x</code>的方式创建</li><li><code>dyn</code>关键字只用在特征对象的类型声明上，在创建时无需使用 <code>dyn</code></li></ul><p><strong>使用特征对象作为函数返回值</strong></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">hatch_a_bird</span>(num: <span class="hljs-type">u32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> Bird&gt; &#123;<br>    <span class="hljs-keyword">match</span> num &#123;<br>        <span class="hljs-number">1</span> =&gt; <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(Swan&#123;&#125;),<br>        <span class="hljs-number">2</span> =&gt; <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(Duck&#123;&#125;),<br>        _ =&gt; <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(Swan&#123;&#125;),<br>    &#125;  <br>&#125;<br><span class="hljs-comment">// 如上函数返回了具有Bird特征的返回值</span><br><span class="hljs-comment">// 注意这里的Swan和Duck将会丢失Bird特征内部未实现的方法</span><br></code></pre></td></tr></table></figure><h5 id="8-3-1-特征对象的动态分发"><a href="#8-3-1-特征对象的动态分发" class="headerlink" title="8.3.1 特征对象的动态分发"></a>8.3.1 特征对象的动态分发</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">trait</span> <span class="hljs-title class_">Bird</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">quack</span>(&amp;<span class="hljs-keyword">self</span>);<br>&#125;<br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Duck</span>;<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Duck</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fly</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Look, the duck is flying&quot;</span>)<br>    &#125;<br>&#125;<br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Swan</span>;<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Swan</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fly</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Look, the duck.. oh sorry, the swan is flying&quot;</span>)<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Bird</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Duck</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">quack</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, <span class="hljs-string">&quot;duck duck&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Bird</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Swan</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">quack</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, <span class="hljs-string">&quot;swan swan&quot;</span>);<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 此处以数组形式来使用特征对象</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">birds</span>: [<span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> Bird&gt;; <span class="hljs-number">2</span>] = [<span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(Duck &#123;&#125;), <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(Swan &#123;&#125;)];<br><br>    <span class="hljs-keyword">for</span> <span class="hljs-variable">bird</span> <span class="hljs-keyword">in</span> birds &#123;<br>        bird.<span class="hljs-title function_ invoke__">quack</span>();<br>        <span class="hljs-comment">// 当 duck 和 swan 变成 bird 后，它们都忘了如何翱翔于天际，只记得该怎么叫唤了。。</span><br>        <span class="hljs-comment">// 因此，以下代码会报错</span><br>        <span class="hljs-comment">// bird.fly();</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>如上当duck和swan在使用Bird特征对象后，会丢失原有的未在Bird特征内实现的特性。</p><p>这是因为特征对象的动态分发导致的，后面再来仔细看看这个特性</p><h6 id="⚠TODO！-4"><a href="#⚠TODO！-4" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h6><h5 id="8-3-2-Self-与-self"><a href="#8-3-2-Self-与-self" class="headerlink" title="8.3.2 Self 与 self"></a>8.3.2 Self 与 self</h5><blockquote><p>Rust中有两个self,一个指代当前的实例对象，一个指代特征或者方法类型的别名</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">trait</span> <span class="hljs-title class_">Draw</span>&#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">draw</span>(&amp;<span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span>;<br>&#125;<br></code></pre></td></tr></table></figure><p>上述代码中：</p><ul><li>Self：特征（当作为impl 代码块中的Self时，一般指返回的类型）</li><li>self：当前实例对象</li></ul><h5 id="8-3-3-特征对象的限制"><a href="#8-3-3-特征对象的限制" class="headerlink" title="8.3.3 特征对象的限制"></a>8.3.3 特征对象的限制</h5><p>只有对象安全的特征才可以拥有特征对象，当一个特征的所有方法都有如下属性时，它的对象才是安全的：</p><ul><li>方法的返回类型不能是Self</li><li>方法没有任何泛型参数</li></ul><h4 id="8-4-进一步深入"><a href="#8-4-进一步深入" class="headerlink" title="8.4 进一步深入"></a>8.4 进一步深入</h4><h5 id="8-4-1-关联类型"><a href="#8-4-1-关联类型" class="headerlink" title="8.4.1 关联类型"></a>8.4.1 关联类型</h5><blockquote><p>在特征定义的语句块中，申明一个自定义类型，这样就可以在特征的方法签名中使用该类型，这个就是该特征的关联类型</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">Iterator</span> &#123;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Item</span>;<br>  <br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">next</span>(&amp;<span class="hljs-keyword">mut</span> <span class="hljs-keyword">self</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;<span class="hljs-keyword">Self</span>::Item&gt;;<br>&#125;<br></code></pre></td></tr></table></figure><p>以上就是标准库中的迭代器特征 <code>Iterator</code>,有一个 <code>Item</code>关联类型，用于替代遍历的值的类型。</p><h5 id="8-4-2-默认泛型类型参数"><a href="#8-4-2-默认泛型类型参数" class="headerlink" title="8.4.2 默认泛型类型参数"></a>8.4.2 默认泛型类型参数</h5><blockquote><p>当使用泛型类型参数时，可以为其指定一个默认的具体类型，例如标准库中的std::ops::Add特征：</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">trait</span> <span class="hljs-title class_">Add</span>&lt;RHS=<span class="hljs-keyword">Self</span>&gt; &#123;<br>    <span class="hljs-keyword">type</span> <span class="hljs-title class_">Output</span>;<br>  <br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>(<span class="hljs-keyword">self</span>, rhs: RHS) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-keyword">Self</span>::Output;<br>&#125;<br></code></pre></td></tr></table></figure><p>可以看到有一个泛型参数 <code>RHS</code>, 但是这里给 <code>RHS</code>一个默认值，也就是当用户不指定 <code>RHS</code>时，默认使用两个同样类型的值进行相加，然后返回一个关联类型 <code>Output</code></p><h6 id="8-4-2-1-运算符重载"><a href="#8-4-2-1-运算符重载" class="headerlink" title="8.4.2.1 运算符重载"></a>8.4.2.1 运算符重载</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::ops::Add;<br><br><span class="hljs-meta">#[derive(Debug, PartialEq)]</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Point</span> &#123;<br>    x: <span class="hljs-type">i32</span>,<br>    y: <span class="hljs-type">i32</span>,<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Add</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Point</span> &#123;<br>    <span class="hljs-keyword">type</span> <span class="hljs-title class_">Output</span> = Point;<br><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>(<span class="hljs-keyword">self</span>, other: Point) <span class="hljs-punctuation">-&gt;</span> Point &#123;<br>        Point &#123;<br>            x: <span class="hljs-keyword">self</span>.x + other.x,<br>            y: <span class="hljs-keyword">self</span>.y + other.y,<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">assert_eq!</span>(Point &#123; x: <span class="hljs-number">1</span>, y: <span class="hljs-number">0</span> &#125; + Point &#123; x: <span class="hljs-number">2</span>, y: <span class="hljs-number">3</span> &#125;,<br>               Point &#123; x: <span class="hljs-number">3</span>, y: <span class="hljs-number">3</span> &#125;);<br>&#125;<br></code></pre></td></tr></table></figure><p>如上代码为 <code>Point</code>类型重载了（实现了）<code>+</code>的能力，但是Rust不支持创建自定义运算符，且只有定义在 <code>std::ops</code>中的运算符才能进行重载，上述例子没有实现 <code>Add&lt;RHS&gt;</code>特征，而是实现了 <code>Add</code>特征，这意味着我们使用了RHS的默认类型也就是Self,或者说，此处定义的是两个相同的Point类型相加，无需指定RHS.</p><p>如下例子与上述例子相反，创建了两个不同类型的相加：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::ops::Add;<br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Millimeters</span>(<span class="hljs-type">u32</span>);<br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Meters</span>(<span class="hljs-type">u32</span>);<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Add</span>&lt;Meters&gt; <span class="hljs-keyword">for</span> <span class="hljs-title class_">Millimeters</span> &#123;<br>    <span class="hljs-keyword">type</span> <span class="hljs-title class_">Output</span> = Millimeters;<br><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add</span>(<span class="hljs-keyword">self</span>, other: Meters) <span class="hljs-punctuation">-&gt;</span> Millimeters &#123;<br>        <span class="hljs-title function_ invoke__">Millimeters</span>(<span class="hljs-keyword">self</span>.<span class="hljs-number">0</span> + (other.<span class="hljs-number">0</span> * <span class="hljs-number">1000</span>))<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>默认类型参数主要用于两个方面：</p><ul><li>减少实现的样板代码</li><li>扩展类型但是无需大幅修改现有的代码</li></ul><h5 id="8-4-3-调用同名方法"><a href="#8-4-3-调用同名方法" class="headerlink" title="8.4.3 调用同名方法"></a>8.4.3 调用同名方法</h5><p>当类型与特征具有同名的方法的时候，编译器会优先调用类型中的方法</p><h6 id="8-4-3-1-调用特征上的同名方法"><a href="#8-4-3-1-调用特征上的同名方法" class="headerlink" title="8.4.3.1 调用特征上的同名方法"></a>8.4.3.1 <strong>调用特征上的同名方法</strong></h6><p>使用显式调用的语法，如下</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">person</span> = Human;<br>    Pilot::<span class="hljs-title function_ invoke__">fly</span>(&amp;person); <span class="hljs-comment">// 调用Pilot特征上的方法</span><br>    Wizard::<span class="hljs-title function_ invoke__">fly</span>(&amp;person); <span class="hljs-comment">// 调用Wizard特征上的方法</span><br>    person.<span class="hljs-title function_ invoke__">fly</span>(); <span class="hljs-comment">// 调用Human类型自身的方法</span><br>&#125;<br><span class="hljs-comment">// 注意特征上的fly()参数为&amp;self</span><br></code></pre></td></tr></table></figure><p>如果是关联函数，也即没有 <code>&amp;self</code>参数的时候呢？使用<strong>完全限定语法</strong></p><h6 id="8-4-3-2-完全限定语法"><a href="#8-4-3-2-完全限定语法" class="headerlink" title="8.4.3.2 完全限定语法"></a>8.4.3.2 <strong>完全限定语法</strong></h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust">&lt;SpecificType <span class="hljs-keyword">as</span> TraitName&gt;::<span class="hljs-title function_ invoke__">func_name</span>(receiver_if_method, args...)<br></code></pre></td></tr></table></figure><p>使用如上形式调用函数</p><p>在尖括号内，通过as关键字，向Rust编译器提供了类型注解，最终会调用</p><p><code>impl TraitName for SpecificType</code>中的方法。</p><p>其中第一个参数是方法接收器 <code>receiver</code>（三种 <code>self</code>-&gt; 1. <code>self</code> 值接收器 2.<code>&amp;self</code>可借用的引用接收器 3. <code>&amp;mut self</code> 可变引用接收器）,只有方法才拥有。</p><blockquote><p>完全限定语法可以用于任何函数或方法调用，那么我们为何很少用到这个语法？原因是 Rust  编译器能根据上下文自动推导出调用的路径，因此大多数时候，我们都无需使用完全限定语法。只有当存在多个同名函数或方法，且 Rust  无法区分出你想调用的目标函数时，该用法才能真正有用武之地。</p></blockquote><h5 id="8-4-4-特征定义中的特征约束"><a href="#8-4-4-特征定义中的特征约束" class="headerlink" title="8.4.4 特征定义中的特征约束"></a>8.4.4 特征定义中的特征约束</h5><blockquote><p>当需要让特征A使用特征B的功能时，不仅要为类型实现特征A,还需要为类型实现特征B</p><p>这里就要引入一个新概念 <strong>基特征（super trait）</strong>, 此处的特征B就被成为<strong>基特征</strong></p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">trait</span> <span class="hljs-title class_">A</span>: B &#123;<br>    <span class="hljs-comment">/// ...</span><br>&#125;<br></code></pre></td></tr></table></figure><p>如上，若要实现A特征，则需要先实现B特征</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">impl</span> <span class="hljs-title class_">B</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">SpecificType</span> &#123;<br>    <span class="hljs-comment">/// ...</span><br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">A</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">SpecificType</span> &#123;<br>    <span class="hljs-comment">/// ...</span><br>&#125;<br></code></pre></td></tr></table></figure><h5 id="8-4-5-在外部类型上实现外部特征（newtype）"><a href="#8-4-5-在外部类型上实现外部特征（newtype）" class="headerlink" title="8.4.5 在外部类型上实现外部特征（newtype）"></a>8.4.5 在外部类型上实现外部特征（newtype）</h5><blockquote><p>newtype 模式用于绕过<strong>孤儿原则</strong></p><p>简而言之：就是为一个<strong>元组结构体</strong>创建新类型。该元组结构体封装有一个字段，该字段就是希望实现特征的具体类型。</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fmt;<br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Wrapper</span>(<span class="hljs-type">Vec</span>&lt;<span class="hljs-type">String</span>&gt;);<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">fmt</span>::Display <span class="hljs-keyword">for</span> <span class="hljs-title class_">Wrapper</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fmt</span>(&amp;<span class="hljs-keyword">self</span>, f: &amp;<span class="hljs-keyword">mut</span> fmt::Formatter) <span class="hljs-punctuation">-&gt;</span> fmt::<span class="hljs-type">Result</span> &#123;<br>        <span class="hljs-built_in">write!</span>(f, <span class="hljs-string">&quot;[&#123;&#125;]&quot;</span>, <span class="hljs-keyword">self</span>.<span class="hljs-number">0</span>.<span class="hljs-title function_ invoke__">join</span>(<span class="hljs-string">&quot;, &quot;</span>))<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">w</span> = <span class="hljs-title function_ invoke__">Wrapper</span>(<span class="hljs-built_in">vec!</span>[<span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;hello&quot;</span>), <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;world&quot;</span>)]);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;w = &#123;&#125;&quot;</span>, w);<br>&#125;<br></code></pre></td></tr></table></figure><p>这样一来，本来 <code>Vec(T)</code>和 <code>Display</code>特征都不在标准库中，无法直接为其实现Display,但是可以通过 <code>newtype</code>模式为其实现 <code>Display</code>，即上述方法。通过先创建一个类型，这样就摆脱了孤儿规则——即在本地有特征和类型其中一种。</p><hr><h3 id="9-集合类型"><a href="#9-集合类型" class="headerlink" title="9.集合类型"></a>9.集合类型</h3><h4 id="9-1-动态数组-Vector"><a href="#9-1-动态数组-Vector" class="headerlink" title="9.1 动态数组 Vector"></a>9.1 动态数组 Vector</h4><h5 id="9-1-1-创建动态数组"><a href="#9-1-1-创建动态数组" class="headerlink" title="9.1.1 创建动态数组"></a>9.1.1 创建动态数组</h5><h6 id="Vec-new"><a href="#Vec-new" class="headerlink" title="Vec::new"></a>Vec::new</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span>: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">i32</span>&gt; = <span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">new</span>();<br><span class="hljs-comment">// or this way</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span>: <span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">new</span>();<br>v.<span class="hljs-title function_ invoke__">push</span>(<span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p>We could use <code>Vec::with_capacity(capacity)</code> if we know the capacity of the number of items, this could help performance promotion.</p><h6 id="vec-macro"><a href="#vec-macro" class="headerlink" title="vec![] (macro)"></a>vec![] (macro)</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br></code></pre></td></tr></table></figure><h5 id="9-1-2-Update-Vector"><a href="#9-1-2-Update-Vector" class="headerlink" title="9.1.2 Update Vector"></a>9.1.2 Update Vector</h5><p>Using <strong><code>push()</code></strong> Method (Should Declare as Mutable -&gt; let mut xxx;)</p><blockquote><p>与结构体一样，Vector 类型在超出作用域范围后，会被自动删除</p><p>其内部所有的内容也会随之被删除</p></blockquote><h5 id="9-1-3-Read-Elements-from-Vector"><a href="#9-1-3-Read-Elements-from-Vector" class="headerlink" title="9.1.3 Read Elements from Vector"></a>9.1.3 Read Elements from Vector</h5><ul><li>Index</li><li>get Method</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br><span class="hljs-keyword">let</span> <span class="hljs-variable">third</span>: &amp;<span class="hljs-number">32</span> = &amp;v[<span class="hljs-number">2</span>];<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;第三个元素是 &#123;&#125;&quot;</span>, third);<br><br>v.<span class="hljs-title function_ invoke__">get</span>(<span class="hljs-number">2</span>); <span class="hljs-comment">// 返回一个 Option</span><br></code></pre></td></tr></table></figure><p>使用Index可能会造成越界</p><p>使用 <code>.get()</code>方法返回一个Option（有值的时候返回 <code>Some(T)</code>，无值的时候返回 <code>None</code>）</p><h5 id="9-1-4-同时借用多个数组元素（切片？）"><a href="#9-1-4-同时借用多个数组元素（切片？）" class="headerlink" title="9.1.4 同时借用多个数组元素（切片？）"></a>9.1.4 同时借用多个数组元素（切片？）</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">first</span> = &amp;v[<span class="hljs-number">0</span>];<br><br>v.<span class="hljs-title function_ invoke__">push</span>(<span class="hljs-number">6</span>);<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The first element is: &#123;first&#125;&quot;</span>);<br><br></code></pre></td></tr></table></figure><p>如上会发生错误，因为 <code>let first = &amp;v[0];</code> 发生了不可变借用，而push方法又发生了可变借用，而first在push方法之后还会用到，所以可以通过交换这两行代码来避免报错</p><h5 id="9-1-5-迭代遍历-Vector-中的元素"><a href="#9-1-5-迭代遍历-Vector-中的元素" class="headerlink" title="9.1.5 迭代遍历 Vector 中的元素"></a>9.1.5 迭代遍历 Vector 中的元素</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br><span class="hljs-comment">// 不可变借用</span><br><span class="hljs-keyword">for</span> <span class="hljs-variable">i</span> <span class="hljs-keyword">in</span> &amp;v&#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;i&#125;&quot;</span>)<br>&#125;<br><span class="hljs-comment">// 可变借用</span><br><span class="hljs-keyword">for</span> <span class="hljs-variable">i</span> <span class="hljs-keyword">in</span> &amp;<span class="hljs-keyword">mut</span> v&#123;<br>    <span class="hljs-comment">// *i 代表解引用</span><br>    *i += <span class="hljs-number">10</span>;<br>&#125;<br><br><span class="hljs-keyword">for</span> <span class="hljs-variable">i</span> <span class="hljs-keyword">in</span> &amp;v&#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;i&#125;&quot;</span>)<br>&#125;<br><br></code></pre></td></tr></table></figure><h5 id="9-1-6-存储不同元素"><a href="#9-1-6-存储不同元素" class="headerlink" title="9.1.6 存储不同元素"></a>9.1.6 存储不同元素</h5><p>可以使用枚举套娃来实现（即在不同类型上套一层同样的枚举类型）</p><p>也可以用特征对象来实现</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">trait</span> <span class="hljs-title class_">IpAddr</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">display</span>(&amp;<span class="hljs-keyword">self</span>);<br>&#125;<br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">V4</span>(<span class="hljs-type">String</span>);<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">IpAddr</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">V4</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">display</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;ipv4: &#123;:?&#125;&quot;</span>,<span class="hljs-keyword">self</span>.<span class="hljs-number">0</span>)<br>    &#125;<br>&#125;<br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">V6</span>(<span class="hljs-type">String</span>);<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">IpAddr</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">V6</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">display</span>(&amp;<span class="hljs-keyword">self</span>) &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;ipv6: &#123;:?&#125;&quot;</span>,<span class="hljs-keyword">self</span>.<span class="hljs-number">0</span>)<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span>: <span class="hljs-type">Vec</span>&lt;<span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> IpAddr&gt;&gt; = <span class="hljs-built_in">vec!</span>[<br>        <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-title function_ invoke__">V4</span>(<span class="hljs-string">&quot;127.0.0.1&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>())),<br>        <span class="hljs-type">Box</span>::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-title function_ invoke__">V6</span>(<span class="hljs-string">&quot;::1&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>())),<br>    ];<br><br>    <span class="hljs-keyword">for</span> <span class="hljs-variable">ip</span> <span class="hljs-keyword">in</span> v &#123;<br>        ip.<span class="hljs-title function_ invoke__">display</span>();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="9-1-7-Vector-常用方法"><a href="#9-1-7-Vector-常用方法" class="headerlink" title="9.1.7 Vector 常用方法"></a>9.1.7 Vector 常用方法</h5><h6 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>;<span class="hljs-number">3</span>];<span class="hljs-comment">// 3个1</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v_from</span> = <span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">from</span>([<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>]); <span class="hljs-comment">// 从已有数组初始化</span><br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>除此之外</p><p><code>vec!(..)</code> 和 <code>vec![..]</code> 是同样的宏，宏可以使用 []、()、{}三种形式</p></blockquote><p>例子1：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span> = <span class="hljs-type">Vec</span>::<span class="hljs-title function_ invoke__">with_capacity</span>(<span class="hljs-number">10</span>);<br>    v.<span class="hljs-title function_ invoke__">extend</span>([<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]);    <span class="hljs-comment">// 附加数据到 v</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Vector 长度是: &#123;&#125;, 容量是: &#123;&#125;&quot;</span>, v.<span class="hljs-title function_ invoke__">len</span>(), v.<span class="hljs-title function_ invoke__">capacity</span>());<br><br>    v.<span class="hljs-title function_ invoke__">reserve</span>(<span class="hljs-number">100</span>);        <span class="hljs-comment">// 调整 v 的容量，至少要有 100 的容量</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Vector（reserve） 长度是: &#123;&#125;, 容量是: &#123;&#125;&quot;</span>, v.<span class="hljs-title function_ invoke__">len</span>(), v.<span class="hljs-title function_ invoke__">capacity</span>());<br><br>    v.<span class="hljs-title function_ invoke__">shrink_to_fit</span>();     <span class="hljs-comment">// 释放剩余的容量，一般情况下，不会主动去释放容量</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Vector（shrink_to_fit） 长度是: &#123;&#125;, 容量是: &#123;&#125;&quot;</span>, v.<span class="hljs-title function_ invoke__">len</span>(), v.<span class="hljs-title function_ invoke__">capacity</span>());<br>&#125;<br></code></pre></td></tr></table></figure><p>例子2：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span> =  <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>];<br><span class="hljs-built_in">assert!</span>(!v.<span class="hljs-title function_ invoke__">is_empty</span>());         <span class="hljs-comment">// 检查 v 是否为空</span><br><br>v.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-number">2</span>, <span class="hljs-number">3</span>);                 <span class="hljs-comment">// 在指定索引插入数据，索引值不能大于 v 的长度， v: [1, 2, 3] </span><br><span class="hljs-built_in">assert_eq!</span>(v.<span class="hljs-title function_ invoke__">remove</span>(<span class="hljs-number">1</span>), <span class="hljs-number">2</span>);     <span class="hljs-comment">// 移除指定位置的元素并返回, v: [1, 3]</span><br><span class="hljs-built_in">assert_eq!</span>(v.<span class="hljs-title function_ invoke__">pop</span>(), <span class="hljs-title function_ invoke__">Some</span>(<span class="hljs-number">3</span>));   <span class="hljs-comment">// 删除并返回 v 尾部的元素，v: [1]</span><br><span class="hljs-built_in">assert_eq!</span>(v.<span class="hljs-title function_ invoke__">pop</span>(), <span class="hljs-title function_ invoke__">Some</span>(<span class="hljs-number">1</span>));   <span class="hljs-comment">// v: []</span><br><span class="hljs-built_in">assert_eq!</span>(v.<span class="hljs-title function_ invoke__">pop</span>(), <span class="hljs-literal">None</span>);      <span class="hljs-comment">// 记得 pop 方法返回的是 Option 枚举值</span><br>v.<span class="hljs-title function_ invoke__">clear</span>();                      <span class="hljs-comment">// 清空 v, v: []</span><br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v1</span> = [<span class="hljs-number">11</span>, <span class="hljs-number">22</span>].<span class="hljs-title function_ invoke__">to_vec</span>(); <span class="hljs-comment">// append 操作会导致 v1 清空数据，增加可变声明</span><br>v.<span class="hljs-title function_ invoke__">append</span>(&amp;<span class="hljs-keyword">mut</span> v1);              <span class="hljs-comment">// 将 v1 中的所有元素附加到 v 中, v1: []</span><br>v.<span class="hljs-title function_ invoke__">truncate</span>(<span class="hljs-number">1</span>);                  <span class="hljs-comment">// 截断到指定长度，多余的元素被删除, v: [11]</span><br>v.<span class="hljs-title function_ invoke__">retain</span>(|x| *x &gt; <span class="hljs-number">10</span>);          <span class="hljs-comment">// 保留满足条件的元素，即删除不满足条件的元素</span><br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">11</span>, <span class="hljs-number">22</span>, <span class="hljs-number">33</span>, <span class="hljs-number">44</span>, <span class="hljs-number">55</span>];<br><span class="hljs-comment">// 删除指定范围的元素，同时获取被删除元素的迭代器, v: [11, 55], m: [22, 33, 44]</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">m</span>: <span class="hljs-type">Vec</span>&lt;_&gt; = v.<span class="hljs-title function_ invoke__">drain</span>(<span class="hljs-number">1</span>..=<span class="hljs-number">3</span>).<span class="hljs-title function_ invoke__">collect</span>();  <br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">v2</span> = m.<span class="hljs-title function_ invoke__">split_off</span>(<span class="hljs-number">1</span>);        <span class="hljs-comment">// 指定索引处切分成两个 vec, m: [22], v2: [33, 44]</span><br></code></pre></td></tr></table></figure><p>例子3：（数组切片的方式）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">11</span>, <span class="hljs-number">22</span>, <span class="hljs-number">33</span>, <span class="hljs-number">44</span>, <span class="hljs-number">55</span>];<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">slice</span> = &amp;v[<span class="hljs-number">1</span>..=<span class="hljs-number">3</span>];<br>    <span class="hljs-built_in">assert_eq!</span>(slice, &amp;[<span class="hljs-number">22</span>, <span class="hljs-number">33</span>, <span class="hljs-number">44</span>]);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="9-1-8-Vector-的排序"><a href="#9-1-8-Vector-的排序" class="headerlink" title="9.1.8 Vector 的排序"></a>9.1.8 Vector 的排序</h5><ul><li>稳定排序：<ul><li>sort</li><li>sort_by</li></ul></li><li>非稳定排序：<ul><li>sort_unstable</li><li>sort_unstable_by</li></ul></li></ul><h6 id="整数数组的排序"><a href="#整数数组的排序" class="headerlink" title="整数数组的排序"></a>整数数组的排序</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">vec</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">5</span>, <span class="hljs-number">10</span>, <span class="hljs-number">2</span>, <span class="hljs-number">15</span>];  <br>    vec.<span class="hljs-title function_ invoke__">sort_unstable</span>();  <br>    <span class="hljs-built_in">assert_eq!</span>(vec, <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">5</span>, <span class="hljs-number">10</span>, <span class="hljs-number">15</span>]);<br>&#125;<br><span class="hljs-comment">// 正常执行无报错</span><br></code></pre></td></tr></table></figure><h6 id="浮点数数组的排序"><a href="#浮点数数组的排序" class="headerlink" title="浮点数数组的排序"></a>浮点数数组的排序</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">vec</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1.0</span>, <span class="hljs-number">5.6</span>, <span class="hljs-number">10.3</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">15f32</span>];  <br>    vec.<span class="hljs-title function_ invoke__">sort_unstable</span>();  <br>    <span class="hljs-built_in">assert_eq!</span>(vec, <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1.0</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">5.6</span>, <span class="hljs-number">10.3</span>, <span class="hljs-number">15f32</span>]);<br>&#125;<br><span class="hljs-comment">// 会报错，因为浮点数中存在一个NAN值，无法与其他的浮点数进行比较所以没有实现Trait Ord,</span><br><span class="hljs-comment">// 只实现了部分可比较的特性 PartialOrd</span><br></code></pre></td></tr></table></figure><p>所以使用 <code>partial_cmp</code>来排序</p><h6 id="⚠todo！"><a href="#⚠todo！" class="headerlink" title="⚠todo！"></a>⚠todo！</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">vec</span> = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1.0</span>, <span class="hljs-number">5.6</span>, <span class="hljs-number">10.3</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">15f32</span>];  <br>    vec.<span class="hljs-title function_ invoke__">sort_unstable_by</span>(|a, b| a.<span class="hljs-title function_ invoke__">partial_cmp</span>(b).<span class="hljs-title function_ invoke__">unwrap</span>());  <br>    <span class="hljs-built_in">assert_eq!</span>(vec, <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1.0</span>, <span class="hljs-number">2.0</span>, <span class="hljs-number">5.6</span>, <span class="hljs-number">10.3</span>, <span class="hljs-number">15f32</span>]);<br>&#125;<br></code></pre></td></tr></table></figure><h6 id="结构体数组的排序"><a href="#结构体数组的排序" class="headerlink" title="结构体数组的排序"></a>结构体数组的排序</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[derive(Debug)]</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Person</span> &#123;<br>    name: <span class="hljs-type">String</span>,<br>    age: <span class="hljs-type">u32</span>,<br>&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">new</span>(name: <span class="hljs-type">String</span>, age: <span class="hljs-type">u32</span>) <span class="hljs-punctuation">-&gt;</span> Person &#123;<br>        Person &#123; name, age &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">people</span> = <span class="hljs-built_in">vec!</span>[<br>        Person::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-string">&quot;Zoe&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">25</span>),<br>        Person::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-string">&quot;Al&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">60</span>),<br>        Person::<span class="hljs-title function_ invoke__">new</span>(<span class="hljs-string">&quot;John&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">1</span>),<br>    ];<br>    <span class="hljs-comment">// 定义一个按照年龄倒序排序的对比函数</span><br>    people.<span class="hljs-title function_ invoke__">sort_unstable_by</span>(|a, b| b.age.<span class="hljs-title function_ invoke__">cmp</span>(&amp;a.age));<br><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, people);<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="9-2-KV-存储-HashMap"><a href="#9-2-KV-存储-HashMap" class="headerlink" title="9.2 KV 存储 HashMap"></a>9.2 KV 存储 HashMap</h4><h5 id="9-2-1-创建HashMap"><a href="#9-2-1-创建HashMap" class="headerlink" title="9.2.1 创建HashMap"></a>9.2.1 创建HashMap</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// new方法</span><br><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">my_gems</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();<br><br>my_gems.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;红宝石&quot;</span>, <span class="hljs-number">1</span>);<br>my_gems.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;蓝宝石&quot;</span>, <span class="hljs-number">2</span>);<br>my_gems.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;河边捡的误以为是宝石的破石头&quot;</span>, <span class="hljs-number">18</span>);<br><span class="hljs-comment">// 和 Vec一样如果预先知道要存储的KV对个数，可以使用HashMap::with_capacity(capacity)创建指定大小的HashMap，以此来提升性能</span><br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">use</span> std::collections::HashMap;<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">teams_list</span> = <span class="hljs-built_in">vec!</span>[<br>        (<span class="hljs-string">&quot;中国队&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">100</span>),<br>        (<span class="hljs-string">&quot;美国队&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">10</span>),<br>        (<span class="hljs-string">&quot;日本队&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(), <span class="hljs-number">50</span>),<br>    ];<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">teams_map</span>: HashMap&lt;_,_&gt; = teams_list.<span class="hljs-title function_ invoke__">into_iter</span>().<span class="hljs-title function_ invoke__">collect</span>();<br>    <span class="hljs-comment">// into_iter() 方法将列表（数组）转换为迭代器，接着通过collect()进行收集</span><br>    <span class="hljs-comment">// 同时需要标注类型 HashMap&lt;_,_&gt;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>,teams_map)<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="9-2-2-所有权转移"><a href="#9-2-2-所有权转移" class="headerlink" title="9.2.2 所有权转移"></a>9.2.2 所有权转移</h5><ul><li>若类型实现Copy特征，则该类型会被复制进HashMap</li><li>若没有实现Copy特征，所有权将被转移给HashMap中</li></ul><p><strong>如果使用引用类型放入 HashMap 中</strong>，请确保该引用的生命周期至少跟 <code>HashMap</code> 活得一样久（否则会出错）</p><h5 id="9-2-3-查询-HashMap"><a href="#9-2-3-查询-HashMap" class="headerlink" title="9.2.3 查询 HashMap"></a>9.2.3 查询 HashMap</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 通过 get 方法获取元素</span><br><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">scores</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();<br><br>scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-number">10</span>);<br>scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Yellow&quot;</span>), <span class="hljs-number">50</span>);<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">team_name</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Blue&quot;</span>);<br><span class="hljs-keyword">let</span> <span class="hljs-variable">score</span>: <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-type">i32</span>&gt; = scores.<span class="hljs-title function_ invoke__">get</span>(&amp;team_name);<br></code></pre></td></tr></table></figure><ul><li><code>get</code>方法返回一个 <code>Option&lt;&amp;i32&gt;</code>类型：查询不到就会返回 <code>None</code>,查询到了返回 <code>Some(&amp;i32)</code></li><li><code>&amp;i32</code>是对HashMap中值的借用，如果不使用借用，可能会发生所有权的转移</li></ul><h6 id="通过循环的方式依次遍历KV对"><a href="#通过循环的方式依次遍历KV对" class="headerlink" title="通过循环的方式依次遍历KV对"></a>通过循环的方式依次遍历KV对</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">scores</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();<br><br>scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-number">10</span>);<br>scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;Yellow&quot;</span>), <span class="hljs-number">50</span>);<br><br><span class="hljs-keyword">for</span> (key, value) <span class="hljs-keyword">in</span> &amp;scores &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, key, value);<br>&#125;<br><br></code></pre></td></tr></table></figure><h5 id="9-2-4-Option中的copied方法和unwrap-or方法"><a href="#9-2-4-Option中的copied方法和unwrap-or方法" class="headerlink" title="9.2.4 Option中的copied方法和unwrap_or方法"></a>9.2.4 <strong>Option中的copied方法和unwrap_or方法</strong></h5><h6 id="⚠TODO"><a href="#⚠TODO" class="headerlink" title="⚠TODO!"></a>⚠TODO!</h6><ul><li><p><code>copied</code>方法</p><ul><li>含义：</li><li>用法：</li><li>例子：</li></ul></li><li><p><code>unwrap_or</code>方法</p><ul><li>含义：</li><li>用法：</li><li>例子：</li></ul></li></ul><h5 id="9-2-5-更新HashMap中的值"><a href="#9-2-5-更新HashMap中的值" class="headerlink" title="9.2.5 更新HashMap中的值"></a>9.2.5 更新HashMap中的值</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">use</span> std::collections::HashMap;<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">scores</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();<br><br>    scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;Blue&quot;</span>, <span class="hljs-number">10</span>);<br><br>    <span class="hljs-comment">// 覆盖已有的值</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">old</span> = scores.<span class="hljs-title function_ invoke__">insert</span>(<span class="hljs-string">&quot;Blue&quot;</span>, <span class="hljs-number">20</span>);<span class="hljs-comment">// 此处old会获取上一次的值</span><br>    <span class="hljs-built_in">assert_eq!</span>(old, <span class="hljs-title function_ invoke__">Some</span>(<span class="hljs-number">10</span>));<br><br>    <span class="hljs-comment">// 查询新插入的值</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">new</span> = scores.<span class="hljs-title function_ invoke__">get</span>(<span class="hljs-string">&quot;Blue&quot;</span>);<span class="hljs-comment">// 此处new会获取到更新后的值</span><br>    <span class="hljs-built_in">assert_eq!</span>(new, <span class="hljs-title function_ invoke__">Some</span>(&amp;<span class="hljs-number">20</span>));<br><br>    <span class="hljs-comment">// 查询Yellow对应的值，若不存在则插入新值</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = scores.<span class="hljs-title function_ invoke__">entry</span>(<span class="hljs-string">&quot;Yellow&quot;</span>).<span class="hljs-title function_ invoke__">or_insert</span>(<span class="hljs-number">5</span>);<span class="hljs-comment">// 不存在，所以此处v会获取到插入的值</span><br>    <span class="hljs-built_in">assert_eq!</span>(*v, <span class="hljs-number">5</span>); <span class="hljs-comment">// 不存在，插入5</span><br><br>    <span class="hljs-comment">// 查询Yellow对应的值，若不存在则插入新值</span><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = scores.<span class="hljs-title function_ invoke__">entry</span>(<span class="hljs-string">&quot;Yellow&quot;</span>).<span class="hljs-title function_ invoke__">or_insert</span>(<span class="hljs-number">50</span>);<span class="hljs-comment">// 此处v会获取到存在的值</span><br>    <span class="hljs-built_in">assert_eq!</span>(*v, <span class="hljs-number">5</span>); <span class="hljs-comment">// 已经存在，因此50没有插入</span><br>&#125;<br></code></pre></td></tr></table></figure><h6 id="在已有值的基础上更新"><a href="#在已有值的基础上更新" class="headerlink" title="在已有值的基础上更新"></a>在已有值的基础上更新</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-variable">text</span> = <span class="hljs-string">&quot;hello world wonderful world&quot;</span>;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">map</span> = HashMap::<span class="hljs-title function_ invoke__">new</span>();<br><span class="hljs-comment">// 根据空格来切分字符串(英文单词都是通过空格切分)</span><br><span class="hljs-keyword">for</span> <span class="hljs-variable">word</span> <span class="hljs-keyword">in</span> text.<span class="hljs-title function_ invoke__">split_whitespace</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">count</span> = map.<span class="hljs-title function_ invoke__">entry</span>(word).<span class="hljs-title function_ invoke__">or_insert</span>(<span class="hljs-number">0</span>);<br>    *count += <span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, map);<br></code></pre></td></tr></table></figure><p><strong>注意：</strong></p><ul><li><code>or_insert</code>返回了 <code>&amp;mut v</code>引用，因此可以通过该可变引用来直接修改 <code>map</code>中对应的值</li><li>使用 <code>count</code>引用时，需要先进行解引用 <code>*count</code></li></ul><h5 id="9-2-6-哈希函数"><a href="#9-2-6-哈希函数" class="headerlink" title="9.2.6 哈希函数"></a>9.2.6 哈希函数</h5><p>一个类型需要实现 <code>std::cmp::Eq</code>特征，才可以作为 <code>Key</code>因为需要两个Key可以互相比较看是否相等。</p><p>不同的Key不能映射到相同的值！（这种情况称为哈希碰撞或者散列碰撞，需要用其他的方法来解决，开放寻址法或者链地址法）</p><p>像f32和f64没有实现std::cmp::Eq特征，所以不能用作HashMap的Key</p><blockquote><p>若要追求安全，尽可能减少冲突，同时防止拒绝服务（Denial of Service, DoS）攻击，就要使用密码学安全的哈希函数，<code>HashMap</code> 就是使用了这样的哈希函数。反之若要追求性能，就需要使用没有那么安全的算法。</p></blockquote><h6 id="高性能第三方库"><a href="#高性能第三方库" class="headerlink" title="高性能第三方库"></a>高性能第三方库</h6><blockquote><p>目前，<code>HashMap</code> 使用的哈希函数是 <code>SipHash</code>，它的性能不是很高，但是安全性很高。<code>SipHash</code> 在中等大小的 <code>Key</code> 上，性能相当不错，但是对于小型的 <code>Key</code> （例如整数）或者大型 <code>Key</code> （例如字符串）来说，性能还是不够好。若你需要极致性能，例如实现算法，可以考虑这个库：<a href="https://github.com/tkaitchuck/ahash">ahash</a>。</p></blockquote><hr><h3 id="10-认识生命周期"><a href="#10-认识生命周期" class="headerlink" title="10.认识生命周期"></a>10.认识生命周期</h3><h4 id="⚠TODO！-5"><a href="#⚠TODO！-5" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h4><hr><h3 id="11-返回值和错误处理"><a href="#11-返回值和错误处理" class="headerlink" title="11.返回值和错误处理"></a>11.返回值和错误处理</h3><h4 id="11-1-panic-深入剖析"><a href="#11-1-panic-深入剖析" class="headerlink" title="11.1 panic 深入剖析"></a>11.1 <code>panic</code> 深入剖析</h4><h5 id="11-1-1-panic-与不可恢复错误"><a href="#11-1-1-panic-与不可恢复错误" class="headerlink" title="11.1.1 panic! 与不可恢复错误"></a>11.1.1 <code>panic!</code> 与不可恢复错误</h5><h6 id="被动触发"><a href="#被动触发" class="headerlink" title="被动触发"></a>被动触发</h6><p>如数组越界</p><h6 id="主动调用"><a href="#主动调用" class="headerlink" title="主动调用"></a>主动调用</h6><p>即在代码中主动调用 <code>panic!</code></p><h5 id="11-1-2-backtrace栈展开"><a href="#11-1-2-backtrace栈展开" class="headerlink" title="11.1.2 backtrace栈展开"></a>11.1.2 <code>backtrace</code>栈展开</h5><p>在使用时加上一个环境变量可以获取更详细的栈展开信息：</p><ul><li>Linux&#x2F;macOS 等 UNIX 系统： <code>RUST_BACKTRACE=1 cargo run</code></li><li>Windows 系统（PowerShell）： <code>$env:RUST_BACKTRACE=1 ; cargo run</code></li></ul><p>得到的代码就是依次栈展开（也称栈回溯），包含了函数调用的顺序（按照逆序排列）：最近调用的函数排在列表最上方</p><p>排在最顶部的最后一个调用的函数是 <code>rust_begin_unwind</code></p><p>要获取到栈回溯信息，你还需要开启 <code>debug</code> 标志，该标志在使用 <code>cargo run</code> 或者 <code>cargo build</code> 时自动开启（这两个操作默认是 <code>Debug</code> 运行方式）。同时，栈展开信息在不同操作系统或者 Rust 版本上也有所不同。</p><h5 id="11-1-3-panic-时的两种终止方式"><a href="#11-1-3-panic-时的两种终止方式" class="headerlink" title="11.1.3 panic 时的两种终止方式"></a>11.1.3 <code>panic</code> 时的两种终止方式</h5><ul><li><p>栈展开（默认方式）</p><p>回溯栈上数据和函数调用，因此意味着更多的善后工作，好处是可以给出充分的报错信息和栈调用信息，便于事后的问题复盘</p></li><li><p>直接终止</p><p>不清理数据直接退出程序，善后工作交与操作系统来负责</p><p>当关心最终编译出的二进制可执行文件大小时，可以尝试去使用直接终止的方式，例如下面的配置修改 <code>Cargo.toml</code> 文件，实现在 <code>release</code>手动编译和运行项目 模式下遇到 <code>panic</code> 直接终止：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs toml"><span class="hljs-section">[profile.release]</span><br><span class="hljs-attr">panic</span> = <span class="hljs-string">&#x27;abort&#x27;</span><br></code></pre></td></tr></table></figure></li></ul><h5 id="11-1-4-线程-panic-后，程序是否会终止？"><a href="#11-1-4-线程-panic-后，程序是否会终止？" class="headerlink" title="11.1.4 线程 panic 后，程序是否会终止？"></a>11.1.4 线程 <code>panic</code> 后，程序是否会终止？</h5><p>如果是main线程，则程序终止，如果是其它子线程，该线程终止，但是不会影响main线程，因此不要尽量在main线程中做太多任务，交由子线程去做，这样就算子线程panic也不会导致整个程序结束</p><h5 id="11-1-5-何时该使用-panic"><a href="#11-1-5-何时该使用-panic" class="headerlink" title="11.1.5 何时该使用 panic!"></a>11.1.5 何时该使用 <code>panic!</code></h5><p>首先展示一下Result枚举类型：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Result</span>&lt;T, E&gt; &#123;<br>    <span class="hljs-title function_ invoke__">Ok</span>(T),<br>    <span class="hljs-title function_ invoke__">Err</span>(E),<br>&#125;<br></code></pre></td></tr></table></figure><p>当没有错误发生时，函数返回一个用Reult类型包裹的值Ok(T)，当错误时，返回一个Err(E)，对于Resule返回有很多处理方法，最简单粗暴的就是 <code>unwrap</code>和 <code>expect</code></p><p>以 <code>unwrap</code> 举例：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::net::IpAddr;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">home</span>: IpAddr = <span class="hljs-string">&quot;127.0.0.1&quot;</span>.<span class="hljs-title function_ invoke__">parse</span>().<span class="hljs-title function_ invoke__">unwrap</span>();<br></code></pre></td></tr></table></figure><p>上面的 <code>parse</code> 方法试图将字符串 <code>&quot;127.0.0.1&quot; </code>解析为一个 IP 地址类型 <code>IpAddr</code>，它返回一个 <code>Result&lt;IpAddr, E&gt;</code> 类型，如果解析成功，则把 <code>Ok(IpAddr)</code> 中的值赋给 <code>home</code>，如果失败，则不处理 <code>Err(E)</code>，而是直接 <code>panic</code>。</p><p>因此 <code>unwrap</code> 简而言之：成功则返回值，失败则 <code>panic</code>，总之不进行任何错误处理。</p><p>当代码确定是正确时，可以用 <code>unwrap</code>方法直接处理，因为不可能 <code>panic</code></p><p>如果该字符串是来自于用户输入，那在实际项目中，就必须用错误处理的方式，而不是 <code>unwrap</code>，否则程序崩溃次数无法计量。</p><h6 id="可能导致全局有害状态时"><a href="#可能导致全局有害状态时" class="headerlink" title="可能导致全局有害状态时"></a>可能导致全局有害状态时</h6><p>有害状态大概分为几类：</p><ul><li>非预期的错误</li><li>后续代码的运行会受到显著影响</li><li>内存安全的问题</li></ul><p>当错误预期会出现时，返回一个错误较为合适</p><h5 id="11-1-6-panic-原理分析"><a href="#11-1-6-panic-原理分析" class="headerlink" title="11.1.6 panic 原理分析"></a>11.1.6 <code>panic</code> 原理分析</h5><p>当调用 <code>panic!</code> 宏时，它会</p><ol><li>格式化 <code>panic</code> 信息，然后使用该信息作为参数，调用 <code>std::panic::panic_any()</code> 函数</li><li><code>panic_any</code> 会检查应用是否使用了 <code>panic hook</code> ，如果使用了，该 <code>hook</code> 函数就会被调用（<code>hook</code> 是一个钩子函数，是外部代码设置的，用于在 <code>panic</code> 触发时，执行外部代码所需的功能）</li><li>当 <code>hook</code> 函数返回后，当前的线程就开始进行栈展开：从 <code>panic_any</code> 开始，如果寄存器或者栈因为某些原因信息错乱了，那很可能该展开会发生异常，最终线程会直接停止，展开也无法继续进行</li><li>展开的过程是一帧一帧的去回溯整个栈，每个帧的数据都会随之被丢弃，但是在展开过程中，你可能会遇到被用户标记为 <code>catching</code> 的帧（通过 <code>std::panic::catch_unwind()</code> 函数标记），此时用户提供的 <code>catch</code> 函数会被调用，展开也随之停止：当然，如果 <code>catch</code> 选择在内部调用 <code>std::panic::resume_unwind()</code> 函数，则展开还会继续。</li></ol><p>还有一种情况，在展开过程中，如果展开本身 <code>panic</code> 了，那展开线程会终止，展开也随之停止。</p><p>一旦线程展开被终止或者完成，最终的输出结果是取决于哪个线程 <code>panic</code>：对于 <code>main</code> 线程，操作系统提供的终止功能 <code>core::intrinsics::abort()</code> 会被调用，最终结束当前的 <code>panic</code> 进程；如果是其它子线程，那么子线程就会简单的终止，同时信息会在稍后通过 <code>std::thread::join()</code> 进行收集。</p><h4 id="11-2-返回值-Result-和-可恢复的错误"><a href="#11-2-返回值-Result-和-可恢复的错误" class="headerlink" title="11.2 返回值 Result 和 ? (可恢复的错误)"></a>11.2 返回值 <code>Result</code> 和 <code>?</code> (可恢复的错误)</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">enum</span> <span class="hljs-title class_">Result</span>&lt;T, E&gt; &#123;<br>    <span class="hljs-title function_ invoke__">Ok</span>(T),<br>    <span class="hljs-title function_ invoke__">Err</span>(E),<br>&#125;<br></code></pre></td></tr></table></figure><p>泛型参数 <code>T</code> 代表成功时存入的正确值的类型，存放方式是 <code>Ok(T)</code>，<code>E</code> 代表错误时存入的错误值，存放方式是 <code>Err(E)</code></p><p>例子</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fs::File;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>以上 <code>File::open</code> 返回一个 <code>Result</code> 类型</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fs::File;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>);<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = <span class="hljs-keyword">match</span> f &#123;<br>        <span class="hljs-title function_ invoke__">Ok</span>(file) =&gt; file,<br>        <span class="hljs-title function_ invoke__">Err</span>(error) =&gt; &#123;<br>            <span class="hljs-built_in">panic!</span>(<span class="hljs-string">&quot;Problem opening the file: &#123;:?&#125;&quot;</span>, error)<br>        &#125;,<br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="11-2-1-对返回的错误进行处理"><a href="#11-2-1-对返回的错误进行处理" class="headerlink" title="11.2.1 对返回的错误进行处理"></a>11.2.1 对返回的错误进行处理</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fs::File;<br><span class="hljs-keyword">use</span> std::io::ErrorKind;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>);<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = <span class="hljs-keyword">match</span> f &#123;<br>        <span class="hljs-title function_ invoke__">Ok</span>(file) =&gt; file,<br>        <span class="hljs-title function_ invoke__">Err</span>(error) =&gt; <span class="hljs-keyword">match</span> error.<span class="hljs-title function_ invoke__">kind</span>() &#123;<br>            ErrorKind::NotFound<span class="hljs-comment">/* 文件不存在错误，则创建文件 */</span> =&gt; <span class="hljs-keyword">match</span> File::<span class="hljs-title function_ invoke__">create</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>) &#123;<br>                <span class="hljs-title function_ invoke__">Ok</span>(fc) =&gt; fc,<br>                <span class="hljs-title function_ invoke__">Err</span>(e) =&gt; <span class="hljs-built_in">panic!</span>(<span class="hljs-string">&quot;Problem creating the file: &#123;:?&#125;&quot;</span>, e),<br>            &#125;,<br>            other_error =&gt; <span class="hljs-built_in">panic!</span>(<span class="hljs-string">&quot;Problem opening the file: &#123;:?&#125;&quot;</span>, other_error),<br>        &#125;,<br>    &#125;;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>如果是文件不存在错误 <code>ErrorKind::NotFound</code>，就创建文件，这里创建文件 <code>File::create</code> 也是返回 <code>Result</code>，因此继续用 <code>match</code> 对其结果进行处理：创建成功，将新的文件句柄赋值给 <code>f</code>，如果失败，则 <code>panic</code></li><li>剩下的错误，一律 <code>panic</code></li></ul><h5 id="11-2-2-失败就-panic-unwrap-和-expect"><a href="#11-2-2-失败就-panic-unwrap-和-expect" class="headerlink" title="11.2.2 失败就 panic: unwrap 和 expect"></a>11.2.2 失败就 <code>panic: unwrap</code> 和 <code>expect</code></h5><p><code>unwrap()</code>直接将result中的值取出，如果是 <code>Ok</code>就取出，如果是 <code>Err</code>就直接 <code>panic</code>，直接崩溃</p><p><code>expect</code> 跟 <code>unwrap</code> 很像，也是遇到错误直接 <code>panic</code>, 但是会带上自定义的错误提示信息，相当于重载了错误打印的函数：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fs::File;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>).<span class="hljs-title function_ invoke__">expect</span>(<span class="hljs-string">&quot;Failed to open hello.txt&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p><code>expect</code> 相比 <code>unwrap</code> 能提供更精确的错误信息，在有些场景也会更加实用。</p><h5 id="11-2-3-传播错误"><a href="#11-2-3-传播错误" class="headerlink" title="11.2.3 传播错误"></a>11.2.3 传播错误</h5><p>指被调用的函数将错误一层一层网上传给调用链的上游函数进行处理</p><h6 id=""><a href="#" class="headerlink" title="?"></a><code>?</code></h6><p>Example</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fs::File;<br><span class="hljs-keyword">use</span> std::io;<br><span class="hljs-keyword">use</span> std::io::Read;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">read_username_from_file</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">String</span>, io::Error&gt; &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>)?;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">s</span> = <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">new</span>();<br>    f.<span class="hljs-title function_ invoke__">read_to_string</span>(&amp;<span class="hljs-keyword">mut</span> s)?;<br>    <span class="hljs-title function_ invoke__">Ok</span>(s)<br>&#125;<br></code></pre></td></tr></table></figure><p><code>?</code> 作用就相当于</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">f</span> = <span class="hljs-keyword">match</span> f &#123;<br>    <span class="hljs-comment">// 打开文件成功，将file句柄赋值给f</span><br>    <span class="hljs-title function_ invoke__">Ok</span>(file) =&gt; file,<br>    <span class="hljs-comment">// 打开文件失败，将错误返回(向上传播)</span><br>    <span class="hljs-title function_ invoke__">Err</span>(e) =&gt; <span class="hljs-keyword">return</span> <span class="hljs-title function_ invoke__">Err</span>(e),<br>&#125;;<br></code></pre></td></tr></table></figure><p>如果结果是 <code>Ok(T)</code>，则把 <code>T</code> 赋值给 <code>f</code>，如果结果是 <code>Err(E)</code>，则返回该错误，所以 <code>?</code> 特别适合用来传播错误。</p><p><code>?</code> 也可以自动进行类型提升（转换）</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">open_file</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;File, <span class="hljs-type">Box</span>&lt;<span class="hljs-keyword">dyn</span> std::error::Error&gt;&gt; &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-keyword">mut </span><span class="hljs-variable">f</span> = File::<span class="hljs-title function_ invoke__">open</span>(<span class="hljs-string">&quot;hello.txt&quot;</span>)?;<br>    <span class="hljs-title function_ invoke__">Ok</span>(f)<br>&#125;<br></code></pre></td></tr></table></figure><p>上面代码中 <code>File::open</code> 报错时返回的错误是 <code>std::io::Error</code> 类型，但是 <code>open_file</code> 函数返回的错误类型是 <code>std::error::Error</code> 的特征对象，可以看到一个错误类型通过 <code>?</code> 返回后，变成了另一个错误类型，这就是 <code>?</code> 的神奇之处。</p><p>根本原因是 <code>?</code> 会自动调用 <code>From</code> 特征中的 <code>from</code> 方法，然后进行隐式类型转换，因此只要函数返回的错误 <code>ReturnError</code> 实现了 <code>From&lt;OtherError&gt;</code> 特征， <code>?</code> 就会自动把 <code>OtherError</code> 转换为 <code>ReturnError</code></p><p>与用于返回 <code>Result</code> 的 <code>Err</code> 和 <code>Ok</code> 类似</p><h6 id="也可用于返回-Option-的-Some-和-None"><a href="#也可用于返回-Option-的-Some-和-None" class="headerlink" title="? 也可用于返回 Option 的 Some 和 None"></a><code>?</code> 也可用于返回 <code>Option</code> 的 <code>Some</code> 和 <code>None</code></h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">last_char_of_first_line</span>(text: &amp;<span class="hljs-type">str</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;<span class="hljs-type">char</span>&gt; &#123;<br>    text.<span class="hljs-title function_ invoke__">lines</span>().<span class="hljs-title function_ invoke__">next</span>()?.<span class="hljs-title function_ invoke__">chars</span>().<span class="hljs-title function_ invoke__">last</span>()<br>&#125;<br></code></pre></td></tr></table></figure><p>上面代码展示了在链式调用中使用 <code>?</code> 提前返回 <code>None</code> 的用法， <code>.next</code> 方法返回的是 <code>Option</code> 类型：如果返回 <code>Some(&amp;str)</code>，那么继续调用 <code>chars</code> 方法，如果返回 <code>None</code>，则直接从整个函数中返回 <code>None</code>，不再继续进行链式调用。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">first</span>(arr: &amp;[<span class="hljs-type">i32</span>]) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Option</span>&lt;&amp;<span class="hljs-type">i32</span>&gt; &#123;<br>   arr.<span class="hljs-title function_ invoke__">get</span>(<span class="hljs-number">0</span>)?<br>&#125;<br></code></pre></td></tr></table></figure><p>这段代码无法通过编译，切记：? 操作符需要一个变量来承载正确的值，这个函数只会返回 Some(&amp;i32) 或者 None，只有错误值能直接返回，正确的值不行，所以如果数组中存在 0 号元素，那么函数第二行使用 ? 后的返回类型为 &amp;i32 而不是 Some(&amp;i32)。因此 ? 只能用于以下形式：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-title function_ invoke__">xxx</span>()?;<br><span class="hljs-title function_ invoke__">xxx</span>()?.<span class="hljs-title function_ invoke__">yyy</span>()?;<br></code></pre></td></tr></table></figure><h6 id="try"><a href="#try" class="headerlink" title="try!"></a><code>try!</code></h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">macro_rules!</span> <span class="hljs-keyword">try</span> &#123;<br>    ($e:expr) =&gt; (<span class="hljs-keyword">match</span> $e &#123;<br>        <span class="hljs-title function_ invoke__">Ok</span>(val) =&gt; val,<br>        <span class="hljs-title function_ invoke__">Err</span>(err) =&gt; <span class="hljs-keyword">return</span> <span class="hljs-title function_ invoke__">Err</span>(::std::convert::<span class="hljs-built_in">From</span>::<span class="hljs-title function_ invoke__">from</span>(err)),<br>    &#125;);<br>&#125;<br></code></pre></td></tr></table></figure><p>用法（对比）：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">//  `?`</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-title function_ invoke__">function_with_error</span>()?; <span class="hljs-comment">// 若返回 Err, 则立刻返回；若返回 Ok(255)，则将 x 的值设置为 255</span><br><br><span class="hljs-comment">// `try!()`</span><br><span class="hljs-keyword">let</span> <span class="hljs-variable">x</span> = <span class="hljs-built_in">try!</span>(<span class="hljs-title function_ invoke__">function_with_error</span>());<br></code></pre></td></tr></table></figure><p>但是 <code>try!</code> 要避免使用，因为 <code>?</code> 不仅代码量更少，还可以做链式调用</p><p><code>map</code> &amp; <code>and_then</code></p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">map</span>&lt;U, F&gt;(<span class="hljs-keyword">self</span>, op: F) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;U, E&gt;<br><span class="hljs-keyword">where</span><br>    F: <span class="hljs-title function_ invoke__">FnOnce</span>(T) <span class="hljs-punctuation">-&gt;</span> U,<br></code></pre></td></tr></table></figure><p>Maps a Result&lt;T, E&gt; to Result&lt;U, E&gt; by applying a function to a contained Ok value, leaving an Err value untouched.</p><p>例子</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::num::ParseIntError;<br><br><span class="hljs-comment">// 使用两种方式填空: map, and then</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_two</span>(n_str: &amp;<span class="hljs-type">str</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">i32</span>, ParseIntError&gt; &#123;<br>   n_str.parse::&lt;<span class="hljs-type">i32</span>&gt;().<span class="hljs-title function_ invoke__">map</span>(|x| x+<span class="hljs-number">2</span>)<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">assert_eq!</span>(<span class="hljs-title function_ invoke__">add_two</span>(<span class="hljs-string">&quot;4&quot;</span>).<span class="hljs-title function_ invoke__">unwrap</span>(), <span class="hljs-number">6</span>);<br><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Success!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">and_then</span>&lt;U, F&gt;(<span class="hljs-keyword">self</span>, op: F) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;U, E&gt;<br><span class="hljs-keyword">where</span><br>    F: <span class="hljs-title function_ invoke__">FnOnce</span>(T) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;U, E&gt;,<br></code></pre></td></tr></table></figure><p>Calls op if the result is Ok, otherwise returns the Err value of self.</p><p>This function can be used for control flow based on Result values.</p><p>例子</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::num::ParseIntError;<br><br><span class="hljs-comment">// 使用两种方式填空: map, and then</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_two</span>(n_str: &amp;<span class="hljs-type">str</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">Result</span>&lt;<span class="hljs-type">i32</span>, ParseIntError&gt; &#123;<br>   n_str.parse::&lt;<span class="hljs-type">i32</span>&gt;().<span class="hljs-title function_ invoke__">and_then</span>(|x| <span class="hljs-title function_ invoke__">Ok</span>(x+<span class="hljs-number">2</span>))<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">assert_eq!</span>(<span class="hljs-title function_ invoke__">add_two</span>(<span class="hljs-string">&quot;4&quot;</span>).<span class="hljs-title function_ invoke__">unwrap</span>(), <span class="hljs-number">6</span>);<br><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Success!&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="12-包和模块-Crates-Module"><a href="#12-包和模块-Crates-Module" class="headerlink" title="12.包和模块 Crates &amp; Module"></a>12.包和模块 Crates &amp; Module</h3><blockquote><p>三个基本概念</p><p>项目：Packages 一个Cargo提供的 feature(特征)，可以用于构建、测试和分享包</p><p>包：Crate 一个由多个模块组成的树形结构，可以作为三方库进行分发，也可以生成可执行文件进行运行</p><p>模块：Module 可以一个文件多个模块，也可以一个文件一个模块，模块可以被认为是真实项目中的代码组织单元</p></blockquote><h4 id="12-1-包和-Package"><a href="#12-1-包和-Package" class="headerlink" title="12.1 包和 Package"></a>12.1 包和 Package</h4><blockquote><p>不同包之间可以由同名的类型，对于编译器而言，两者的边界非常清晰，不会存在引用歧义</p></blockquote><p>Crate 被编译后会生成一个可执行文件或者一个库。对于Rust来说，包就是一个独立的可编译单元</p><blockquote><p>由于Rust起名比较标新立异，包的名称被crate所占用，库的名称被library占用，所以Rust圣经将Package翻译成项目，也可以理解为工程、软件包</p></blockquote><p>因为是一个项目，所以Package由独立的Cargo.toml，以及一些被组织在一起的包（一个或多个）。一个Package智能包含一个库（library）类型的包，但是可以包含多个二进制可执行类型的包。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 创建一个库类型的 package</span><br>cargo new my-lib --lib<br><span class="hljs-comment"># 创建一个二进制类型（应用类型）的 package</span><br>cargo new my-project<br></code></pre></td></tr></table></figure><blockquote><p>如果一个Package 包含有 src&#x2F;lib.rs 意味着它包含有一个库类型的同名包，该包的根文件是src&#x2F;lib.rs</p><p>同上，如果一个Package 包含有 src&#x2F;main.rs, 则意味着它有一个二进制类型的同名包</p></blockquote><p>如果一个Package同时含有 src&#x2F;main.rs 和 src&#x2F;lib.rs ，就意味着它包含两个包：库包和二进制包两个包的包名与Package同名</p><p>一般标准Rust项目录结构如下：</p><ul><li>唯一库包：src&#x2F;lib.rs</li><li>默认二进制包：src&#x2F;main.rs</li><li>其余二进制包：src&#x2F;bin&#x2F;*.rs</li><li>集成测试文件：tests目录下</li><li>基准性能测试：benchmark文件：benches目录下</li><li>项目示例：examples目录下</li></ul><h4 id="12-2-模块-Module"><a href="#12-2-模块-Module" class="headerlink" title="12.2 模块 Module"></a>12.2 模块 Module</h4><h5 id="12-2-1-创建嵌套模块"><a href="#12-2-1-创建嵌套模块" class="headerlink" title="12.2.1 创建嵌套模块"></a>12.2.1 创建嵌套模块</h5><p>形如下方代码即为嵌套模块：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// src/lib.rs</span><br><span class="hljs-comment">// 餐厅前厅，用于吃饭</span><br><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_to_waitlist</span>() &#123;&#125;<br><br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">seat_at_table</span>() &#123;&#125;<br>    &#125;<br><br>    <span class="hljs-keyword">mod</span> serving &#123;<br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">take_order</span>() &#123;&#125;<br><br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">serve_order</span>() &#123;&#125;<br><br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">take_payment</span>() &#123;&#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">eat_at_restaurant</span>() &#123;<br>    <span class="hljs-comment">// 绝对路径</span><br>    crate::front_of_house::hosting::<span class="hljs-title function_ invoke__">add_to_waitlist</span>();<br><br>    <span class="hljs-comment">// 相对路径</span><br>    front_of_house::hosting::<span class="hljs-title function_ invoke__">add_to_waitlist</span>();<br>&#125;<br></code></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 上述嵌套模块的结构如下所示</span><br>crate(lib.rs)<br> └── front_of_house<br>     ├── hosting<br>     │   ├── add_to_waitlist<br>     │   └── seat_at_table<br>     └── serving<br>         ├── take_order<br>         ├── serve_order<br>         └── take_payment<br><br></code></pre></td></tr></table></figure><h5 id="12-2-2-用路径引用模块"><a href="#12-2-2-用路径引用模块" class="headerlink" title="12.2.2 用路径引用模块"></a>12.2.2 用路径引用模块</h5><ul><li><strong>绝对路径</strong>，从包根开始，路径名以包名或者 <code>crate</code> 作为开头</li><li><strong>相对路径</strong>，从当前模块开始，以 <code>self</code>，<code>super</code> 或当前模块的标识符作为开头</li></ul><p>上面代码中的 <code>eat_at_restaurant()</code>内部就有[**示例**](#####12.2.1 创建嵌套模块)</p><h5 id="12-2-3-代码可见性"><a href="#12-2-3-代码可见性" class="headerlink" title="12.2.3 代码可见性"></a>12.2.3 代码可见性</h5><p><strong>pub 关键字</strong></p><p>在访问某个模块内部的模块时，因为默认是private,即被隐藏了，要访问需要在该模块前面添加pub关键字（如果内部有函数要访问，内部的函数也一样要添加pub）</p><h5 id="12-2-4-使用-super-引用模块"><a href="#12-2-4-使用-super-引用模块" class="headerlink" title="12.2.4 使用 super 引用模块"></a>12.2.4 使用 <code>super</code> 引用模块</h5><p>在相对路径引用模块的方式中，可以用super来指代父模块，有点类似文件系统中的 <code>..</code>路径</p><h5 id="12-2-5-使用-self-引用模块"><a href="#12-2-5-使用-self-引用模块" class="headerlink" title="12.2.5 使用 self 引用模块"></a>12.2.5 使用 <code>self</code> 引用模块</h5><p>即引用自身模块中的项（自身所在文件中）</p><h5 id="12-2-6-结构体和枚举的可见性"><a href="#12-2-6-结构体和枚举的可见性" class="headerlink" title="12.2.6 结构体和枚举的可见性"></a>12.2.6 结构体和枚举的可见性</h5><ul><li>将结构体设置为 <code>pub</code>，但它的所有字段依然是私有的</li><li>将枚举设置为 <code>pub</code>，它的所有字段也将对外可见</li></ul><blockquote><p>枚举和结构体的使用方式不一样。如果枚举的成员对外不可见，那该枚举将一点用都没有，因此枚举成员的可见性自动跟枚举可见性保持一致，这样可以简化用户的使用。</p><p>而结构体的应用场景比较复杂，其中的字段也往往部分在 A 处被使用，部分在 B 处被使用，因此无法确定成员的可见性，那索性就设置为全部不可见，将选择权交给程序员。</p></blockquote><h5 id="12-2-7-模块与文件分离"><a href="#12-2-7-模块与文件分离" class="headerlink" title="12.2.7 模块与文件分离"></a>12.2.7 模块与文件分离</h5><p>当模块变多或者变大时，需要将模块放入一个单独的文件中，让代码更好维护。</p><p>如果需要将文件夹作为一个模块，我们需要进行显示指定暴露哪些子模块。按照上述的报错信息，我们有两种方法：</p><ul><li>在 <code>divsion_mod</code> 目录里创建一个 <code>mod.rs</code>，如果你使用的 <code>rustc</code> 版本 <code>1.30</code> 之前，这是唯一的方法。</li><li>在 <code>division_mod</code> <strong>同级</strong>目录里创建一个与模块（目录）<strong>同名</strong>的 rs 文件 <code>division_mod.rs</code>，在新版本里，更建议使用这样的命名方式来避免项目中存在大量同名的 <code>mod.rs</code> 文件（ Python 点了个 <code>踩</code>）。</li></ul><p>如果使用第二种方式，文件结构将如下所示：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-attribute">src</span><br>├── division_mod<br>│   └── xxx<span class="hljs-selector-class">.rs</span><br>├── division_mod<span class="hljs-selector-class">.rs</span><br>└── lib.rs<br></code></pre></td></tr></table></figure><p>在上述完成之后，在另一个需要用到的文件里面</p><p>添加</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> division_mod<br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">use</span> crate::division_mod::xxx;<br></code></pre></td></tr></table></figure><ul><li><code>mod division_mod;</code> 告诉 Rust 从另一个和模块 <code>division_mod</code> 同名的文件中加载该模块的内容</li><li>使用绝对路径的方式来引用 <code>xxx</code> 模块：<code>crate::division_mod::xxx;</code></li></ul><h4 id="12-3-use"><a href="#12-3-use" class="headerlink" title="12.3 use"></a>12.3 use</h4><blockquote><p>使用use 可以使代码简化，不需要再用类似</p><p>crate::division_mod::xxx这样那么长的调用方式</p></blockquote><h5 id="12-3-1-基本引入方式"><a href="#12-3-1-基本引入方式" class="headerlink" title="12.3.1 基本引入方式"></a>12.3.1 基本引入方式</h5><p><strong>绝对路径和相对路径</strong></p><blockquote><p>基本与模块相同</p></blockquote><h6 id="引入模块还是函数"><a href="#引入模块还是函数" class="headerlink" title="引入模块还是函数"></a>引入模块还是函数</h6><p>从使用简洁性来说，引入函数自然是更甚一筹，但是在某些时候，引入模块会更好：</p><ul><li>需要引入同一个模块的多个函数</li><li>作用域中存在同名函数</li></ul><p>Rust Course 建议：<strong>优先使用最细粒度（引入函数、结构体等）的引用方式，如果引起了某种麻烦（例如前面两种情况），再使用引入模块的方式</strong>。</p><h5 id="12-3-2-避免同名引用"><a href="#12-3-2-避免同名引用" class="headerlink" title="12.3.2 避免同名引用"></a>12.3.2 避免同名引用</h5><h6 id="模块-函数"><a href="#模块-函数" class="headerlink" title="模块::函数"></a>模块::函数</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fmt<br><span class="hljs-keyword">use</span> std::io<br></code></pre></td></tr></table></figure><h6 id="as-别名引用"><a href="#as-别名引用" class="headerlink" title="as 别名引用"></a>as 别名引用</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fmt<br><span class="hljs-keyword">use</span> std::io::<span class="hljs-type">Result</span> <span class="hljs-keyword">as</span> IoResult;<br><span class="hljs-comment">// 则Result 代表 std::fmt::Result</span><br><span class="hljs-comment">// IoResult 代表 std::io::Result</span><br></code></pre></td></tr></table></figure><h5 id="12-3-3-引入项再导出"><a href="#12-3-3-引入项再导出" class="headerlink" title="12.3.3 引入项再导出"></a>12.3.3 引入项再导出</h5><p>当外部模块项A被引入到当前模块中时，其可见性自动被设置为私有，如果希望允许其他外部代码引用模块项A,可以再次进行导出</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_to_waitlist</span>() &#123;&#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">use</span> crate::front_of_house::hosting; <span class="hljs-comment">// 这里的pub就可以让外部模块使用该模块时，也可以使用</span><br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">eat_at_restaurant</span>() &#123;<br>    hosting::<span class="hljs-title function_ invoke__">add_to_waitlist</span>();<br>    hosting::<span class="hljs-title function_ invoke__">add_to_waitlist</span>();<br>    hosting::<span class="hljs-title function_ invoke__">add_to_waitlist</span>();<br>&#125;<br></code></pre></td></tr></table></figure><p>如上，使用 <code>pub use</code>即可实现目的。</p><p>这里 <code>use</code>代表引入 <code>hosting</code>模块到当前作用域，<code>pub</code>表示将该引入的内容再度设置为可见。</p><h5 id="12-3-4-使用第三方包"><a href="#12-3-4-使用第三方包" class="headerlink" title="12.3.4 使用第三方包"></a>12.3.4 使用第三方包</h5><ol><li>现在 <code>Cargo.toml</code>文件内的 <code>[dependencies]</code>区域添加一行：<code>xxx_mod = &quot;x.x.x&quot;</code></li><li>此时如果IDE有合适的配置，就会自动拉取该库。</li></ol><p>然后在代码中：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> xxx_mod::xxx_func_set;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br><span class="hljs-keyword">let</span> <span class="hljs-variable">xxx_val</span> = xxx_func_inside::<span class="hljs-title function_ invoke__">xxx_func</span>();<br>&#125;<br><span class="hljs-comment">// xxx_func_inside 是xxx_func_set内部的模块</span><br><span class="hljs-comment">// xxx_func 是xxx_func_inside内部的函数</span><br></code></pre></td></tr></table></figure><h6 id="crates-io，lib-rs"><a href="#crates-io，lib-rs" class="headerlink" title="crates.io，lib.rs"></a><code>crates.io</code>，<code>lib.rs</code></h6><p>Rust 社区已经为我们贡献了大量高质量的第三方包，你可以在 <code>crates.io</code> 或者 <code>lib.rs</code> 中检索和使用，从目前来说查找包更推荐 <code>lib.rs</code>，搜索功能更强大，内容展示也更加合理，但是下载依赖包还是得用 <code>crates.io</code>。</p><p>可以在网站上搜索 <code>rand</code> 包，看看它的文档使用方式是否和我们之前引入方式相一致：在网上找到想要的包，然后将你想要的包和版本信息写入到 <code>Cargo.toml</code> 中。</p><h5 id="12-3-5-使用-简化引入方式"><a href="#12-3-5-使用-简化引入方式" class="headerlink" title="12.3.5 使用 {} 简化引入方式"></a>12.3.5 使用 {} 简化引入方式</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><span class="hljs-keyword">use</span> std::collections::BTreeMap;<br><span class="hljs-keyword">use</span> std::collections::HashSet;<br><br><span class="hljs-keyword">use</span> std::cmp::Ordering;<br><span class="hljs-keyword">use</span> std::io;<br></code></pre></td></tr></table></figure><p>优化为</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::&#123;HashMap,BTreeMap,HashSet&#125;;<br><span class="hljs-keyword">use</span> std::&#123;cmp::Ordering, io&#125;;<br></code></pre></td></tr></table></figure><p>然后同时引入模块和模块中的项时：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::io;<br><span class="hljs-keyword">use</span> std::io::Write;<br></code></pre></td></tr></table></figure><p>可以优化为</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::io::&#123;<span class="hljs-keyword">self</span>, Write&#125;;<br></code></pre></td></tr></table></figure><h6 id="self"><a href="#self" class="headerlink" title="self"></a>self</h6><p>上面使用到了模块章节提到的 <code>self</code> 关键字，用来替代模块自身，结合上一节中的 <code>self</code>，可以得出它在模块中的两个用途：</p><ul><li><code>use self::xxx</code>，表示加载当前模块中的 <code>xxx</code>。此时 <code>self</code> 可省略</li><li><code>use xxx::&#123;self, yyy&#125;</code>，表示，加载当前路径下模块 <code>xxx</code> 本身，以及模块 <code>xxx</code> 下的 <code>yyy</code></li></ul><h5 id="12-3-6-使用-引入模块下的所有项"><a href="#12-3-6-使用-引入模块下的所有项" class="headerlink" title="12.3.6 使用*引入模块下的所有项"></a>12.3.6 使用*引入模块下的所有项</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::*<br></code></pre></td></tr></table></figure><p>使用*要小心，因为不知道会不会引用到同名的模块或函数导致冲突，对于编译器来说，本体同名类型的优先级更高</p><h5 id="12-3-7-受限的可见性"><a href="#12-3-7-受限的可见性" class="headerlink" title="12.3.7 受限的可见性"></a>12.3.7 受限的可见性</h5><p>如果我们想要让某一项可以在整个包中都可以被使用，那么有两种办法：</p><ul><li>在包根中定义一个非 <code>pub</code> 类型的 <code>X</code>(父模块的项对子模块都是可见的，因此包根中的项对模块树上的所有模块都可见)</li><li>在子模块中定义一个 <code>pub</code> 类型的 <code>Y</code>，同时通过 <code>use</code> 将其引入到包根</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> a &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> b &#123;<br>        <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">c</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>,crate::X);<br>        &#125;<br><br>        <span class="hljs-meta">#[derive(Debug)]</span><br>        <span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">Y</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-meta">#[derive(Debug)]</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">X</span>;<br><span class="hljs-keyword">use</span> a::b::Y;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">d</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>,Y);<br>&#125;<br></code></pre></td></tr></table></figure><p>有时我们会遇到这两种方法都不太好用的时候。例如希望对于某些特定的模块可见，但是对于其他模块又不可见：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 目标：`a` 导出 `I`、`bar` and `foo`，其他的不导出</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> a &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">const</span> I: <span class="hljs-type">i32</span> = <span class="hljs-number">3</span>;<br><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">semisecret</span>(x: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-keyword">use</span> self::b::c::J;<br>        x + J<br>    &#125;<br><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">bar</span>(z: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-title function_ invoke__">semisecret</span>(I) * z<br>    &#125;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">foo</span>(y: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-title function_ invoke__">semisecret</span>(I) + y<br>    &#125;<br><br>    <span class="hljs-keyword">mod</span> b &#123;<br>        <span class="hljs-keyword">mod</span> c &#123;<br>            <span class="hljs-keyword">const</span> J: <span class="hljs-type">i32</span> = <span class="hljs-number">4</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>子模块看得到父模块的项，父模块看不到子模块的项，所以这段代码会报错，semisecret方法中，<code>a</code> -&gt; <code>b</code> -&gt; <code>c</code> 形成了父子模块链，那 <code>c</code> 中的 <code>J</code> 自然对 <code>a</code> 模块不可见。</p><p>如果想保持代码逻辑，同时又只让 <code>J</code> 在 <code>a</code> 内可见该怎么办？</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> a &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">const</span> I: <span class="hljs-type">i32</span> = <span class="hljs-number">3</span>;<br><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">semisecret</span>(x: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-keyword">use</span> self::b::c::J;<br>        x + J<br>    &#125;<br><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">bar</span>(z: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-title function_ invoke__">semisecret</span>(I) * z<br>    &#125;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">foo</span>(y: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>        <span class="hljs-title function_ invoke__">semisecret</span>(I) + y<br>    &#125;<br><br>    <span class="hljs-keyword">mod</span> b &#123;<br>        <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">in</span> crate::a) <span class="hljs-keyword">mod</span> c &#123;<br>            <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">in</span> crate::a) <span class="hljs-keyword">const</span> J: <span class="hljs-type">i32</span> = <span class="hljs-number">4</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>通过 <code>pub(in crate::a)</code> 的方式，我们指定了模块 <code>c</code> 和常量 <code>J</code> 的可见范围都只是 <code>a</code> 模块中，<code>a</code> 之外的模块是完全访问不到它们的。</p><h6 id="限制可见性语法"><a href="#限制可见性语法" class="headerlink" title="限制可见性语法"></a>限制可见性语法</h6><p><code>pub(crate)</code> 或 <code>pub(in crate::a)</code> 就是限制可见性语法，前者是限制在整个包内可见，后者是通过绝对路径，限制在包内的某个模块内可见，总结一下：</p><ul><li><code>pub</code> 意味着可见性无任何限制</li><li><code>pub(crate)</code> 表示在当前包可见</li><li><code>pub(self)</code> 在当前模块可见</li><li><code>pub(super)</code> 在父模块可见</li><li><code>pub(in &lt;path&gt;)</code> 表示在某个路径代表的模块中可见，其中 <code>path</code> 必须是父模块或者祖先模块</li></ul><h6 id="综合示例"><a href="#综合示例" class="headerlink" title="综合示例"></a>综合示例</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 一个名为 `my_mod` 的模块</span><br><span class="hljs-keyword">mod</span> my_mod &#123;<br>    <span class="hljs-comment">// 模块中的项默认具有私有的可见性</span><br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">private_function</span>() &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::private_function()`&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 使用 `pub` 修饰语来改变默认可见性。</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">function</span>() &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::function()`&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 在同一模块中，项可以访问其它项，即使它是私有的。</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">indirect_access</span>() &#123;<br>        <span class="hljs-built_in">print!</span>(<span class="hljs-string">&quot;called `my_mod::indirect_access()`, that\n&gt; &quot;</span>);<br>        <span class="hljs-title function_ invoke__">private_function</span>();<br>    &#125;<br><br>    <span class="hljs-comment">// 模块也可以嵌套</span><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> nested &#123;<br>        <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">function</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::nested::function()`&quot;</span>);<br>        &#125;<br><br>        <span class="hljs-meta">#[allow(dead_code)]</span><br>        <span class="hljs-keyword">fn</span> <span class="hljs-title function_">private_function</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::nested::private_function()`&quot;</span>);<br>        &#125;<br><br>        <span class="hljs-comment">// 使用 `pub(in path)` 语法定义的函数只在给定的路径中可见。</span><br>        <span class="hljs-comment">// `path` 必须是父模块（parent module）或祖先模块（ancestor module）</span><br>        <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">in</span> crate::my_mod) <span class="hljs-keyword">fn</span> <span class="hljs-title function_">public_function_in_my_mod</span>() &#123;<br>            <span class="hljs-built_in">print!</span>(<span class="hljs-string">&quot;called `my_mod::nested::public_function_in_my_mod()`, that\n &gt; &quot;</span>);<br>            <span class="hljs-title function_ invoke__">public_function_in_nested</span>()<br>        &#125;<br><br>        <span class="hljs-comment">// 使用 `pub(self)` 语法定义的函数则只在当前模块中可见。</span><br>        <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">self</span>) <span class="hljs-keyword">fn</span> <span class="hljs-title function_">public_function_in_nested</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::nested::public_function_in_nested&quot;</span>);<br>        &#125;<br><br>        <span class="hljs-comment">// 使用 `pub(super)` 语法定义的函数只在父模块中可见。</span><br>        <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">super</span>) <span class="hljs-keyword">fn</span> <span class="hljs-title function_">public_function_in_super_mod</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called my_mod::nested::public_function_in_super_mod&quot;</span>);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">call_public_function_in_my_mod</span>() &#123;<br>        <span class="hljs-built_in">print!</span>(<span class="hljs-string">&quot;called `my_mod::call_public_funcion_in_my_mod()`, that\n&gt; &quot;</span>);<br>        nested::<span class="hljs-title function_ invoke__">public_function_in_my_mod</span>();<br>        <span class="hljs-built_in">print!</span>(<span class="hljs-string">&quot;&gt; &quot;</span>);<br>        nested::<span class="hljs-title function_ invoke__">public_function_in_super_mod</span>();<br>    &#125;<br><br>    <span class="hljs-comment">// `pub(crate)` 使得函数只在当前包中可见</span><br>    <span class="hljs-title function_ invoke__">pub</span>(<span class="hljs-keyword">crate</span>) <span class="hljs-keyword">fn</span> <span class="hljs-title function_">public_function_in_crate</span>() &#123;<br>        <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::public_function_in_crate()&quot;</span>);<br>    &#125;<br><br>    <span class="hljs-comment">// 嵌套模块的可见性遵循相同的规则</span><br>    <span class="hljs-keyword">mod</span> private_nested &#123;<br>        <span class="hljs-meta">#[allow(dead_code)]</span><br>        <span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">function</span>() &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `my_mod::private_nested::function()`&quot;</span>);<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">function</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;called `function()`&quot;</span>);<br>&#125;<br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 模块机制消除了相同名字的项之间的歧义。</span><br>    <span class="hljs-title function_ invoke__">function</span>();<br>    my_mod::<span class="hljs-title function_ invoke__">function</span>();<br><br>    <span class="hljs-comment">// 公有项，包括嵌套模块内的，都可以在父模块外部访问。</span><br>    my_mod::<span class="hljs-title function_ invoke__">indirect_access</span>();<br>    my_mod::nested::<span class="hljs-title function_ invoke__">function</span>();<br>    my_mod::<span class="hljs-title function_ invoke__">call_public_function_in_my_mod</span>();<br><br>    <span class="hljs-comment">// pub(crate) 项可以在同一个 crate 中的任何地方访问</span><br>    my_mod::<span class="hljs-title function_ invoke__">public_function_in_crate</span>();<br><br>    <span class="hljs-comment">// pub(in path) 项只能在指定的模块中访问</span><br>    <span class="hljs-comment">// 报错！函数 `public_function_in_my_mod` 是私有的</span><br>    <span class="hljs-comment">//my_mod::nested::public_function_in_my_mod();</span><br>    <span class="hljs-comment">// 试一试 ^ 取消该行的注释</span><br><br>    <span class="hljs-comment">// 模块的私有项不能直接访问，即便它是嵌套在公有模块内部的</span><br><br>    <span class="hljs-comment">// 报错！`private_function` 是私有的</span><br>    <span class="hljs-comment">//my_mod::private_function();</span><br>    <span class="hljs-comment">// 试一试 ^ 取消此行注释</span><br><br>    <span class="hljs-comment">// 报错！`private_function` 是私有的</span><br>    <span class="hljs-comment">//my_mod::nested::private_function();</span><br>    <span class="hljs-comment">// 试一试 ^ 取消此行的注释</span><br><br>    <span class="hljs-comment">// 报错！ `private_nested` 是私有的</span><br>    <span class="hljs-comment">//my_mod::private_nested::function();</span><br>    <span class="hljs-comment">// 试一试 ^ 取消此行的注释</span><br>&#125;<br></code></pre></td></tr></table></figure><hr><h3 id="13-注释和文档"><a href="#13-注释和文档" class="headerlink" title="13.注释和文档"></a>13.注释和文档</h3><h4 id="13-1-注释的种类"><a href="#13-1-注释的种类" class="headerlink" title="13.1 注释的种类"></a>13.1 注释的种类</h4><ul><li>代码注释：用于说明某一块代码的功能</li><li>文档注释：支持Markdown</li><li>包和模块注释：主要用于说明当前包和模块的功能</li></ul><h4 id="13-2-代码注释"><a href="#13-2-代码注释" class="headerlink" title="13.2 代码注释"></a>13.2 代码注释</h4><ul><li><p>行注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">//</span><br></code></pre></td></tr></table></figure></li><li><p>块注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">/* ... */</span><br></code></pre></td></tr></table></figure></li></ul><h4 id="13-3-文档注释"><a href="#13-3-文档注释" class="headerlink" title="13.3 文档注释"></a>13.3 文档注释</h4><ul><li><p>文档行注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">///</span><br></code></pre></td></tr></table></figure></li><li><p>文档块注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">/** ... */</span><br></code></pre></td></tr></table></figure></li></ul><h5 id="13-3-1-查看文档"><a href="#13-3-1-查看文档" class="headerlink" title="13.3.1 查看文档"></a>13.3.1 查看文档</h5><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">cargo doc<br></code></pre></td></tr></table></figure><p>执行如上命令会直接生成 <code>HTML</code>文件，放在 <code>target/doc</code>目录下</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">cargo doc --open<br><span class="hljs-comment"># 在生成文档后，自动在浏览器中打开网页</span><br></code></pre></td></tr></table></figure><h4 id="13-4-包和模块级别的注释"><a href="#13-4-包和模块级别的注释" class="headerlink" title="13.4 包和模块级别的注释"></a>13.4 包和模块级别的注释</h4><ul><li><p>行注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">//!</span><br></code></pre></td></tr></table></figure></li><li><p>块注释</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">/*! ... */</span><br></code></pre></td></tr></table></figure></li></ul><p><strong>注意这些注释要放到包和模块的最上方！！！</strong></p><h4 id="13-5-文档测试（Doc-Test）"><a href="#13-5-文档测试（Doc-Test）" class="headerlink" title="13.5 文档测试（Doc Test）"></a>13.5 文档测试（Doc Test）</h4><p>Rust允许在文档注释中编写单元用例测试，如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">/// `add_one` 将指定值加1</span><br><span class="hljs-comment">///</span><br><span class="hljs-comment">/// # Examples11</span><br><span class="hljs-comment">///</span><br><span class="hljs-comment">/// ```</span><br><span class="hljs-comment">/// let arg = 5;</span><br><span class="hljs-comment">/// let answer = world_hello::compute::add_one(arg);</span><br><span class="hljs-comment">///</span><br><span class="hljs-comment">/// assert_eq!(6, answer);</span><br><span class="hljs-comment">/// ```</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">fn</span> <span class="hljs-title function_">add_one</span>(x: <span class="hljs-type">i32</span>) <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">i32</span> &#123;<br>    x + <span class="hljs-number">1</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="13-6-文档搜索别名"><a href="#13-6-文档搜索别名" class="headerlink" title="13.6 文档搜索别名"></a>13.6 文档搜索别名</h4><p>Rust 文档支持搜索功能，我们可以为自己的类型定义几个别名，以实现更好的搜索展现（在Rust doc网页中），当别名命中时，搜索结果会被放在第一位：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#[doc(alias = <span class="hljs-string">&quot;x&quot;</span>)]</span><br><span class="hljs-meta">#[doc(alias = <span class="hljs-string">&quot;big&quot;</span>)]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">BigX</span>;<br><br><span class="hljs-meta">#[doc(alias(<span class="hljs-string">&quot;y&quot;</span>, <span class="hljs-string">&quot;big&quot;</span>))]</span><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">struct</span> <span class="hljs-title class_">BigY</span>;<br></code></pre></td></tr></table></figure><hr><h3 id="14-格式化输出"><a href="#14-格式化输出" class="headerlink" title="14.格式化输出"></a>14.格式化输出</h3><h6 id="简要示例"><a href="#简要示例" class="headerlink" title="简要示例"></a>简要示例</h6><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello&quot;</span>);                 <span class="hljs-comment">// =&gt; &quot;Hello&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello, &#123;&#125;!&quot;</span>, <span class="hljs-string">&quot;world&quot;</span>);   <span class="hljs-comment">// =&gt; &quot;Hello, world!&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The number is &#123;&#125;&quot;</span>, <span class="hljs-number">1</span>);   <span class="hljs-comment">// =&gt; &quot;The number is 1&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, (<span class="hljs-number">3</span>, <span class="hljs-number">4</span>));          <span class="hljs-comment">// =&gt; &quot;(3, 4)&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;value&#125;&quot;</span>, value=<span class="hljs-number">4</span>);      <span class="hljs-comment">// =&gt; &quot;4&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125; &#123;&#125;&quot;</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>);           <span class="hljs-comment">// =&gt; &quot;1 2&quot;</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:04&#125;&quot;</span>, <span class="hljs-number">42</span>);             <span class="hljs-comment">// =&gt; &quot;0042&quot; with leading zeros</span><br></code></pre></td></tr></table></figure><h4 id="14-1-print-println-format"><a href="#14-1-print-println-format" class="headerlink" title="14.1 print!, println!, format!"></a>14.1 <code>print!</code>, <code>println!</code>, <code>format!</code></h4><ul><li><code>print!</code> 将格式化文本输出到标准输出，不带换行符</li><li><code>println!</code> 同上，但是在行的末尾添加换行符</li><li><code>format!</code> 将格式化文本输出到 <code>String</code> 字符串</li></ul><h5 id="14-1-1-eprint-eprintln"><a href="#14-1-1-eprint-eprintln" class="headerlink" title="14.1.1 eprint!, eprintln!"></a>14.1.1 <code>eprint!</code>, <code>eprintln!</code></h5><blockquote><p>它们仅应该被用于输出错误信息和进度信息，其它场景都应该使用 <code>print!</code> 系列。</p></blockquote><h4 id="14-2-与"><a href="#14-2-与" class="headerlink" title="14.2 {} 与"></a>14.2 {} 与</h4><p>与 <code>&#123;&#125;</code> 类似，<code>&#123;:?&#125;</code> 也是占位符：</p><ul><li><code>&#123;&#125;</code> 适用于实现了 <code>std::fmt::Display</code> 特征的类型，用来以更优雅、更友好的方式格式化文本，例如展示给用户</li><li><code>&#123;:?&#125;</code> 适用于实现了 <code>std::fmt::Debug</code> 特征的类型，用于调试场景</li></ul><p>与大部分类型实现了 <code>Debug</code> 不同，实现了 <code>Display</code> 特征的 Rust 类型并没有那么多，往往需要我们自定义想要的格式化方式</p><p><code>&#123;:#?&#125;</code> 与 <code>&#123;:?&#125;</code> 几乎一样，唯一的区别在于它能更优美地输出内容</p><p>对于 <code>Display</code> 不支持的类型，可以考虑使用 <code>&#123;:#?&#125;</code> 进行格式化，虽然理论上它更适合进行调试输出。</p><h5 id="14-2-1-为自定义类型实现Display特征"><a href="#14-2-1-为自定义类型实现Display特征" class="headerlink" title="14.2.1 为自定义类型实现Display特征"></a>14.2.1 为自定义类型实现Display特征</h5><p>如果类型是定义在当前作用域，可以为其实现 <code>Display</code>特征，可用于格式化输出：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Person</span> &#123;<br>    name: <span class="hljs-type">String</span>,<br>    age: <span class="hljs-type">u8</span>,<br>&#125;<br><br><span class="hljs-keyword">use</span> std::fmt;<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">fmt</span>::Display <span class="hljs-keyword">for</span> <span class="hljs-title class_">Person</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fmt</span>(&amp;<span class="hljs-keyword">self</span>, f: &amp;<span class="hljs-keyword">mut</span> fmt::Formatter) <span class="hljs-punctuation">-&gt;</span> fmt::<span class="hljs-type">Result</span> &#123;<br>        <span class="hljs-built_in">write!</span>(<br>            f,<br>            <span class="hljs-string">&quot;大佬在上，请受我一拜，小弟姓名&#123;&#125;，年芳&#123;&#125;，家里无田又无车，生活苦哈哈&quot;</span>,<br>            <span class="hljs-keyword">self</span>.name, <span class="hljs-keyword">self</span>.age<br>        )<br>    &#125;<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">p</span> = Person &#123;<br>        name: <span class="hljs-string">&quot;sunface&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>(),<br>        age: <span class="hljs-number">18</span>,<br>    &#125;;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, p);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-2-2-为外部类型实现Display特征"><a href="#14-2-2-为外部类型实现Display特征" class="headerlink" title="14.2.2 为外部类型实现Display特征"></a>14.2.2 为外部类型实现Display特征</h5><p>在 Rust 中，无法直接为外部类型实现外部特征，但是可以使用 <code>newtype</code>解决此问题（孤儿规则）：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Array</span>(<span class="hljs-type">Vec</span>&lt;<span class="hljs-type">i32</span>&gt;);<br><br><span class="hljs-keyword">use</span> std::fmt;<br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">fmt</span>::Display <span class="hljs-keyword">for</span> <span class="hljs-title class_">Array</span> &#123;<br>    <span class="hljs-keyword">fn</span> <span class="hljs-title function_">fmt</span>(&amp;<span class="hljs-keyword">self</span>, f: &amp;<span class="hljs-keyword">mut</span> fmt::Formatter) <span class="hljs-punctuation">-&gt;</span> fmt::<span class="hljs-type">Result</span> &#123;<br>        <span class="hljs-built_in">write!</span>(f, <span class="hljs-string">&quot;数组是：&#123;:?&#125;&quot;</span>, <span class="hljs-keyword">self</span>.<span class="hljs-number">0</span>)<br>    &#125;<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">arr</span> = <span class="hljs-title function_ invoke__">Array</span>(<span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, arr);<br>&#125;<br></code></pre></td></tr></table></figure><h4 id="14-3-位置参数"><a href="#14-3-位置参数" class="headerlink" title="14.3 位置参数"></a>14.3 位置参数</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&#123;&#125;&quot;</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>); <span class="hljs-comment">// =&gt;&quot;12&quot;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;1&#125;&#123;0&#125;&quot;</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>); <span class="hljs-comment">// =&gt;&quot;21&quot;</span><br>    <span class="hljs-comment">// =&gt; Alice, this is Bob. Bob, this is Alice</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;0&#125;, this is &#123;1&#125;. &#123;1&#125;, this is &#123;0&#125;&quot;</span>, <span class="hljs-string">&quot;Alice&quot;</span>, <span class="hljs-string">&quot;Bob&quot;</span>);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;1&#125;&#123;&#125;&#123;0&#125;&#123;&#125;&quot;</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>); <span class="hljs-comment">// =&gt; 2112</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="14-4-具名参数"><a href="#14-4-具名参数" class="headerlink" title="14.4 具名参数"></a>14.4 具名参数</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;argument&#125;&quot;</span>, argument = <span class="hljs-string">&quot;test&quot;</span>); <span class="hljs-comment">// =&gt; &quot;test&quot;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;name&#125; &#123;&#125;&quot;</span>, <span class="hljs-number">1</span>, name = <span class="hljs-number">2</span>); <span class="hljs-comment">// =&gt; &quot;2 1&quot;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;a&#125; &#123;c&#125; &#123;b&#125;&quot;</span>, a = <span class="hljs-string">&quot;a&quot;</span>, b = <span class="hljs-string">&#x27;b&#x27;</span>, c = <span class="hljs-number">3</span>); <span class="hljs-comment">// =&gt; &quot;a 3 b&quot;</span><br>&#125;<br></code></pre></td></tr></table></figure><p>需要注意的是：<strong>带名称的参数必须放在不带名称参数的后面</strong>，例如下面代码将报错：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;abc&#125; &#123;1&#125;&quot;</span>, abc = <span class="hljs-string">&quot;def&quot;</span>, <span class="hljs-number">2</span>);<br></code></pre></td></tr></table></figure><h4 id="14-5-格式化参数"><a href="#14-5-格式化参数" class="headerlink" title="14.5 格式化参数"></a>14.5 格式化参数</h4><blockquote><p>对输出有更多的要求</p><p><strong>可以观察到下面全部代码 <code>&#123;&#125;</code>占位符内都加上了 <code>:</code></strong></p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-number">3.1415926</span>;<br>    <span class="hljs-comment">// Display =&gt; 3.14</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.2&#125;&quot;</span>, v);<br>    <span class="hljs-comment">// Debug =&gt; 3.14</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.2?&#125;&quot;</span>, v);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-1-宽度"><a href="#14-5-1-宽度" class="headerlink" title="14.5.1 宽度"></a>14.5.1 宽度</h5><p>宽度用来指示输出目标的长度，如果长度不够，则进行填充和对齐：</p><h6 id="字符串填充"><a href="#字符串填充" class="headerlink" title="字符串填充"></a>字符串填充</h6><blockquote><p>字符串格式化默认使用空格进行填充，并且进行左对齐，长度超过的话无视要求的对齐长度（按原格式继续输出）</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">//-----------------------------------</span><br>    <span class="hljs-comment">// 以下全部输出 &quot;Hello x    !&quot;</span><br>    <span class="hljs-comment">// 为&quot;x&quot;后面填充空格，补齐宽度5</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:5&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>);<br>    <span class="hljs-comment">// 使用参数5来指定宽度</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:1$&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>, <span class="hljs-number">5</span>);<br>    <span class="hljs-comment">// 使用x作为占位符输出内容，同时使用5作为宽度</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;1:0$&#125;!&quot;</span>, <span class="hljs-number">5</span>, <span class="hljs-string">&quot;x&quot;</span>);<br>    <span class="hljs-comment">// 使用有名称的参数作为宽度</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:width$&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>, width = <span class="hljs-number">5</span>);<br>    <span class="hljs-comment">//-----------------------------------</span><br><br>    <span class="hljs-comment">// 使用参数5为参数x指定宽度，同时在结尾输出参数5 =&gt; Hello x    !5</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:1$&#125;!&#123;&#125;&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>, <span class="hljs-number">5</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h6 id="数字填充：符号和0"><a href="#数字填充：符号和0" class="headerlink" title="数字填充：符号和0"></a>数字填充：符号和0</h6><blockquote><p>数字格式化默认也是使用空格进行填充，但与字符串左对齐不同的是，数字是右对齐。</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 宽度是5 =&gt; Hello     5!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:5&#125;!&quot;</span>, <span class="hljs-number">5</span>);<br>    <span class="hljs-comment">// 显式的输出正号 =&gt; Hello +5!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:+&#125;!&quot;</span>, <span class="hljs-number">5</span>);<br>    <span class="hljs-comment">// 宽度5，使用0进行填充 =&gt; Hello 00005!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:05&#125;!&quot;</span>, <span class="hljs-number">5</span>);<br>    <span class="hljs-comment">// 负号也要占用一位宽度 =&gt; Hello -0005!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:05&#125;!&quot;</span>, -<span class="hljs-number">5</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-2-对齐"><a href="#14-5-2-对齐" class="headerlink" title="14.5.2 对齐"></a>14.5.2 对齐</h5><p>左对齐就是把原格式往左边顶，右边进来补齐的内容</p><p>右对齐就是把原格式往右边顶，左边进来补齐的内容</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 以下全部都会补齐5个字符的长度</span><br>    <span class="hljs-comment">// 左对齐 =&gt; Hello x    !</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:&lt;5&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>);<br>    <span class="hljs-comment">// 右对齐 =&gt; Hello     x!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:&gt;5&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>);<br>    <span class="hljs-comment">// 居中对齐 =&gt; Hello   x  !</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:^5&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>);<br><br>    <span class="hljs-comment">// 对齐并使用指定符号填充 =&gt; Hello x&amp;&amp;&amp;&amp;!</span><br>    <span class="hljs-comment">// 指定符号填充的前提条件是必须有对齐字符</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:&amp;&lt;5&#125;!&quot;</span>, <span class="hljs-string">&quot;x&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-3-精度"><a href="#14-5-3-精度" class="headerlink" title="14.5.3 精度"></a>14.5.3 精度</h5><blockquote><p>控制浮点数的精度或者字符串的长度</p></blockquote><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">v</span> = <span class="hljs-number">3.1415926</span>;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">miv</span> = -<span class="hljs-number">3.1415926</span>;<br>    <span class="hljs-comment">// 保留小数点后两位 =&gt; 3.14</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.2&#125;&quot;</span>, v);<br>    <span class="hljs-comment">// 带符号保留小数点后两位 =&gt; +3.14</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:+.2&#125;&quot;</span>, v);<br>    <span class="hljs-comment">// =&gt; -3.14</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:+.2&#125;&quot;</span>, miv)<br>    <span class="hljs-comment">// 不带小数 =&gt; 3</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.0&#125;&quot;</span>, v);<br>    <span class="hljs-comment">// 通过参数来设定精度 =&gt; 3.1416，相当于&#123;:.4&#125;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.1$&#125;&quot;</span>, v, <span class="hljs-number">4</span>);<br><br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">s</span> = <span class="hljs-string">&quot;hi我是Sunface孙飞&quot;</span>;<br>    <span class="hljs-comment">// 保留字符串前三个字符 =&gt; hi我</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:.3&#125;&quot;</span>, s);<br>    <span class="hljs-comment">// &#123;:.*&#125;接收两个参数，第一个是精度，第二个是被格式化的值 =&gt; Hello abc!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello &#123;:.*&#125;!&quot;</span>, <span class="hljs-number">3</span>, <span class="hljs-string">&quot;abcdefg&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-3-进制"><a href="#14-5-3-进制" class="headerlink" title="14.5.3 进制"></a>14.5.3 进制</h5><p>可以使用 <code>#</code> 号来控制数字的进制输出：</p><ul><li><code>#b</code>, 二进制</li><li><code>#o</code>, 八进制</li><li><code>#x</code>, 小写十六进制</li><li><code>#X</code>, 大写十六进制</li><li><code>x</code>, 不带前缀的小写十六进制</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 二进制 =&gt; 0b11011!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:#b&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br>    <span class="hljs-comment">// 八进制 =&gt; 0o33!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:#o&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br>    <span class="hljs-comment">// 十进制 =&gt; 27!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br>    <span class="hljs-comment">// 小写十六进制 =&gt; 0x1b!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:#x&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br>    <span class="hljs-comment">// 大写十六进制 =&gt; 0x1B!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:#X&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br><br>    <span class="hljs-comment">// 不带前缀的十六进制 =&gt; 1b!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:x&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br><br>    <span class="hljs-comment">// 使用0填充二进制，宽度为10 =&gt; 0b00011011!</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:#010b&#125;!&quot;</span>, <span class="hljs-number">27</span>);<br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-4-指数"><a href="#14-5-4-指数" class="headerlink" title="14.5.4 指数"></a>14.5.4 指数</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// 数字代表限定最小宽度，默认会用空格补齐右对齐</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:5e&#125;&quot;</span>, <span class="hljs-number">0.01</span>); <span class="hljs-comment">// =&gt; &quot; 1e-2&quot;</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:2E&#125;&quot;</span>, <span class="hljs-number">1000000000</span>); <span class="hljs-comment">// =&gt; 1E9</span><br>&#125;<br></code></pre></td></tr></table></figure><h5 id="14-5-5-指针地址"><a href="#14-5-5-指针地址" class="headerlink" title="14.5.5 指针地址"></a>14.5.5 指针地址</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-variable">v</span>= <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:p&#125;&quot;</span>, v.<span class="hljs-title function_ invoke__">as_ptr</span>()) <span class="hljs-comment">// =&gt; 0x600002324050</span><br></code></pre></td></tr></table></figure><h5 id="14-5-6-转义"><a href="#14-5-6-转义" class="headerlink" title="14.5.6 转义"></a>14.5.6 转义</h5><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-comment">// &quot;&#123;&#123;&quot; 转义为 &#x27;&#123;&#x27;   &quot;&#125;&#125;&quot; 转义为 &#x27;&#125;&#x27;   &quot;\&quot;&quot; 转义为 &#x27;&quot;&#x27;</span><br>    <span class="hljs-comment">// =&gt; Hello &quot;&#123;World&#125;&quot; </span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot; Hello \&quot;&#123;&#123;World&#125;&#125;\&quot; &quot;</span>);<br><br>    <span class="hljs-comment">// 下面代码会报错，因为占位符&#123;&#125;只有一个右括号&#125;，左括号被转义成字符串的内容</span><br>    <span class="hljs-comment">// println!(&quot; &#123;&#123; Hello &#125; &quot;);</span><br>    <span class="hljs-comment">// 也不可使用 &#x27;\&#x27; 来转义 &quot;&#123;&#125;&quot;</span><br>    <span class="hljs-comment">// println!(&quot; \&#123; Hello \&#125; &quot;)</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="14-6-在格式化字符串时捕获环境中的值-Rust-1-58之后"><a href="#14-6-在格式化字符串时捕获环境中的值-Rust-1-58之后" class="headerlink" title="14.6 在格式化字符串时捕获环境中的值(Rust 1.58之后)"></a>14.6 在格式化字符串时捕获环境中的值(Rust 1.58之后)</h4><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_person</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>    <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;sunface&quot;</span>)<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">person</span> = <span class="hljs-title function_ invoke__">get_person</span>();<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;Hello, &#123;person&#125;!&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>将环境中的值用于格式化参数:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> (width, precision) = <span class="hljs-title function_ invoke__">get_format</span>();<br><span class="hljs-keyword">for</span> (name, score) <span class="hljs-keyword">in</span> <span class="hljs-title function_ invoke__">get_scores</span>() &#123;<br>  <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;name&#125;: &#123;score:width$.precision$&#125;&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>但也有局限，它只能捕获普通的变量，对于更复杂的类型（例如表达式），可以先将它赋值给一个变量或使用以前的 <code>name = expression</code> 形式的格式化参数。 目前除了 <code>panic!</code> 外，其它接收格式化参数的宏，都可以使用新的特性。对于 <code>panic!</code> 而言，如果还在使用 <code>2015版本</code> 或 <code>2018版本</code>，那 <code>panic!(&quot;&#123;ident&#125;&quot;)</code> 依然会被当成 正常的字符串来处理，同时编译器会给予 <code>warn</code> 提示。而对于 <code>2021版本</code> ，则可以正常使用:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">fn</span> <span class="hljs-title function_">get_person</span>() <span class="hljs-punctuation">-&gt;</span> <span class="hljs-type">String</span> &#123;<br>    <span class="hljs-type">String</span>::<span class="hljs-title function_ invoke__">from</span>(<span class="hljs-string">&quot;sunface&quot;</span>)<br>&#125;<br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-variable">person</span> = <span class="hljs-title function_ invoke__">get_person</span>();<br>    <span class="hljs-built_in">panic!</span>(<span class="hljs-string">&quot;Hello, &#123;person&#125;!&quot;</span>);<br>&#125;<br></code></pre></td></tr></table></figure><p>输出:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">thread <span class="hljs-string">&#x27;main&#x27;</span> panicked at <span class="hljs-string">&#x27;Hello, sunface!&#x27;</span>, src/main.rs:6:5<br>note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace<br></code></pre></td></tr></table></figure><hr><h3 id="♾️-一些比较有意思的题目"><a href="#♾️-一些比较有意思的题目" class="headerlink" title="♾️.一些比较有意思的题目"></a>♾️.一些比较有意思的题目</h3><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-meta">#![allow(incomplete_features)]</span><br><span class="hljs-meta">#![feature(generic_const_exprs)]</span><br><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">check_size</span>&lt;T&gt;(val: T)<br><span class="hljs-comment">// 此处会检查 where 后面的表达式，即Assert&lt;xxx&gt;是否实现了 IsTrue 特征，而最下面的代码IsTrue只为Assert&lt;true&gt;实现了特征，也就是说，当内部表达式为false时，编译器会报错也即size_of&lt;T&gt; &gt;= 768时，会报错</span><br><span class="hljs-comment">// 由最下方Assert的枚举实现代码可知，Assert 接收一个常量 bool 值</span><br><span class="hljs-keyword">where</span><br>    Assert&lt;&#123; core::mem::size_of::&lt;T&gt;() &lt; <span class="hljs-number">768</span> &#125;&gt;: IsTrue,<br>&#123;<br>    <span class="hljs-comment">//...</span><br>&#125;<br><br><span class="hljs-comment">// fix the errors in main</span><br><span class="hljs-keyword">fn</span> <span class="hljs-title function_">main</span>() &#123;<br>    <span class="hljs-title function_ invoke__">check_size</span>([<span class="hljs-number">0u8</span>; <span class="hljs-number">767</span>]); <br>    <span class="hljs-title function_ invoke__">check_size</span>([<span class="hljs-number">0i32</span>; <span class="hljs-number">191</span>]);<br>    <span class="hljs-title function_ invoke__">check_size</span>([<span class="hljs-string">&quot;hello你好&quot;</span>; <span class="hljs-number">47</span>]); <span class="hljs-comment">// &amp;str is a string reference, containing a pointer and string length in it, so it takes two word long, in x86-64, 1 word = 8 bytes</span><br>    <span class="hljs-title function_ invoke__">check_size</span>([(); <span class="hljs-number">31</span>].<span class="hljs-title function_ invoke__">map</span>(|_| <span class="hljs-string">&quot;hello你好&quot;</span>.<span class="hljs-title function_ invoke__">to_string</span>()));  <span class="hljs-comment">// String is a smart pointer struct, it has three fields: pointer, length and capacity, each takes 8 bytes</span><br>    <span class="hljs-title function_ invoke__">check_size</span>([<span class="hljs-string">&#x27;中&#x27;</span>; <span class="hljs-number">191</span>]); <span class="hljs-comment">// A char takes 4 bytes in Rust</span><br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">enum</span> <span class="hljs-title class_">Assert</span>&lt;<span class="hljs-keyword">const</span> CHECK: <span class="hljs-type">bool</span>&gt; &#123;&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">trait</span> <span class="hljs-title class_">IsTrue</span> &#123;&#125;<br><br><span class="hljs-keyword">impl</span> <span class="hljs-title class_">IsTrue</span> <span class="hljs-keyword">for</span> <span class="hljs-title class_">Assert</span>&lt;<span class="hljs-literal">true</span>&gt; &#123;&#125;<br></code></pre></td></tr></table></figure><hr><h2 id="一、进阶"><a href="#一、进阶" class="headerlink" title="一、进阶"></a>一、进阶</h2><h3 id="Arc"><a href="#Arc" class="headerlink" title="Arc"></a>Arc</h3><p><code>Arc</code>：用于在多个线程间共享数据，确保数据的引用计数是线程安全的，但不提供修改数据的能力。</p><h3 id="Mutex"><a href="#Mutex" class="headerlink" title="Mutex"></a>Mutex</h3><p><code>Mutex</code>：用于确保多个线程对同一数据的访问是互斥的，确保同一时刻只有一个线程能修改数据。</p><h3 id="RwLock-vs-Mutex-的区别"><a href="#RwLock-vs-Mutex-的区别" class="headerlink" title="RwLock vs Mutex 的区别"></a><code>RwLock</code> vs <code>Mutex</code> 的区别</h3><ol><li><strong>锁的类型</strong>：<ul><li><code>Mutex</code>：是 <strong>互斥锁</strong>，它在任何时刻只允许一个线程访问数据，不管是读取还是写入。如果一个线程持有 <code>Mutex</code>，其他线程就无法访问该资源，直到锁被释放。</li><li><code>RwLock</code>：是 <strong>读写锁</strong>，它允许<strong>多个线程同时读取</strong>数据，但是<strong>写操作</strong>是互斥的，即同一时刻只能有一个线程进行写操作，且写操作不能与任何读操作并发执行。</li></ul></li><li><strong>适用场景</strong>：<ul><li><code>Mutex</code>：适用于只有少量线程需要修改共享数据，或者对数据的修改较为频繁的情况。</li><li><code>RwLock</code>：适用于数据的<strong>读取远多于写入</strong>的场景，能够提供更高的并发性，允许多个线程同时读取，而不会阻塞读线程。只有当一个线程需要修改数据时，<code>RwLock</code> 会阻塞其他的读线程和写线程，确保写操作的独占性。</li></ul></li><li><strong>锁的粒度</strong>：<ul><li><code>Mutex</code>：锁粒度较粗，所有对资源的访问（读取和写入）都必须经过 <code>Mutex</code>，保证互斥性。</li><li><code>RwLock</code>：支持两种锁：<strong>读锁</strong>（<code>R</code>）和<strong>写锁</strong>（<code>W</code>）。多个线程可以同时获得读锁，但只有一个线程可以获得写锁，并且写锁会阻止所有读锁和其他写锁。</li></ul></li></ol><h3 id="⚠TODO！-6"><a href="#⚠TODO！-6" class="headerlink" title="⚠TODO！"></a>⚠TODO！</h3>]]></content>
    
    
    <categories>
      
      <category>编程语言</category>
      
      <category>Rust</category>
      
    </categories>
    
    
    <tags>
      
      <tag>编程</tag>
      
      <tag>基础</tag>
      
      <tag>Rust</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
